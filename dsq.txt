print("PhantomXPro - MM2 Script (Ultimate Edition)")

-- تحميل WindUI
local success, result = pcall(function()
    local source = game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua", true)
    if source then
        local loaded = loadstring(source)
        if loaded then
            return loaded()
        end
    end
    return nil
end)

if success and result then
    local WindUI = result
    
    -- =============================================
    -- نظام اللغات - عربي (سعودي) + إنجليزي
    -- =============================================
    _G.SelectedLanguage = _G.SelectedLanguage or "Saudi"
    
    local Translations = {
        Saudi = {
            welcome = "أهلاً وسهلاً في PhantomXPro 🌟",
            warning = "⚠️ تنبيه هام:\nتشغيل السكربت يكون على مسؤوليتك الشخصية والكاملة، ونحن غير مسؤولين عن أي ضرر. مع أننا نعمل على تقديم سكربت بأعلى معايير الأمان الممكنة، هذا تحذير احتراطي في حال حدوث أي مشكلة غير متوقعة.نتمني لكم تجربة ممتعة ❤",
            discord = "🎮 انظم لسيرفر الدسكورد",
            discordContent = "انظم للسيرفر الرسمي للسكربت!\n• آخر التحديثات\n• مساعدة ودعم\n• اقتراحات وتطويرات\n• تحديثات أمان\n\nالرابط: https://discord.gg/CgUa36sPNs",
            copyLink = "انسخ الرابط",
            linkCopied = "✅ انسخت الرابط",
            linkCopiedContent = "رابط الدسكورد في الحافظة",
            scriptClosed = "أقفل السكربت",
            notification = "إشعار",
            
            -- تبويبات
            infoTab = "👤 معلوماتك",
            mainTab = "🏠 الرئيسي",
            espTab = "👁️ رؤية",
            playerTab = "🏃 حركات",
            innocentTab = "🛡️ أبرياء",
            murdererTab = "🔪 قتلة",
            sheriffTab = "🤠 شرطة",
            teleportTab = "📍 انتقالات",
            settingsTab = "⚙️ إعدادات",
            
            -- ESP Labels
            innocentESP = "🟢 بريء",
            murdererESP = "🔴 قاتل",
            sheriffESP = "🔵 شرطي",
            gunESP = "🔫 سلاح",
            coinESP = "🪙 عملة",
            boxESP = "📦 صندوق",
            boxType = "نوع الصندوق",
            nameESP = "🏷️ اسم",
            tracerESP = "📏 خط",
            distanceESP = "📐 مسافة",
            highlight = "🌟 هايلايت",
            
            -- MAIN
            autofarm = "💰 جمع العملات",
            teleportToLobby = "🏠 انتقال للوبي",
            teleportTool = "🛠️ أداة الانتقال",
            
            -- PLAYER
            walkspeedAdjustment = "👟 سرعة المشي (16)",
            jumpPowerAdjustment = "🚀 قوة القفز (50)",
            gravityAdjustment = "🌌 الجاذبية (196.2)",
            fovAdjustment = "🎥 زاوية الرؤية (70)",
            flySpeedAdjustment = "✈️ سرعة الطيران (60)",
            infiniteJump = "☁️ قفز لا نهائي",
            noclipAdjustment = "👻 عبور الجدران",
            flyMode = "🦅 طيران",
            unlockEmotes = "🎭 فتح الإيموشنات",
            unlockEmotesDesc = "يفتح كل الإيموشنات والتويس",
            emoteNameInput = "📝 اسم الإيموشن",
            playEmote = "🎬 تشغيل الإيموشن",
            
            -- INNOCENT
            getDroppedGun = "🔫 سحب السلاح",
            autoGetDroppedGun = "🤖 سحب تلقائي",
            getGunAndShoot = "💥 خذ السلاح و صوب",
            flingTitle = "🌀 تطيير",
            flingTargetInput = "🎯 هدف التطيير",
            flingMode = "طريقة التطيير",
            flingModeFlinger = "Flinger",
            flingModeShhhlol = "Shhhlol",
            flingModeYeet = "Yeet",
            flingActive = "🔥 تفعيل التطيير",
            flingMurdererBtn = "🔪 تطيير القاتل",
            flingSheriffBtn = "🤠 تطيير الشرطي",
            godMode = "🛡️ وضع الخلود",
            godModeMethod = "طريقة الخلود",
            antiVoid = "🌌 منع الهاوية",
            
            -- MURDERER
            aimbot = "🎯 تصويب تلقائي",
            selectPlayer = "👤 اختر لاعب",
            refreshPlayers = "🔄 تحديث القائمة",
            killSelected = "💀 قتل المختار",
            killAllPlayers = "❌ قتل الجميع",
            killSheriff = "💀 قتل الشرطي",
            
            -- SHERIFF
            shotTypeSelection = "🛰️ نوع الطلقة",
            shootMurderer = "🔫 صوب على القاتل",
            lockCamera = "🔒 تثبيت الكاميرا",
            aimbotMurderer = "🎯 تصويب تلقائي",
            shootButton = "🔫 زر الإطلاق",
            shootDistance = "📏 مسافة الإطلاق (500)",
            autoShoot = "🤖 إطلاق تلقائي",
            showManualButton = "🔘 زر يدوي",
            shootPrediction = "📡 توقع الطلقة",
            pingMultiplier = "🌐 مضاعف البينق",
            
            -- TELEPORT
            playerSelectionTeleport = "👤 اختر لاعب",
            teleportToSelected = "🌀 انتقل للاعب",
            updatePlayersList = "🔄 تحديث القائمة",
            teleportToLobbyTP = "🏠 انتقال للوبي",
            teleportToSheriff = "🔵 انتقل لشرطي",
            teleportToMurderer = "🔴 انتقل لقاتل",
            teleportToRandom = "🎲 انتقل عشوائي",
            teleportToGun = "🔫 انتقل للسلاح",
            
            -- SETTINGS
            settings = "⚙️ إعدادات",
            advancedSettings = "🔧 إعدادات متقدمة",
            developer = "💼 المطور",
            
            boostFPS = "🚀 تحسين الفريمات",
            fullBrightness = "💡 إضاءة كاملة",
            antiAFK = "💤 منع الطرد",
            rejoinServer = "🔄 رجع للسيرفر",
            serverHop = "🛰️ غير السيرفر",
            joinLowerServer = "📉 سيرفر بأقل ناس",
            selectTheme = "🎨 غير المظهر",
            updateScript = "🔄 حدث السكربت",
            closeScriptBtn = "❌ قفل السكربت",
            
            -- INFO
            yourName = "👤 اسمك: ",
            yourUsername = "🎫 يوزرك: ",
            accountAge = "📅 عمر الحساب: ",
            hack = "⚡ الهاك: ",
            level = "📊 مستواك: ",
            xp = "⭐ خبرتك: ",
            players = "👥 عدد اللاعبين: ",
            fps = "🔄 فريماتك: ",
            ping = "📡 البينق: ",
            
            -- Developer
            developerName = "Cypher",
            developerDesc = "مبرمج سكربتات روبلوكس",
            joinDiscord = "انضم للدسكورد",
            version = "نسخة 12.0",
            contactDev = "تواصل مع المبرمج"
        },
        English = {
            welcome = "Welcome to PhantomXPro 🌟",
            warning = "⚠️ Important Warning:\nUsing this script is entirely at your own risk and responsibility. We are not responsible for any damage. Although we work to provide a script with the highest possible security standards, this is a precautionary warning in case any unexpected problem occurs. We hope you have an enjoyable experience ❤",
            discord = "🎮 Join Discord Server",
            discordContent = "Join the official script server!\n• Latest updates\n• Support\n• Suggestions\n• Security updates\n\nLink: https://discord.gg/CgUa36sPNs",
            copyLink = "Copy Link",
            linkCopied = "✅ Link Copied",
            linkCopiedContent = "Discord link copied",
            scriptClosed = "Script Closed",
            notification = "Notification",
            
            -- Tabs
            infoTab = "👤 Your Info",
            mainTab = "🏠 Main",
            espTab = "👁️ Esp",
            playerTab = "🏃 Movement",
            innocentTab = "🛡️ Innocent",
            murdererTab = "🔪 Murderer",
            sheriffTab = "🤠 Sheriff",
            teleportTab = "📍 Teleport",
            settingsTab = "⚙️ Settings",
            
            -- ESP Labels
            innocentESP = "🟢 Innocent",
            murdererESP = "🔴 Murderer",
            sheriffESP = "🔵 Sheriff",
            gunESP = "🔫 Gun",
            coinESP = "🪙 Coin",
            boxESP = "📦 Box",
            boxType = "Box Type",
            nameESP = "🏷️ Name",
            tracerESP = "📏 Tracer",
            distanceESP = "📐 Distance",
            highlight = "🌟 Highlight",
            
            -- MAIN
            autofarm = "💰 Collect Coins",
            teleportToLobby = "🏠 Go to Lobby",
            teleportTool = "🛠️ Teleport Tool",
            
            -- PLAYER
            walkspeedAdjustment = "👟 Walk Speed (16)",
            jumpPowerAdjustment = "🚀 Jump Power (50)",
            gravityAdjustment = "🌌 Gravity (196.2)",
            fovAdjustment = "🎥 FOV (70)",
            flySpeedAdjustment = "✈️ Fly Speed (60)",
            infiniteJump = "☁️ Infinite Jump",
            noclipAdjustment = "👻 Noclip",
            flyMode = "🦅 Fly Mode",
            unlockEmotes = "🎭 Unlock Emotes",
            unlockEmotesDesc = "Unlocks all emotes and toys",
            emoteNameInput = "📝 Emote Name",
            playEmote = "🎬 Play Emote",
            
            -- INNOCENT
            getDroppedGun = "🔫 Get Gun",
            autoGetDroppedGun = "🤖 Auto Gun",
            getGunAndShoot = "💥 Get Gun & Shoot",
            flingTitle = "🌀 Fling",
            flingTargetInput = "🎯 Target Input",
            flingMode = "Fling Mode",
            flingModeFlinger = "Flinger",
            flingModeShhhlol = "Shhhlol",
            flingModeYeet = "Yeet",
            flingActive = "🔥 Fling Active",
            flingMurdererBtn = "🔪 Fling Murderer",
            flingSheriffBtn = "🤠 Fling Sheriff",
            godMode = "🛡️ God Mode",
            godModeMethod = "God Mode Method",
            antiVoid = "🌌 Anti Void",
            
            -- MURDERER
            aimbot = "🎯 Aimbot",
            selectPlayer = "👤 Select Player",
            refreshPlayers = "🔄 Refresh List",
            killSelected = "💀 Kill Selected",
            killAllPlayers = "❌ Kill All",
            killSheriff = "💀 Kill Sheriff",
            
            -- SHERIFF
            shotTypeSelection = "🛰️ Shot Type",
            shootMurderer = "🔫 Shoot Murderer",
            lockCamera = "🔒 Lock Camera",
            aimbotMurderer = "🎯 Aimbot",
            shootButton = "🔫 Shoot Button",
            shootDistance = "📏 Shoot Distance (500)",
            autoShoot = "🤖 Auto Shoot",
            showManualButton = "🔘 Manual Button",
            shootPrediction = "📡 Shoot Prediction",
            pingMultiplier = "🌐 Ping Multiplier",
            
            -- TELEPORT
            playerSelectionTeleport = "👤 Select Player",
            teleportToSelected = "🌀 Teleport to Player",
            updatePlayersList = "🔄 Update List",
            teleportToLobbyTP = "🏠 Teleport to Lobby",
            teleportToSheriff = "🔵 Teleport to Sheriff",
            teleportToMurderer = "🔴 Teleport to Murderer",
            teleportToRandom = "🎲 Teleport Random",
            teleportToGun = "🔫 Teleport to Gun",
            
            -- SETTINGS
            settings = "⚙️ Settings",
            advancedSettings = "🔧 Advanced",
            developer = "💼 Developer",
            
            boostFPS = "🚀 Boost FPS",
            fullBrightness = "💡 Full Bright",
            antiAFK = "💤 Anti AFK",
            rejoinServer = "🔄 Rejoin Server",
            serverHop = "🛰️ Server Hop",
            joinLowerServer = "📉 Join Lower Server",
            selectTheme = "🎨 Select Theme",
            updateScript = "🔄 Update Script",
            closeScriptBtn = "❌ Close Script",
            
            -- INFO
            yourName = "👤 Name: ",
            yourUsername = "🎫 Username: ",
            accountAge = "📅 Account Age: ",
            hack = "⚡ Executor: ",
            level = "📊 Level: ",
            xp = "⭐ XP: ",
            players = "👥 Players: ",
            fps = "🔄 FPS: ",
            ping = "📡 Ping: ",
            
            -- Developer
            developerName = "Cypher",
            developerDesc = "Roblox Script Developer",
            joinDiscord = "Join Discord",
            version = "Version 12.0",
            contactDev = "Contact Developer"
        }
    }
    
    local function T(key)
        return Translations[_G.SelectedLanguage][key] or key
    end
    
    local function cleanupGUI()
        for _, v in pairs(game.CoreGui:GetChildren()) do
            if v:IsA("ScreenGui") and (v.Name:find("Wind") or v.Name:find("Phantom") or v.Name:find("Shoot") or v.Name:find("Hawk")) then
                v:Destroy()
            end
        end
    end
    
    -- =============================================
    -- المتغيرات العالمية
    -- =============================================
    _G.WalkspeedValue = 16
    _G.JumpPowerValue = 50
    _G.GravityValue = 196.2
    _G.FOVValue = 70
    _G.FlySpeedValue = 60
    _G.ShootDistance = 500
    _G.LockCameraEnabled = false
    _G.XrayEnabled = false
    _G.MurdererESPEnabled = false
    _G.InnocentESPEnabled = false
    _G.SheriffESPEnabled = false
    _G.GunESPEnabled = false
    _G.CoinESPEnabled = false
    _G.InfiniteJumpEnabled = false
    _G.NoclipEnabled = false
    _G.FlyEnabled = false
    _G.AutoGetGunEnabled = false
    _G.KillAllEnabled = false
    _G.AimbotEnabled = false
    _G.AimbotMurdererEnabled = false
    _G.FullBrightEnabled = false
    _G.AntiAFKEnabled = true
    _G.BoostFPSEnabled = false
    _G.GodModeEnabled = false
    _G.GodModeMethod = "Health Math.huge"
    _G.AntiVoidEnabled = false
    _G.AutofarmActive = false
    _G.AutoShootEnabled = false
    _G.ShootButtonActive = false
    _G.ShootPrediction = 0
    _G.PingMultiplier = 0
    
    -- متغيرات ESP من Rise
    local featureStates = {
        InnocentESP = {names = false, boxes = false, tracers = false, distance = false, boxType = "2D"},
        MurderESP = {names = false, boxes = false, tracers = false, distance = false, boxType = "2D"},
        HeroSheriffESP = {names = false, boxes = false, tracers = false, distance = false, boxType = "2D"},
        GunESP = {names = false, boxes = false, tracers = false, distance = false, boxType = "3D"},
        CoinESP = {names = false, boxes = false, tracers = false, distance = false, boxType = "3D"},
        InnocentHighlights = false,
        MurderHighlights = false,
        SheriffHeroHighlights = false,
        CoinHighlights = false,
        GunHighlights = false
    }
    
    -- متغيرات ESP
    local innocentEspElements = {}
    local murderEspElements = {}
    local sheriffEspElements = {}
    local coinEspElements = {}
    local gunEspElements = {}
    local playerEspConnection = nil
    local roleUpdateConnection = nil
    local coinEspConnection = nil
    local gunEspConnection = nil
    local HighlightsConnection = nil
    
    local roleData = {}
    local lastRoleUpdate = 0
    local roleUpdating = false
    local lastCoinSearch = 0
    local lastGunSearch = 0
    local coinCache = {}
    local gunCache = {}
    local cachedPlayers = {}
    local lastPlayerCacheUpdate = 0
    
    -- متغيرات الطيران
    local BodyVelocity = nil
    local BodyGyro = nil
    
    -- متغيرات التجميد للقتل الجماعي
    local frozenPlayers = {}
    
    -- متغيرات الفلنق من Rise
    local flingActive = false
    local flingMode = 1
    local flingInputValue = ""
    local currentInput = ""
    local processedPlayers = {}
    local flingRunning = false
    
    -- متغيرات God Mode
    local godModeConnection = nil
    
    -- متغيرات Auto Shoot
    local autoShootConnection = nil
    local sheriffGui = nil
    
    -- متغيرات Gun Grabber
    local GunSystem = {
        AutoGrabEnabled = false,
        NotifyGun = false,
        GunDropCheckInterval = 1,
        ActiveGunDrops = {},
        Mode = "Grab only"
    }
    local notifiedGunPickups = {}
    local notifiedGunSpawns = {}
    
    -- متغيرات Anti Void
    local originalDestroyHeight = nil
    
    -- متغيرات Auto Farm
    local AutoFarm = {
        Enabled = false,
        Mode = "Teleport",
        TeleportDelay = 0,
        MoveSpeed = 50,
        CoinCheckInterval = 0.5,
        CoinContainers = {
            "Factory", "Hospital3", "MilBase", "House2", "Workplace",
            "Mansion2", "BioLab", "Hotel", "Bank2", "PoliceStation",
            "ResearchFacility", "Lobby"
        },
        Connection = nil
    }
    
    -- إحداثيات الكوتش
    local LobbyPosition = CFrame.new(361.785, 521.475, -11.444)
    
    -- متغيرات الاتصالات
    local espConnections = {}
    local espLoops = {}
    
    -- =============================================
    -- الدوال المساعدة من Rise
    -- =============================================
    
    local function SendNotification(title, description)
        if WindUI then
            WindUI:Notify({
                Title = title,
                Content = description,
                Duration = 3
            })
        end
    end
    
    local function findMurderer()
        local Players = game:GetService("Players")
        local player = Players.LocalPlayer
        
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player and p.Character then
                local hasKnife = p.Character:FindFirstChild("Knife") or 
                                (p.Backpack and p.Backpack:FindFirstChild("Knife"))
                if hasKnife then
                    return p
                end
            end
        end
        return nil
    end
    
    local function findSheriff()
        local Players = game:GetService("Players")
        local player = Players.LocalPlayer
        
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player and p.Character then
                local hasGun = p.Character:FindFirstChild("Gun") or 
                              (p.Backpack and p.Backpack:FindFirstChild("Gun"))
                if hasGun then
                    return p
                end
            end
        end
        return nil
    end
    
    local function findGun()
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart") and obj.Name == "DropGun" then
                return obj
            end
        end
        return nil
    end
    
    local function findCoins()
        local coins = {}
        for _, container in pairs(workspace:GetDescendants()) do
            if container:IsA("Model") and container.Name == "CoinContainer" then
                for _, coinServer in pairs(container:GetChildren()) do
                    if coinServer.Name == "Coin_Server" then
                        local coinVisual = coinServer:FindFirstChild("CoinVisual")
                        if coinVisual then
                            local mainCoin = coinVisual:FindFirstChild("MainCoin")
                            if mainCoin and mainCoin:IsA("MeshPart") and mainCoin.Parent then
                                table.insert(coins, mainCoin)
                            end
                        end
                    end
                end
            end
        end
        return coins
    end
    
    local function GetMurderer()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local Players = game:GetService("Players")
        
        local success, roles = pcall(function()
            return ReplicatedStorage:FindFirstChild("GetPlayerData", true):InvokeServer()
        end)
        if success and roles then
            for name, data in pairs(roles) do
                if data.Role == "Murderer" then
                    return Players:FindFirstChild(name)
                end
            end
        end
        return nil
    end
    
    -- =============================================
    -- ESP Functions من Rise
    -- =============================================
    
    function IsAlive(plr)
        return plr and plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health > 0
    end

    function getOutlineColor(color)
        if color.R > 0.8 and color.G > 0.8 and color.B > 0.8 then
            return Color3.new(0,0,0)
        else
            return Color3.new(1,1,1)
        end
    end

    function getDistanceFromPlayer(targetPosition)
        local player = game.Players.LocalPlayer
        if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then return 0 end
        return (targetPosition - player.Character.HumanoidRootPart.Position).Magnitude
    end

    function cleanupDrawingTable(drawingTable)
        if not drawingTable then return end
        for _, drawing in pairs(drawingTable) do
            if type(drawing) == "table" then
                for _, line in ipairs(drawing) do
                    if line and line.Remove then pcall(line.Remove, line) end
                end
            else
                if drawing and drawing.Remove then pcall(drawing.Remove, drawing) end
            end
        end
    end

    function createESPObject()
        return {
            box = Drawing.new("Square"),
            tracer = Drawing.new("Line"),
            name = Drawing.new("Text"),
            distance = Drawing.new("Text"),
            boxLines = {}
        }
    end

    function setupESPObject(esp)
        esp.box.Thickness = 2
        esp.box.Filled = false
        esp.box.Visible = false

        esp.tracer.Thickness = 1
        esp.tracer.Visible = false

        esp.name.Size = 14
        esp.name.Center = true
        esp.name.Outline = true
        esp.name.Visible = false

        esp.distance.Size = 14
        esp.distance.Center = true
        esp.distance.Outline = true
        esp.distance.Visible = false
    end

    function draw3DBox(esp, cf, pos, cam, boxColor, boxSize)
        if not cf or not cam then return end

        boxSize = boxSize or Vector3.new(4, 5, 3)
        local size = boxSize
        local offsets = {
            Vector3.new( size.X/2,  size.Y/2,  size.Z/2), Vector3.new( size.X/2,  size.Y/2, -size.Z/2),
            Vector3.new( size.X/2, -size.Y/2,  size.Z/2), Vector3.new( size.X/2, -size.Y/2, -size.Z/2),
            Vector3.new(-size.X/2,  size.Y/2,  size.Z/2), Vector3.new(-size.X/2,  size.Y/2, -size.Z/2),
            Vector3.new(-size.X/2, -size.Y/2,  size.Z/2), Vector3.new(-size.X/2, -size.Y/2, -size.Z/2),
        }

        local screenPoints = {}
        local anyPointOnScreen = false

        for i, offset in ipairs(offsets) do
            local success, vec, onScreen = pcall(function()
                local worldPos = cf * CFrame.Angles(0, math.rad(90), 0) * offset
                return cam:WorldToViewportPoint(worldPos)
            end)
            if success then
                screenPoints[i] = {pos = Vector2.new(vec.X, vec.Y), depth = vec.Z, onScreen = onScreen}
                if onScreen and vec.Z > 0 then anyPointOnScreen = true end
            end
        end

        if not esp.boxLines or #esp.boxLines == 0 then
            esp.boxLines = {}
            for i = 1, 12 do
                local line = Drawing.new("Line")
                line.Thickness = 1
                line.ZIndex = 2
                table.insert(esp.boxLines, line)
            end
        end

        local edges = {
            {1, 2}, {1, 3}, {1, 5}, {2, 4}, {2, 6},
            {3, 4}, {3, 7}, {5, 6}, {5, 7}, {4, 8}, {6, 8}, {7, 8}
        }

        local dist = getDistanceFromPlayer(pos) or 10
        local thickness = math.clamp(3 / (dist / 50), 1, 3)

        for i, edge in ipairs(edges) do
            local line = esp.boxLines[i]
            if line then
                local p1, p2 = screenPoints[edge[1]], screenPoints[edge[2]]
                line.Color = boxColor or Color3.fromRGB(255, 255, 255)
                line.Thickness = thickness
                line.Transparency = 1
                if anyPointOnScreen and p1 and p2 and p1.depth > 0 and p2.depth > 0 then
                    line.From = p1.pos
                    line.To = p2.pos
                    line.Visible = true
                else
                    line.Visible = false
                end
            end
        end
    end

    function isAnyRoleESPActive()
        local roles = {"InnocentESP", "MurderESP", "HeroSheriffESP", "CoinESP", "GunESP"}
        for _, role in ipairs(roles) do
            local states = featureStates[role]
            if states and (states.names or states.boxes or states.tracers or states.distance) then
                return true
            end
        end
        return false
    end

    function checkAnyHighlights()
        return featureStates.InnocentHighlights or featureStates.MurderHighlights or featureStates.SheriffHeroHighlights or featureStates.CoinHighlights or featureStates.GunHighlights
    end

    function isAnyRoleNeeded()
        return isAnyRoleESPActive() or checkAnyHighlights()
    end

    function updateRoles()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local getPlayerDataRemote = ReplicatedStorage:FindFirstChild("GetPlayerData", true)
        if not getPlayerDataRemote then return end
        if tick() - lastRoleUpdate < 2 then return end
        lastRoleUpdate = tick()

        local success, roles = pcall(function()
            return getPlayerDataRemote:InvokeServer()
        end)

        if success and roles then
            roleData = {}
            for key, v in pairs(roles) do
                if v then roleData[key] = v end
            end
            refreshHighlights()
        end
    end

    function startRoleUpdating()
        local RunService = game:GetService("RunService")
        if roleUpdating then return end
        roleUpdating = true
        updateRoles()
        roleUpdateConnection = RunService.Heartbeat:Connect(function()
            updateRoles()
        end)
    end

    function stopRoleUpdating()
        if roleUpdateConnection then
            roleUpdateConnection:Disconnect()
            roleUpdateConnection = nil
        end
        roleUpdating = false
        roleData = {}
    end

    function manageRoleUpdating()
        if isAnyRoleNeeded() then
            startRoleUpdating()
        else
            stopRoleUpdating()
        end
    end

    function getPlayerRole(plr)
        local playerKey = plr.Name
        return roleData[playerKey] and roleData[playerKey].Role
    end

    function findCoinServerParts()
        if tick() - lastCoinSearch < 3 then return coinCache end
        lastCoinSearch = tick()
        coinCache = {}
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("Part") and obj.Name == "Coin_Server" then
                table.insert(coinCache, obj)
            end
        end
        return coinCache
    end

    function findDropGunParts()
        if tick() - lastGunSearch < 3 then return gunCache end
        lastGunSearch = tick()
        gunCache = {}
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("Part") and obj.Name == "DropGun" then
                table.insert(gunCache, obj)
            end
        end
        return gunCache
    end

    function getCachedPlayers()
        local Players = game:GetService("Players")
        if tick() - lastPlayerCacheUpdate < 1 then return cachedPlayers end
        lastPlayerCacheUpdate = tick()
        cachedPlayers = Players:GetPlayers()
        return cachedPlayers
    end

    function updateRoleHighlights()
        if not roleData then return end

        local Players = game:GetService("Players")
        local sheriffName = nil
        for name, data in pairs(roleData) do
            if data.Role == "Sheriff" then
                sheriffName = name
                break
            end
        end
        local sheriffPlayer = sheriffName and Players:FindFirstChild(sheriffName)
        local isSheriffAlive = sheriffPlayer and IsAlive(sheriffPlayer)

        for _, plr in ipairs(getCachedPlayers()) do
            if plr ~= game.Players.LocalPlayer and plr.Character then
                local model = plr.Character
                local highlight = model:FindFirstChild("PlayerHighlight")
                local data = roleData[plr.Name]
                local role = data and data.Role
                local isAlivePlayer = IsAlive(plr)

                local highlightEnabled = false
                if not role then highlightEnabled = featureStates.InnocentHighlights
                elseif role == "Murderer" then highlightEnabled = featureStates.MurderHighlights
                elseif role == "Sheriff" or role == "Hero" then highlightEnabled = featureStates.SheriffHeroHighlights
                else highlightEnabled = featureStates.InnocentHighlights end

                if highlightEnabled then
                    local color = Color3.new(1,1,1)
                    if isAlivePlayer then
                        if role == "Murderer" then color = Color3.fromRGB(225, 0, 0)
                        elseif role == "Sheriff" then color = Color3.fromRGB(0, 0, 225)
                        elseif role == "Hero" and not isSheriffAlive then color = Color3.fromRGB(255, 250, 0)
                        else color = Color3.fromRGB(0, 225, 0) end
                    end

                    if not highlight then
                        highlight = Instance.new("Highlight")
                        highlight.Name = "PlayerHighlight"
                        highlight.Adornee = model
                        highlight.FillTransparency = 0.5
                        highlight.OutlineTransparency = 0
                        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                        highlight.Parent = model
                    end
                    highlight.FillColor = color
                    highlight.OutlineColor = getOutlineColor(color)
                else
                    if highlight then highlight:Destroy() end
                end
            end
        end

        if featureStates.CoinHighlights then
            for _, coin in ipairs(findCoinServerParts()) do
                if coin and coin.Parent then
                    local h = coin:FindFirstChild("ItemHighlight")
                    if not h then
                        h = Instance.new("Highlight")
                        h.Name = "ItemHighlight"
                        h.Adornee = coin
                        h.FillTransparency = 0.5
                        h.Parent = coin
                    end
                    h.FillColor = Color3.fromRGB(255, 215, 0)
                    h.OutlineColor = Color3.new(0,0,0)
                end
            end
        end

        if featureStates.GunHighlights then
            for _, gun in ipairs(findDropGunParts()) do
                if gun and gun.Parent then
                    local h = gun:FindFirstChild("ItemHighlight")
                    if not h then
                        h = Instance.new("Highlight")
                        h.Name = "ItemHighlight"
                        h.Adornee = gun
                        h.FillTransparency = 0.5
                        h.Parent = gun
                    end
                    h.FillColor = Color3.fromRGB(255, 0, 255)
                    h.OutlineColor = Color3.new(1,1,1)
                end
            end
        end
    end

    function clearAllHighlights()
        local Players = game:GetService("Players")
        for _, plr in pairs(Players:GetPlayers()) do
            if plr.Character then
                local h = plr.Character:FindFirstChild("PlayerHighlight")
                if h then h:Destroy() end
            end
        end
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj.Name == "Coin_Server" or obj.Name == "DropGun" then
                local h = obj:FindFirstChild("ItemHighlight")
                if h then h:Destroy() end
            end
        end
    end

    function manageHighlightsConnection()
        local RunService = game:GetService("RunService")
        if checkAnyHighlights() then
            if not HighlightsConnection then
                HighlightsConnection = RunService.Heartbeat:Connect(updateRoleHighlights)
            end
        else
            if HighlightsConnection then
                HighlightsConnection:Disconnect()
                HighlightsConnection = nil
                clearAllHighlights()
            end
        end
        manageRoleUpdating()
    end

    function refreshHighlights()
        if HighlightsConnection then clearAllHighlights() end
    end

    function updateRoleESP()
        local Players = game:GetService("Players")
        local Workspace = game:GetService("Workspace")
        local camera = Workspace.CurrentCamera
        local player = Players.LocalPlayer
        if not camera then return end
        local screenBottomCenter = Vector2.new(camera.ViewportSize.X / 2, camera.ViewportSize.Y)

        local currentTargets = {Innocent={}, Murder={}, Sheriff={}}

        for _, otherPlayer in ipairs(getCachedPlayers()) do
            if otherPlayer ~= player and otherPlayer.Character then
                local hrp = otherPlayer.Character:FindFirstChild("HumanoidRootPart")
                local hum = otherPlayer.Character:FindFirstChild("Humanoid")

                if hrp and hum then
                    local role = getPlayerRole(otherPlayer)
                    local isDead = hum.Health <= 0

                    local config, elements, targetTable, color

                    if isDead then
                        config = featureStates.InnocentESP
                        elements = innocentEspElements
                        targetTable = currentTargets.Innocent
                        color = Color3.fromRGB(200, 200, 200)
                    elseif role == "Murderer" then
                        config = featureStates.MurderESP
                        elements = murderEspElements
                        targetTable = currentTargets.Murder
                        color = Color3.fromRGB(255, 0, 0)
                    elseif role == "Sheriff" or role == "Hero" then
                        config = featureStates.HeroSheriffESP
                        elements = sheriffEspElements
                        targetTable = currentTargets.Sheriff
                        color = Color3.fromRGB(0, 0, 255)
                    else
                        config = featureStates.InnocentESP
                        elements = innocentEspElements
                        targetTable = currentTargets.Innocent
                        color = Color3.fromRGB(0, 255, 0)
                    end

                    if config.names or config.boxes or config.tracers or config.distance then
                        targetTable[otherPlayer.Character] = true

                        if not elements[otherPlayer.Character] then
                            elements[otherPlayer.Character] = createESPObject()
                            setupESPObject(elements[otherPlayer.Character])
                        end

                        local esp = elements[otherPlayer.Character]
                        local vec, onScreen = camera:WorldToViewportPoint(hrp.Position)

                        if onScreen then
                            local topY = camera:WorldToViewportPoint(hrp.Position + Vector3.new(0, 3, 0)).Y
                            local bottomY = camera:WorldToViewportPoint(hrp.Position - Vector3.new(0, 3, 0)).Y
                            local sizeY = math.abs(bottomY - topY)
                            local sizeX = sizeY / 2

                            if config.boxes then
                                if config.boxType == "2D" then
                                    esp.box.Visible = true
                                    esp.box.Size = Vector2.new(sizeX, sizeY)
                                    esp.box.Position = Vector2.new(vec.X - sizeX/2, vec.Y - sizeY/2)
                                    esp.box.Color = color
                                    for _, l in ipairs(esp.boxLines) do l.Visible = false end
                                else
                                    esp.box.Visible = false
                                    local boxSize = Vector3.new(2, 5, 2)
                                    draw3DBox(esp, hrp.CFrame, hrp.Position, camera, color, boxSize)
                                end
                            else
                                esp.box.Visible = false
                                for _, l in ipairs(esp.boxLines) do l.Visible = false end
                            end

                            if config.tracers then
                                esp.tracer.Visible = true
                                esp.tracer.From = screenBottomCenter
                                esp.tracer.To = Vector2.new(vec.X, vec.Y)
                                esp.tracer.Color = color
                            else
                                esp.tracer.Visible = false
                            end

                            if config.names then
                                esp.name.Visible = true
                                esp.name.Text = otherPlayer.Name
                                esp.name.Position = Vector2.new(vec.X, vec.Y - sizeY/2 - 15)
                                esp.name.Color = color
                            else
                                esp.name.Visible = false
                            end

                            if config.distance then
                                esp.distance.Visible = true
                                esp.distance.Text = math.floor((hrp.Position - camera.CFrame.Position).Magnitude) .. "m"
                                esp.distance.Position = Vector2.new(vec.X, vec.Y + sizeY/2 + 5)
                                esp.distance.Color = color
                            else
                                esp.distance.Visible = false
                            end
                        else
                            esp.box.Visible = false
                            esp.tracer.Visible = false
                            esp.name.Visible = false
                            esp.distance.Visible = false
                            for _, l in ipairs(esp.boxLines) do l.Visible = false end
                        end
                    end
                end
            end
        end

        for target, esp in pairs(innocentEspElements) do
            if not currentTargets.Innocent[target] then
                cleanupDrawingTable(esp)
                innocentEspElements[target] = nil
            end
        end
        for target, esp in pairs(murderEspElements) do
            if not currentTargets.Murder[target] then
                cleanupDrawingTable(esp)
                murderEspElements[target] = nil
            end
        end
        for target, esp in pairs(sheriffEspElements) do
            if not currentTargets.Sheriff[target] then
                cleanupDrawingTable(esp)
                sheriffEspElements[target] = nil
            end
        end
    end

    function managePlayerESPConnection()
        local RunService = game:GetService("RunService")
        if isAnyRoleESPActive() then
            if not playerEspConnection then
                playerEspConnection = RunService.RenderStepped:Connect(updateRoleESP)
            end
        else
            if playerEspConnection then
                playerEspConnection:Disconnect()
                playerEspConnection = nil

                for _, tbl in pairs({innocentEspElements, murderEspElements, sheriffEspElements}) do
                    for _, esp in pairs(tbl) do cleanupDrawingTable(esp) end
                end
                innocentEspElements = {}
                murderEspElements = {}
                sheriffEspElements = {}
            end
        end
        manageRoleUpdating()
    end

    function updateItemESP(config, elementTable, findFunc, itemName, color)
        local camera = workspace.CurrentCamera
        if not camera then return end
        local screenBot = Vector2.new(camera.ViewportSize.X/2, camera.ViewportSize.Y)
        local currentItems = {}

        for _, item in ipairs(findFunc()) do
            if item and item.Parent then
                currentItems[item] = true
                if not elementTable[item] then
                    elementTable[item] = createESPObject()
                    setupESPObject(elementTable[item])
                end
                local esp = elementTable[item]
                local vec, onScreen = camera:WorldToViewportPoint(item.Position)

                if onScreen and (config.names or config.boxes or config.tracers or config.distance) then
                    local size = 20
                    if config.boxes then
                        if config.boxType == "2D" then
                            esp.box.Visible = true
                            esp.box.Size = Vector2.new(size, size)
                            esp.box.Position = Vector2.new(vec.X - size/2, vec.Y - size/2)
                            esp.box.Color = color
                            for _, l in ipairs(esp.boxLines) do l.Visible = false end
                        else
                            esp.box.Visible = false
                            draw3DBox(esp, item.CFrame, item.Position, camera, color, Vector3.new(2,2,2))
                        end
                    else
                        esp.box.Visible = false
                        for _, l in ipairs(esp.boxLines) do l.Visible = false end
                    end

                    if config.tracers then
                        esp.tracer.Visible = true
                        esp.tracer.From = screenBot
                        esp.tracer.To = Vector2.new(vec.X, vec.Y)
                        esp.tracer.Color = color
                    else
                        esp.tracer.Visible = false
                    end

                    if config.names then
                        esp.name.Visible = true
                        esp.name.Text = itemName
                        esp.name.Position = Vector2.new(vec.X, vec.Y - size/2 - 15)
                        esp.name.Color = color
                    else
                        esp.name.Visible = false
                    end

                    if config.distance then
                        esp.distance.Visible = true
                        esp.distance.Text = math.floor((item.Position - camera.CFrame.Position).Magnitude) .. "m"
                        esp.distance.Position = Vector2.new(vec.X, vec.Y + size/2 + 5)
                        esp.distance.Color = color
                    else
                        esp.distance.Visible = false
                    end
                else
                    esp.box.Visible = false
                    esp.tracer.Visible = false
                    esp.name.Visible = false
                    esp.distance.Visible = false
                    for _, l in ipairs(esp.boxLines) do l.Visible = false end
                end
            end
        end

        for item, esp in pairs(elementTable) do
            if not currentItems[item] then
                cleanupDrawingTable(esp)
                elementTable[item] = nil
            end
        end
    end

    function manageCoinESPConnection()
        local RunService = game:GetService("RunService")
        local c = featureStates.CoinESP
        if c.names or c.boxes or c.tracers or c.distance then
            if not coinEspConnection then
                coinEspConnection = RunService.RenderStepped:Connect(function()
                    updateItemESP(featureStates.CoinESP, coinEspElements, findCoinServerParts, "Coin", Color3.fromRGB(255, 215, 0))
                end)
            end
        else
            if coinEspConnection then
                coinEspConnection:Disconnect()
                coinEspConnection = nil
                for _, esp in pairs(coinEspElements) do cleanupDrawingTable(esp) end
                coinEspElements = {}
            end
        end
    end

    function manageGunESPConnection()
        local RunService = game:GetService("RunService")
        local g = featureStates.GunESP
        if g.names or g.boxes or g.tracers or g.distance then
            if not gunEspConnection then
                gunEspConnection = RunService.RenderStepped:Connect(function()
                    updateItemESP(featureStates.GunESP, gunEspElements, findDropGunParts, "Gun", Color3.fromRGB(255, 0, 255))
                end)
            end
        else
            if gunEspConnection then
                gunEspConnection:Disconnect()
                gunEspConnection = nil
                for _, esp in pairs(gunEspElements) do cleanupDrawingTable(esp) end
                gunEspElements = {}
            end
        end
    end
    
    -- =============================================
    -- باقي الدوال
    -- =============================================
    
    local function setupXray()
        if _G.XrayEnabled then
            for _, part in pairs(workspace:GetDescendants()) do
                if part:IsA("BasePart") and not part:IsA("MeshPart") then
                    part.LocalTransparencyModifier = 0.5
                end
            end
        else
            for _, part in pairs(workspace:GetDescendants()) do
                if part:IsA("BasePart") and not part:IsA("MeshPart") then
                    part.LocalTransparencyModifier = 0
                end
            end
        end
    end
    
    local function setupInfiniteJump()
        local UserInputService = game:GetService("UserInputService")
        local player = game.Players.LocalPlayer
        
        if _G.InfiniteJumpEnabled then
            if espConnections.infiniteJump then
                espConnections.infiniteJump:Disconnect()
            end
            espConnections.infiniteJump = UserInputService.JumpRequest:Connect(function()
                if player.Character and player.Character:FindFirstChild("Humanoid") then
                    player.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end)
        elseif espConnections.infiniteJump then
            espConnections.infiniteJump:Disconnect()
            espConnections.infiniteJump = nil
        end
    end
    
    local function setupNoclip()
        local RunService = game:GetService("RunService")
        local player = game.Players.LocalPlayer
        
        if _G.NoclipEnabled then
            if espConnections.noclip then
                espConnections.noclip:Disconnect()
            end
            espConnections.noclip = RunService.Stepped:Connect(function()
                if player.Character then
                    for _, part in pairs(player.Character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
            end)
        elseif espConnections.noclip then
            espConnections.noclip:Disconnect()
            espConnections.noclip = nil
        end
    end
    
    local function setupFly()
        local RunService = game:GetService("RunService")
        local Camera = workspace.CurrentCamera
        local player = game.Players.LocalPlayer
        
        if _G.FlyEnabled then
            if BodyVelocity then BodyVelocity:Destroy() end
            if BodyGyro then BodyGyro:Destroy() end
            
            local Character = player.Character
            if not Character then return end
            
            local RootPart = Character:FindFirstChild("HumanoidRootPart")
            local Humanoid = Character:FindFirstChild("Humanoid")
            
            if not RootPart or not Humanoid then return end
            
            BodyVelocity = Instance.new("BodyVelocity")
            BodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            BodyVelocity.Velocity = Vector3.new(0, 0.1, 0)
            BodyVelocity.Parent = RootPart
            
            BodyGyro = Instance.new("BodyGyro")
            BodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
            BodyGyro.P = 10000
            BodyGyro.D = 500
            BodyGyro.CFrame = RootPart.CFrame
            BodyGyro.Parent = RootPart
            
            Humanoid.PlatformStand = true
            
            if espConnections.fly then
                espConnections.fly:Disconnect()
            end
            
            espConnections.fly = RunService.RenderStepped:Connect(function()
                if not _G.FlyEnabled or not RootPart or not RootPart.Parent then return end
                
                local moveDirection = Humanoid.MoveDirection
                local targetVelocity = Vector3.new(0, 0.1, 0)
                
                if moveDirection.Magnitude > 0 then
                    local cameraCFrame = Camera.CFrame
                    targetVelocity = moveDirection * _G.FlySpeedValue
                    
                    if math.abs(cameraCFrame.LookVector.Y) > 0.1 then
                        targetVelocity = Vector3.new(targetVelocity.X, cameraCFrame.LookVector.Y * _G.FlySpeedValue, targetVelocity.Z)
                    end
                end
                
                BodyVelocity.Velocity = BodyVelocity.Velocity:Lerp(targetVelocity, 0.15)
                BodyGyro.CFrame = BodyGyro.CFrame:Lerp(Camera.CFrame, 0.15)
            end)
        else
            if espConnections.fly then
                espConnections.fly:Disconnect()
                espConnections.fly = nil
            end
            if BodyVelocity then BodyVelocity:Destroy() end
            if BodyGyro then BodyGyro:Destroy() end
            BodyVelocity, BodyGyro = nil, nil
            local Character = player.Character
            if Character then
                local Humanoid = Character:FindFirstChild("Humanoid")
                if Humanoid then Humanoid.PlatformStand = false end
            end
        end
    end
    
    local function setupLockCamera()
        local RunService = game:GetService("RunService")
        local player = game.Players.LocalPlayer
        
        if _G.LockCameraEnabled then
            if espConnections.lockCamera then
                espConnections.lockCamera:Disconnect()
            end
            
            espConnections.lockCamera = RunService.RenderStepped:Connect(function()
                if not _G.LockCameraEnabled then
                    if espConnections.lockCamera then
                        espConnections.lockCamera:Disconnect()
                        espConnections.lockCamera = nil
                    end
                    return
                end
                
                local target = findMurderer()
                if not target then
                    target = findSheriff()
                end
                
                if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                    if player.Character and player.Character:FindFirstChild("Head") then
                        workspace.CurrentCamera.CFrame = CFrame.new(
                            workspace.CurrentCamera.CFrame.Position,
                            target.Character.HumanoidRootPart.Position
                        )
                    end
                end
            end)
        end
    end
    
    local function setupAimbot(targetType)
        local RunService = game:GetService("RunService")
        local player = game.Players.LocalPlayer
        
        if targetType == "sheriff" and not _G.AimbotEnabled then
            if espConnections.aimbot then
                espConnections.aimbot:Disconnect()
                espConnections.aimbot = nil
            end
            return
        end
        
        if targetType == "murderer" and not _G.AimbotMurdererEnabled then
            if espConnections.aimbotMurderer then
                espConnections.aimbotMurderer:Disconnect()
                espConnections.aimbotMurderer = nil
            end
            return
        end
        
        local connection = RunService.Heartbeat:Connect(function()
            local target = nil
            if targetType == "sheriff" then
                target = findSheriff()
            elseif targetType == "murderer" then
                target = findMurderer()
            end
            
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = CFrame.new(
                        player.Character.HumanoidRootPart.Position,
                        target.Character.HumanoidRootPart.Position
                    )
                end
            end
        end)
        
        if targetType == "sheriff" then
            if espConnections.aimbot then
                espConnections.aimbot:Disconnect()
            end
            espConnections.aimbot = connection
        else
            if espConnections.aimbotMurderer then
                espConnections.aimbotMurderer:Disconnect()
            end
            espConnections.aimbotMurderer = connection
        end
    end
    
    local function setupAutoGetGun()
        local RunService = game:GetService("RunService")
        local player = game.Players.LocalPlayer
        
        if _G.AutoGetGunEnabled then
            if espConnections.autoGetGun then
                espConnections.autoGetGun:Disconnect()
            end
            
            espConnections.autoGetGun = RunService.Heartbeat:Connect(function()
                local gun = findGun()
                if gun and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = gun.CFrame + Vector3.new(0, 3, 0)
                end
            end)
        elseif espConnections.autoGetGun then
            espConnections.autoGetGun:Disconnect()
            espConnections.autoGetGun = nil
        end
    end
    
    local function setupKillAll()
        _G.KillAllEnabled = true
        local Players = game:GetService("Players")
        local RunService = game:GetService("RunService")
        local lp = Players.LocalPlayer
        frozenPlayers = {}
        
        if espConnections.killAll then
            espConnections.killAll:Disconnect()
        end
        
        espConnections.killAll = RunService.RenderStepped:Connect(function()
            if not _G.KillAllEnabled then return end
            
            local myChar = lp.Character
            if not myChar then return end
            
            local myRoot = myChar:FindFirstChild("HumanoidRootPart") or myChar:FindFirstChild("Torso")
            if not myRoot then return end
            
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= lp and not frozenPlayers[player] and player.Character then
                    local targetRoot = player.Character:FindFirstChild("HumanoidRootPart") or player.Character:FindFirstChild("Torso")
                    if targetRoot then
                        targetRoot.CFrame = myRoot.CFrame * CFrame.new(0, 0, -7)
                        targetRoot.Anchored = true
                        frozenPlayers[player] = true
                    end
                end
            end
        end)
    end
    
    local function giveTeleportTool()
        local player = game.Players.LocalPlayer
        local toolName = "Teleport Tool"
        
        local existingTool = player.Backpack:FindFirstChild(toolName) or 
                            (player.Character and player.Character:FindFirstChild(toolName))
        
        if existingTool then
            existingTool:Destroy()
        end
        
        local TpTool = Instance.new("Tool")
        TpTool.Name = toolName
        TpTool.RequiresHandle = false
        TpTool.CanBeDropped = false
        TpTool.Parent = player.Backpack
        TpTool.ToolTip = _G.SelectedLanguage == "Saudi" and "اضغط للتليبرت" or "Click to teleport"
        
        TpTool.Activated:Connect(function()
            local character = player.Character
            local hrp = character and (character:FindFirstChild("HumanoidRootPart") or 
                                       character:FindFirstChild("Torso") or 
                                       character:FindFirstChild("UpperTorso"))
            
            if not character or not hrp then return end
            
            local mouse = player:GetMouse()
            local targetPos = mouse.Hit
            
            if targetPos then
                hrp.CFrame = CFrame.new(
                    targetPos.X,
                    targetPos.Y + 3,
                    targetPos.Z,
                    select(4, hrp.CFrame:components())
                )
            end
        end)
    end
    
    local function setupBoostFPS()
        if _G.BoostFPSEnabled then
            settings().Rendering.QualityLevel = 1
            for _, v in pairs(game:GetDescendants()) do
                if v:IsA("Part") or v:IsA("UnionOperation") or v:IsA("MeshPart") then
                    v.Material = Enum.Material.Plastic
                    v.Reflectance = 0
                elseif v:IsA("Decal") then
                    v.Transparency = 1
                elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                    v.Enabled = false
                end
            end
        else
            settings().Rendering.QualityLevel = 21
            for _, v in pairs(game:GetDescendants()) do
                if v:IsA("Decal") then
                    v.Transparency = 0
                elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                    v.Enabled = true
                end
            end
        end
    end
    
    local function setupFullBright()
        if _G.FullBrightEnabled then
            game.Lighting.GlobalShadows = false
            game.Lighting.FogEnd = 9e9
            game.Lighting.Brightness = 2
        else
            game.Lighting.GlobalShadows = true
            game.Lighting.FogEnd = 1000
            game.Lighting.Brightness = 1
        end
    end
    
    local function setupAntiAFK()
        local RunService = game:GetService("RunService")
        
        if _G.AntiAFKEnabled then
            if espConnections.antiAFK then
                espConnections.antiAFK:Disconnect()
            end
            
            espConnections.antiAFK = RunService.Heartbeat:Connect(function()
                game:GetService("VirtualInputManager"):SendMouseMoveEvent(100, 100, workspace)
            end)
        elseif espConnections.antiAFK then
            espConnections.antiAFK:Disconnect()
            espConnections.antiAFK = nil
        end
    end
    
    -- =============================================
    -- God Mode من Rise
    -- =============================================
    
    local function applyHumanoidReplacement()
        local player = game.Players.LocalPlayer
        local Char = player.Character
        local Human = Char and Char:FindFirstChildWhichIsA("Humanoid")
        if not Human then return end

        local nHuman = Human:Clone()
        nHuman.Parent = Char
        player.Character = nil
        nHuman:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
        nHuman:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
        nHuman:SetStateEnabled(Enum.HumanoidStateType.Physics, false)
        nHuman.BreakJointsOnDeath = true
        nHuman.MaxHealth = math.huge
        nHuman.Health = math.huge
        Human:Destroy()
        player.Character = Char
        nHuman.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None

        local Script = Char:FindFirstChild("Animate")
        if Script then
            Script.Disabled = true
            task.wait()
            Script.Disabled = false
        end
    end

    local function applyHealthMathHuge()
        local player = game.Players.LocalPlayer
        local Char = player.Character
        local Human = Char and Char:FindFirstChildWhichIsA("Humanoid")
        if not Human then return end

        Human.MaxHealth = math.huge
        Human.Health = math.huge

        local connection
        connection = Human:GetPropertyChangedSignal("Health"):Connect(function()
            if _G.GodModeEnabled and Human.Health < Human.MaxHealth then
                Human.Health = Human.MaxHealth
            else
                if not _G.GodModeEnabled and connection then connection:Disconnect() end
            end
        end)
    end

    local function applyGodMode()
        if _G.GodModeMethod == "Humanoid Replacement (Very buggy)" then
            applyHumanoidReplacement()
        elseif _G.GodModeMethod == "Health Math.huge" then
            applyHealthMathHuge()
        end
    end

    local function startGodMode()
        local RunService = game:GetService("RunService")
        if godModeConnection then return end

        godModeConnection = RunService.Heartbeat:Connect(function()
            if _G.GodModeEnabled and game.Players.LocalPlayer.Character then
                local Human = game.Players.LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid")
                if Human and Human.Health < math.huge then
                    applyGodMode()
                end
            end
        end)
    end

    local function stopGodMode()
        if godModeConnection then
            godModeConnection:Disconnect()
            godModeConnection = nil
        end
    end
    
    -- =============================================
    -- Anti Void من Rise
    -- =============================================
    
    local function enableAntiVoid()
        if _G.AntiVoidEnabled then
            originalDestroyHeight = workspace.FallenPartsDestroyHeight
            workspace.FallenPartsDestroyHeight = -math.huge
            SendNotification(T("antiVoid"), _G.SelectedLanguage == "Saudi" and "تم التفعيل" or "Enabled")
        else
            if originalDestroyHeight then
                workspace.FallenPartsDestroyHeight = originalDestroyHeight
            end
            SendNotification(T("antiVoid"), _G.SelectedLanguage == "Saudi" and "تم الإيقاف" or "Disabled")
        end
    end
    
    -- =============================================
    -- Auto Shoot من Rise
    -- =============================================
    
    local function ShootMurderer()
        local player = game.Players.LocalPlayer
        if not player.Character then return false end

        local murderer = GetMurderer()
        if not murderer or not murderer.Character then 
            SendNotification(T("notification"), _G.SelectedLanguage == "Saudi" and "القاتل غير موجود!" or "Murderer not found!")
            return false 
        end

        local gun = player.Character:FindFirstChild("Gun") or player.Backpack:FindFirstChild("Gun")
        if not gun then 
            SendNotification(T("notification"), _G.SelectedLanguage == "Saudi" and "السلاح غير موجود!" or "Gun not found!")
            return false 
        end

        if gun.Parent ~= player.Character then
            gun.Parent = player.Character
            task.wait(0.1)
        end

        gun = player.Character:FindFirstChild("Gun")
        if gun and gun:FindFirstChild("KnifeLocal") then
            local targetPart = murderer.Character:FindFirstChild("HumanoidRootPart")
            if targetPart then
                local targetPos = targetPart.Position
                local mHum = murderer.Character:FindFirstChild("Humanoid")

                if _G.ShootPrediction ~= 0 and mHum then
                    local moveDir = mHum.MoveDirection
                    if moveDir.Magnitude > 0 then
                        local finalOffset = _G.ShootPrediction + (_G.PingMultiplier * 0.5)
                        targetPos = targetPos + (moveDir.Unit * finalOffset)
                    end
                end

                pcall(function()
                    gun.KnifeLocal.CreateBeam.RemoteFunction:InvokeServer(1, targetPos, "AH2")
                end)
                return true
            end
        end
        return false
    end

    local function startAutoShoot()
        while _G.AutoShootEnabled do
            ShootMurderer()
            task.wait(0.25)
        end
    end

    local function makeDraggable(frame)
        local UserInputService = game:GetService("UserInputService")
        local dragging, dragInput, dragStart, startPos

        local function update(input)
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end

        frame.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
                dragStart = input.Position
                startPos = frame.Position
                input.Changed:Connect(function()
                    if input.UserInputState == Enum.UserInputState.End then dragging = false end
                end)
            end
        end)

        frame.InputChanged:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
                dragInput = input
            end
        end)

        UserInputService.InputChanged:Connect(function(input)
            if input == dragInput and dragging then update(input) end
        end)
    end

    local function createSheriffGui()
        if sheriffGui then sheriffGui:Destroy() end

        sheriffGui = Instance.new("ScreenGui")
        sheriffGui.Name = "SheriffGui"
        sheriffGui.Parent = game:GetService("CoreGui")
        sheriffGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
        sheriffGui.ResetOnSpawn = false

        local Frame = Instance.new("Frame")
        Frame.Parent = sheriffGui
        Frame.BackgroundColor3 = Color3.fromRGB(26, 26, 26)
        Frame.BackgroundTransparency = 0.450
        Frame.Position = UDim2.new(0.5, -80, 0.2, 0)
        Frame.Size = UDim2.new(0, 161, 0, 78)
        Frame.Active = true

        local UICorner = Instance.new("UICorner")
        UICorner.Parent = Frame

        local TextLabel = Instance.new("TextLabel")
        TextLabel.Parent = Frame
        TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        TextLabel.BackgroundTransparency = 1.000
        TextLabel.Position = UDim2.new(0, 0, 0.1025, 0)
        TextLabel.Size = UDim2.new(0, 161, 0, 29)
        TextLabel.Font = Enum.Font.Michroma
        TextLabel.Text = _G.SelectedLanguage == "Saudi" and "إطلاق" or "FIRE"
        TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        TextLabel.TextScaled = true
        TextLabel.TextSize = 14.000
        TextLabel.TextWrapped = true

        local TriggerButton = Instance.new("TextButton")
        TriggerButton.Name = "TriggerButton"
        TriggerButton.Parent = Frame
        TriggerButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        TriggerButton.Position = UDim2.new(0.0496, 0, 0.5512, 0)
        TriggerButton.Size = UDim2.new(0, 144, 0, 27)
        TriggerButton.Font = Enum.Font.Michroma
        TriggerButton.Text = "🔫 " .. (_G.SelectedLanguage == "Saudi" and "إطلاق" or "SHOOT")
        TriggerButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        TriggerButton.TextScaled = true
        TriggerButton.TextSize = 14.000
        TriggerButton.TextWrapped = true

        local UICorner_2 = Instance.new("UICorner")
        UICorner_2.Parent = TriggerButton

        makeDraggable(Frame)

        TriggerButton.MouseButton1Click:Connect(function()
            ShootMurderer()
        end)
    end

    local function removeSheriffGui()
        if sheriffGui then
            sheriffGui:Destroy()
            sheriffGui = nil
        end
    end
    
    -- =============================================
    -- Gun Grabber من Rise
    -- =============================================
    
    local function ScanForGunDrops()
        GunSystem.ActiveGunDrops = {}
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj.Name == "GunDrop" and obj:IsA("BasePart") then
                table.insert(GunSystem.ActiveGunDrops, obj)
            end
        end
    end

    local function EquipGun()
        local player = game.Players.LocalPlayer
        local char = player.Character
        if not char then return false end
        if char:FindFirstChild("Gun") then return true end

        local backpackGun = player.Backpack:FindFirstChild("Gun")
        if backpackGun then
            backpackGun.Parent = char
            task.wait(0.1)
            return true
        end
        return false
    end

    local function ManualGrab()
        local player = game.Players.LocalPlayer
        if player.Backpack:FindFirstChild("Knife") or (player.Character and player.Character:FindFirstChild("Knife")) then
            SendNotification(T("notification"), _G.SelectedLanguage == "Saudi" and "لا يمكنك أخذ السلاح (أنت القاتل)!" or "You cannot pick up the gun (You are Murderer)!")
            return
        end

        ScanForGunDrops()
        local char = player.Character
        if not char then return end
        local root = char:FindFirstChild("HumanoidRootPart")

        if #GunSystem.ActiveGunDrops > 0 and root then
            local prevPos = root.CFrame
            local targetGun = GunSystem.ActiveGunDrops[1]

            root.CFrame = targetGun.CFrame
            task.wait(0.2)
            local prompt = targetGun:FindFirstChildOfClass("ProximityPrompt")
            if prompt then 
                fireproximityprompt(prompt)
                SendNotification(T("notification"), _G.SelectedLanguage == "Saudi" and "محاولة أخذ السلاح..." or "Attempting to grab gun...")
            end
            task.wait(0.2)
            root.CFrame = prevPos

            if GunSystem.Mode == "Grab & shoot murderer" then
                task.wait(0.5)
                if EquipGun() then 
                    ShootMurderer() 
                end
            end
        else
            SendNotification(T("notification"), _G.SelectedLanguage == "Saudi" and "لا توجد أسلحة!" or "No gun drops found!")
        end
    end

    local function AutoGrabLoop()
        local player = game.Players.LocalPlayer
        while GunSystem.AutoGrabEnabled do
            local hasKnife = player.Backpack:FindFirstChild("Knife") or (player.Character and player.Character:FindFirstChild("Knife"))
            if not hasKnife then
                ManualGrab()
            end
            task.wait(2)
        end
    end

    local function MonitorGunEvents()
        workspace.DescendantAdded:Connect(function(child)
            if GunSystem.NotifyGun and child.Name == "GunDrop" and child:IsA("BasePart") then
                if not notifiedGunSpawns[child] then
                    SendNotification(_G.SelectedLanguage == "Saudi" and "سلاح ظهر!" or "Gun Spawned!", _G.SelectedLanguage == "Saudi" and "ظهر سلاح في الخريطة" or "A gun has dropped on the map.")
                    notifiedGunSpawns[child] = true
                end
            end
        end)

        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer
        for _, plr in ipairs(Players:GetPlayers()) do
            plr.CharacterAdded:Connect(function(char)
                char.ChildAdded:Connect(function(child)
                    if GunSystem.NotifyGun and child.Name == "Gun" and plr ~= LocalPlayer then
                        SendNotification(_G.SelectedLanguage == "Saudi" and "سلاح تم أخذه!" or "Gun Taken!", (plr.Name .. " " .. (_G.SelectedLanguage == "Saudi" and "أخذ السلاح" or "has picked up the gun.")))
                    end
                end)
            end)
        end
    end
    task.spawn(MonitorGunEvents)
    
    -- =============================================
    -- Fling Functions من Rise
    -- =============================================
    
    local function sortPlayersAlphabetically(players)
        table.sort(players, function(a, b)
            return string.lower(a.Name) < string.lower(b.Name)
        end)
        return players
    end

    local function getPlayers(input)
        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer
        local players = {}
        input = string.lower(input or "")

        if input == "all" then
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer then
                    table.insert(players, player)
                end
            end
            players = sortPlayersAlphabetically(players)
        elseif input == "nonfriends" then
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer then
                    local success, isFriend = pcall(function() return player:IsFriendsWith(LocalPlayer.UserId) end)
                    if not (success and isFriend) then
                        table.insert(players, player)
                    end
                end
            end
            players = sortPlayersAlphabetically(players)
        elseif input == "murder" then
            local murderer = findMurderer()
            if murderer and murderer ~= LocalPlayer and murderer.Character and IsAlive(murderer) then
                table.insert(players, murderer)
            end
        elseif input == "sheriff" then
            local sheriff = findSheriff()
            if sheriff and sheriff ~= LocalPlayer and sheriff.Character and IsAlive(sheriff) then
                table.insert(players, sheriff)
            end
        else
            local searchTerms = {}
            for term in string.gmatch(input, "([^,]+)") do
                term = string.match(term, "^%s*(.-)%s*$")
                if term ~= "" then table.insert(searchTerms, term) end
            end

            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer then
                    local playerName = string.lower(player.Name)
                    local displayName = player.DisplayName and string.lower(player.DisplayName) or ""

                    for _, term in ipairs(searchTerms) do
                        if string.find(playerName, term) or string.find(displayName, term) then
                            table.insert(players, player)
                            break
                        end
                    end
                end
            end
        end
        return players
    end

    local function Flinger(TargetPlayer, duration)
        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer
        local startTime = tick()
        local Character = LocalPlayer.Character
        local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
        local RootPart = Humanoid and Humanoid.RootPart
        local TCharacter = TargetPlayer.Character
        local THumanoid, TRootPart, THead, Accessory, Handle

        if TCharacter:FindFirstChildOfClass("Humanoid") then THumanoid = TCharacter:FindFirstChildOfClass("Humanoid") end
        if THumanoid and THumanoid.RootPart then TRootPart = THumanoid.RootPart end
        if TCharacter:FindFirstChild("Head") then THead = TCharacter.Head end
        if TCharacter:FindFirstChildOfClass("Accessory") then Accessory = TCharacter:FindFirstChildOfClass("Accessory") end
        if Accessory and Accessory:FindFirstChild("Handle") then Handle = Accessory.Handle end

        if Character and Humanoid and RootPart then
            if RootPart.Velocity.Magnitude < 50 then getgenv().OldPos = RootPart.CFrame end
            if THead then workspace.CurrentCamera.CameraSubject = THead
            elseif not THead and Handle then workspace.CurrentCamera.CameraSubject = Handle
            elseif THumanoid and TRootPart then workspace.CurrentCamera.CameraSubject = THumanoid end
            if not TCharacter:FindFirstChildWhichIsA("BasePart") then return end

            local FPos = function(BasePart, Pos, Ang)
                RootPart.CFrame = CFrame.new(BasePart.Position) * Pos * Ang
                Character:SetPrimaryPartCFrame(CFrame.new(BasePart.Position) * Pos * Ang)
                RootPart.Velocity = Vector3.new(9e7, 9e7 * 10, 9e7)
                RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
            end

            local SFBasePart = function(BasePart)
                local TimeToWait = duration or 2
                local Time = tick()
                local Angle = 0
                repeat
                    if RootPart and THumanoid then
                        if BasePart.Velocity.Magnitude < 50 then
                            Angle = Angle + 100
                            FPos(BasePart, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle),0 ,0))
                            task.wait()
                            FPos(BasePart, CFrame.new(0, -1.5, 0) + THumanoid.MoveDirection * BasePart.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(Angle), 0, 0))
                            task.wait()
                        else
                            FPos(BasePart, CFrame.new(0, 1.5, THumanoid.WalkSpeed), CFrame.Angles(math.rad(90), 0, 0))
                            task.wait()
                            FPos(BasePart, CFrame.new(0, -1.5, -THumanoid.WalkSpeed), CFrame.Angles(0, 0, 0))
                            task.wait()
                        end
                    else break end
                until not flingActive or BasePart.Velocity.Magnitude > 500 or BasePart.Parent ~= TargetPlayer.Character or TargetPlayer.Parent ~= game.Players or not TargetPlayer.Character == TCharacter or THumanoid.Sit or tick() > Time + TimeToWait
            end

            local previousDestroyHeight = workspace.FallenPartsDestroyHeight
            workspace.FallenPartsDestroyHeight = 0/0
            local BV = Instance.new("BodyVelocity", RootPart)
            BV.Name = "EpixVel"
            BV.Velocity = Vector3.new(9e8, 9e8, 9e8)
            BV.MaxForce = Vector3.new(1/0, 1/0, 1/0)
            Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)

            if TRootPart and THead then
                if (TRootPart.CFrame.p - THead.CFrame.p).Magnitude > 5 then SFBasePart(THead) else SFBasePart(TRootPart) end
            elseif TRootPart and not THead then SFBasePart(TRootPart)
            elseif not TRootPart and THead then SFBasePart(THead)
            elseif not TRootPart and not THead and Accessory and Handle then SFBasePart(Handle) end

            BV:Destroy()
            Humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
            workspace.CurrentCamera.CameraSubject = Humanoid

            repeat
                if Character and Humanoid and RootPart and getgenv().OldPos then
                    RootPart.CFrame = getgenv().OldPos * CFrame.new(0, .5, 0)
                    Character:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0, .5, 0))
                    Humanoid:ChangeState("GettingUp")
                    for _, x in pairs(Character:GetChildren()) do
                        if x:IsA("BasePart") then x.Velocity, x.RotVelocity = Vector3.new(), Vector3.new() end
                    end
                end
                task.wait()
            until not flingActive or (RootPart and getgenv().OldPos and (RootPart.Position - getgenv().OldPos.p).Magnitude < 25)
            workspace.FallenPartsDestroyHeight = previousDestroyHeight
        end
    end

    local function shhhlol(TargetPlayer)
        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer
        local Character = LocalPlayer.Character
        local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
        local RootPart = Humanoid and Humanoid.RootPart
        local TCharacter = TargetPlayer.Character
        local THumanoid = TCharacter and TCharacter:FindFirstChildOfClass("Humanoid")
        local TRootPart = THumanoid and THumanoid.RootPart
        local THead = TCharacter and TCharacter:FindFirstChild("Head")

        if Character and Humanoid and RootPart then
            if RootPart.Velocity.Magnitude < 50 then getgenv().OldPos = RootPart.CFrame end
            if not TCharacter:FindFirstChildWhichIsA("BasePart") then return end

            local function mmmm(comkid, Pos, Ang)
                RootPart.CFrame = CFrame.new(comkid.Position) * Pos * Ang
                RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
            end

            local function wtf(comkid)
                local TimeToWait = 0.134
                local Time = tick()
                local Att1 = Instance.new("Attachment", RootPart)
                local Att2 = Instance.new("Attachment", comkid)

                repeat
                    if RootPart and THumanoid then
                        if comkid.Velocity.Magnitude < 30 then
                            mmmm(comkid, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection * comkid.Velocity.Magnitude / 5, CFrame.Angles(math.rad(180), math.rad(180), math.rad(180)))
                            task.wait()
                            mmmm(comkid, CFrame.new(0, 1.5, 0) + THumanoid.MoveDirection * comkid.Velocity.Magnitude / 1.25, CFrame.Angles(math.rad(0), math.rad(0), math.rad(0)))
                            task.wait()
                        else
                            mmmm(comkid, CFrame.new(0, -1.5, 0), CFrame.Angles(math.rad(0), 0, 0))
                            task.wait()
                        end
                    else break end
                until comkid.Velocity.Magnitude > 1000 or comkid.Parent ~= TargetPlayer.Character or TargetPlayer.Parent ~= game.Players or not TargetPlayer.Character == TCharacter or Humanoid.Health <= 0 or tick() > Time + TimeToWait or not flingActive

                Att1:Destroy()
                Att2:Destroy()
            end

            local previousDestroyHeight = workspace.FallenPartsDestroyHeight
            workspace.FallenPartsDestroyHeight = 0/0

            local BV = Instance.new("BodyVelocity", RootPart)
            BV.Velocity = Vector3.new(-9e99, 9e99, -9e99)
            BV.MaxForce = Vector3.new(-9e9, 9e9, -9e9)

            if TRootPart and THead then
                if (TRootPart.CFrame.p - THead.CFrame.p).Magnitude > 5 then wtf(THead) else wtf(TRootPart) end
            elseif TRootPart and not THead then wtf(TRootPart)
            elseif not TRootPart and THead then wtf(THead) end

            BV:Destroy()
            repeat
                if Character and Humanoid and RootPart and getgenv().OldPos then
                    RootPart.CFrame = getgenv().OldPos * CFrame.new(0, .5, 0)
                    Character:SetPrimaryPartCFrame(getgenv().OldPos * CFrame.new(0, .5, 0))
                    Humanoid:ChangeState("GettingUp")
                    for _, x in pairs(Character:GetDescendants()) do
                        if x:IsA("BasePart") then x.Velocity, x.RotVelocity = Vector3.new(), Vector3.new() end
                    end
                end
                task.wait()
            until not flingActive or (RootPart and getgenv().OldPos and (RootPart.Position - getgenv().OldPos.p).Magnitude < 25)
            workspace.FallenPartsDestroyHeight = previousDestroyHeight
        end
    end

    local function yeet(targetPlayer)
        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer
        local character = LocalPlayer.Character
        local targetCharacter = targetPlayer.Character
        if not character or not targetCharacter or not targetCharacter:FindFirstChild("HumanoidRootPart") then return false end
        if character.HumanoidRootPart.Velocity.Magnitude < 50 then getgenv().OldPos = character.HumanoidRootPart.CFrame end
        local existingForce = character.HumanoidRootPart:FindFirstChild("YeetForce")
        if existingForce then existingForce:Destroy() end

        local Thrust = Instance.new('BodyThrust', character.HumanoidRootPart)
        Thrust.Force = Vector3.new(9999, 9999, 9999)
        Thrust.Name = "YeetForce"

        local previousDestroyHeight = workspace.FallenPartsDestroyHeight
        workspace.FallenPartsDestroyHeight = 0/0
        local startTime = tick()
        local duration = (currentInput == "all" or currentInput == "nonfriends") and 5 or math.huge

        local yeetConnection
        yeetConnection = game:GetService("RunService").Heartbeat:Connect(function()
            if not targetCharacter or not targetCharacter:FindFirstChild("HumanoidRootPart") or not flingActive or tick() > startTime + duration then
                yeetConnection:Disconnect()
                Thrust:Destroy()
                workspace.FallenPartsDestroyHeight = previousDestroyHeight
                if character and character.HumanoidRootPart and getgenv().OldPos then
                    character.HumanoidRootPart.CFrame = getgenv().OldPos * CFrame.new(0, .5, 0)
                    character.Humanoid:ChangeState("GettingUp")
                    for _, x in pairs(character:GetDescendants()) do
                        if x:IsA("BasePart") then x.Velocity, x.RotVelocity = Vector3.new(), Vector3.new() end
                    end
                end
                return
            end
            local targetHRP = targetCharacter.HumanoidRootPart
            local offsetPosition = targetHRP.Position + (targetHRP.Velocity.Magnitude > 0.1 and (targetHRP.Velocity.Unit * targetHRP.Velocity.Magnitude) or Vector3.zero)
            character.HumanoidRootPart.CFrame = CFrame.new(offsetPosition)
            Thrust.Location = targetHRP.Position
        end)
        return true
    end

    local function addPlayerToProcessed(player)
        local Players = game:GetService("Players")
        local LocalPlayer = Players.LocalPlayer
        if not player or player == LocalPlayer then return end

        local matchesFilter = false
        local input = string.lower(currentInput)

        if input == "all" then matchesFilter = true
        elseif input == "nonfriends" then
            local success, isFriend = pcall(function() return player:IsFriendsWith(LocalPlayer.UserId) end)
            matchesFilter = not (success and isFriend)
        elseif input == "murder" then
            local murderer = findMurderer()
            if murderer and player == murderer then matchesFilter = IsAlive(player) end
        elseif input == "sheriff" then
            local sheriff = findSheriff()
            if sheriff and player == sheriff then matchesFilter = IsAlive(player) end
        else
            local searchTerms = {}
            for term in string.gmatch(input, "([^,]+)") do
                term = string.match(term, "^%s*(.-)%s*$")
                if term ~= "" then table.insert(searchTerms, term) end
            end
            local playerName = string.lower(player.Name)
            local displayName = player.DisplayName and string.lower(player.DisplayName) or ""
            for _, term in ipairs(searchTerms) do
                if string.find(playerName, term) or string.find(displayName, term) then
                    matchesFilter = true; break
                end
            end
        end

        if matchesFilter then processedPlayers[player] = true end
    end

    local function flingPlayers()
        local players = {}
        for player, _ in pairs(processedPlayers) do
            if player and player.Character and player.Character.Parent ~= nil then
                table.insert(players, player)
            end
        end

        if currentInput == "all" or currentInput == "nonfriends" then
            players = sortPlayersAlphabetically(players)
        end

        for _, player in ipairs(players) do
            if not flingActive then break end
            if player and player.Character and player.Character.Parent ~= nil then
                local duration = (currentInput == "all" or currentInput == "nonfriends") and 1.5 or nil
                if flingMode == 1 then Flinger(player, duration)
                elseif flingMode == 2 then shhhlol(player)
                elseif flingMode == 3 then
                    yeet(player)
                    if currentInput == "all" or currentInput == "nonfriends" then task.wait(1.5) end
                end
            end
        end

        if flingActive then
            task.wait()
            flingPlayers()
        end
    end
    
    -- =============================================
    -- Auto Farm من Rise
    -- =============================================
    
    local function getHumanoidRootPart()
        local player = game.Players.LocalPlayer
        if not player.Character then return nil end
        return player.Character:FindFirstChild("HumanoidRootPart")
    end

    local function findNearestCoin()
        local hrp = getHumanoidRootPart()
        if not hrp then return nil end

        local closestCoin
        local shortestDistance = math.huge

        for _, containerName in ipairs(AutoFarm.CoinContainers) do
            local container = workspace:FindFirstChild(containerName)
            if container then
                local coinContainer = (containerName == "Lobby") and container or container:FindFirstChild("CoinContainer")
                if coinContainer then
                    for _, coin in ipairs(coinContainer:GetChildren()) do
                        if coin:IsA("BasePart") and coin.Parent then
                            local distance = (hrp.Position - coin.Position).Magnitude
                            if distance < shortestDistance then
                                shortestDistance = distance
                                closestCoin = coin
                            end
                        end
                    end
                end
            end
        end

        return closestCoin
    end

    local function teleportToCoin(coin)
        local hrp = getHumanoidRootPart()
        if hrp and coin then
            hrp.CFrame = CFrame.new(coin.Position + Vector3.new(0, 3, 0))
            task.wait(AutoFarm.TeleportDelay)
        end
    end

    local function smoothMoveToCoin(coin)
        local hrp = getHumanoidRootPart()
        if not hrp or not coin then return end

        local startPos = hrp.Position
        local endPos = coin.Position + Vector3.new(0, 3, 0)
        local distance = (endPos - startPos).Magnitude
        local duration = distance / AutoFarm.MoveSpeed
        local startTime = tick()

        while tick() - startTime < duration and AutoFarm.Enabled do
            if not coin.Parent then break end
            local progress = math.min((tick() - startTime) / duration, 1)
            hrp.CFrame = CFrame.new(startPos:Lerp(endPos, progress))
            task.wait()
        end
    end

    local function walkToCoin(coin)
        local player = game.Players.LocalPlayer
        if not player.Character or not coin then return end
        local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
        if not humanoid then return end

        humanoid.WalkSpeed = 16
        humanoid:MoveTo(coin.Position + Vector3.new(0, 0, 3))
        local startTime = tick()

        while AutoFarm.Enabled and humanoid.MoveDirection.Magnitude > 0 and tick() - startTime < 10 do
            task.wait(0.5)
        end
    end

    local function collectCoin(coin)
        local hrp = getHumanoidRootPart()
        if hrp and coin and coin.Parent then
            firetouchinterest(hrp, coin, 0)
            firetouchinterest(hrp, coin, 1)
            task.wait(1)
        end
    end

    local function farmLoop()
        while AutoFarm.Enabled do
            local coin = findNearestCoin()
            if coin then
                if AutoFarm.Mode == "Teleport" then
                    teleportToCoin(coin)
                elseif AutoFarm.Mode == "Smooth" then
                    smoothMoveToCoin(coin)
                else
                    walkToCoin(coin)
                end
                collectCoin(coin)
            else
                task.wait(1)
            end
            task.wait(AutoFarm.CoinCheckInterval)
        end
    end
    
    -- =============================================
    -- Unlock All Emotes من Rise
    -- =============================================
    
    local function unlockAllEmotes()
        local Players = game:GetService("Players")
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local UserInputService = game:GetService("UserInputService")
        local LocalPlayer = Players.LocalPlayer
        local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

        local PlayEmote = ReplicatedStorage:FindFirstChild("Remotes") 
            and ReplicatedStorage.Remotes:FindFirstChild("Misc") 
            and ReplicatedStorage.Remotes.Misc:FindFirstChild("PlayEmote")

        if not PlayEmote then
            warn("PlayEmote remote not found!")
            return
        end

        local ItemServiceModule = ReplicatedStorage:FindFirstChild("ClientServices") 
            and ReplicatedStorage.ClientServices:FindFirstChild("ItemService")

        if not ItemServiceModule then return end
        local ItemService = require(ItemServiceModule)

        local function getAllEmotes()
            local emotes = {}
            local added = {}

            local success, syncModule = pcall(require, ReplicatedStorage.Database.Sync)
            if success and syncModule then
                if syncModule.Emotes then
                    for emoteName, _ in pairs(syncModule.Emotes) do
                        if not added[emoteName] then
                            table.insert(emotes, emoteName)
                            added[emoteName] = true
                        end
                    end
                end
                if syncModule.Toys then
                    for toyName, _ in pairs(syncModule.Toys) do
                        if not added[toyName] then
                            table.insert(emotes, toyName)
                            added[toyName] = true
                        end
                    end
                end
            end

            local defaultEmotes = {"wave", "cheer", "laugh", "dance1", "dance2", "dance3"}
            for _, emote in ipairs(defaultEmotes) do
                if not added[emote] then
                    table.insert(emotes, emote)
                    added[emote] = true
                end
            end
            return emotes
        end

        local AllEmotes = getAllEmotes()

        local function UnlockEmotesAdvanced()
            local Cross = PlayerGui:FindFirstChild("CrossPlatform")
            if not Cross then return end

            local Emotes = Cross:FindFirstChild("Emotes")
            if not Emotes then return end

            local Controller = Emotes:FindFirstChild("EmoteController")
            local Window = Emotes:FindFirstChild("EmoteWindow")
            if not Controller or not Window then return end

            local GameEmotes = Window.EmoteContainer.EmotePages:FindFirstChild("Game Emotes")
            if not GameEmotes then return end

            local FrameTemplate = Controller:FindFirstChild("EmoteFrame")
            if not FrameTemplate then return end

            for _, v in pairs(GameEmotes:GetChildren()) do
                if v.Name:match("^Row") or v.Name:match("^Emote") then
                    v:Destroy()
                end
            end

            if not GameEmotes:IsA("ScrollingFrame") then
                local scrollingFrame = Instance.new("ScrollingFrame")
                scrollingFrame.Name = "EmoteScroller"
                scrollingFrame.Size = UDim2.new(1, 0, 1, 0)
                scrollingFrame.Position = UDim2.new(0, 0, 0, 0)
                scrollingFrame.BackgroundTransparency = 1
                scrollingFrame.BorderSizePixel = 0
                scrollingFrame.ScrollBarThickness = 8
                scrollingFrame.Parent = GameEmotes

                local uiGridLayout = Instance.new("UIGridLayout")
                uiGridLayout.CellSize = UDim2.new(0, 80, 0, 80)
                uiGridLayout.CellPadding = UDim2.new(0, 5, 0, 5)
                uiGridLayout.FillDirection = Enum.FillDirection.Horizontal
                uiGridLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
                uiGridLayout.SortOrder = Enum.SortOrder.LayoutOrder
                uiGridLayout.Parent = scrollingFrame

                GameEmotes = scrollingFrame
            end

            for index, emote in ipairs(AllEmotes) do
                local info = ItemService:GetItemInfo(emote, "Emotes")
                if not info then
                    info = ItemService:GetItemInfo(emote, "Toys")
                end

                if info then
                    local frame = FrameTemplate:Clone()
                    frame.Name = "Emote"..index
                    frame:SetAttribute("EmoteName", emote)
                    if frame:FindFirstChild("EmoteName") then frame.EmoteName.Text = info.Name or emote end
                    if frame:FindFirstChild("EmoteIcon") then frame.EmoteIcon.Image = ItemService:GetItemImage(info) end

                    if frame:FindFirstChild("Hotkey") and frame.Hotkey:FindFirstChild("KeyLabel") then
                        frame.Hotkey.KeyLabel.Text = index
                        frame.Hotkey.Visible = UserInputService.KeyboardEnabled
                    end

                    frame.LayoutOrder = index
                    frame.Parent = GameEmotes

                    if frame:FindFirstChild("PlayButton") then
                        frame.PlayButton.Activated:Connect(function()
                            PlayEmote:Fire(emote)
                        end)
                    end
                end
            end

            local emotesCount = #AllEmotes
            local rows = math.ceil(emotesCount / 4)
            if GameEmotes:IsA("ScrollingFrame") then
                GameEmotes.CanvasSize = UDim2.new(0, 0, 0, rows * 90)
            end
        end

        local function UnlockEmotesBasic()
            local Cross = PlayerGui:FindFirstChild("CrossPlatform")
            if not Cross then return end
            local Emotes = Cross:FindFirstChild("Emotes")
            if not Emotes then return end
            local Controller = Emotes:FindFirstChild("EmoteController")
            local Window = Emotes:FindFirstChild("EmoteWindow")
            if not Controller or not Window then return end

            local GameEmotes = Window.EmoteContainer.EmotePages:FindFirstChild("Game Emotes")
            local FrameTemplate = Controller:FindFirstChild("EmoteFrame")
            local RowTemplate = Controller:FindFirstChild("RowFrame")

            if not GameEmotes or not FrameTemplate or not RowTemplate then return end

            for _, v in pairs(GameEmotes:GetChildren()) do
                if v.Name:match("^Row") then v:Destroy() end
            end

            local row, slot = 1, 1
            local maxSlotsPerRow = 6

            for _, emote in ipairs(AllEmotes) do
                local info = ItemService:GetItemInfo(emote, "Emotes")
                if not info then info = ItemService:GetItemInfo(emote, "Toys") end

                if info then
                    if slot == 1 then
                        local newRow = RowTemplate:Clone()
                        newRow.Name = "Row"..row
                        newRow.Parent = GameEmotes
                        newRow.LayoutOrder = row
                    end

                    local frame = FrameTemplate:Clone()
                    frame.Name = "Emote"..slot
                    if frame:FindFirstChild("EmoteName") then frame.EmoteName.Text = info.Name or emote end
                    if frame:FindFirstChild("EmoteIcon") then frame.EmoteIcon.Image = ItemService:GetItemImage(info) end

                    if frame:FindFirstChild("Hotkey") then
                        frame.Hotkey.Visible = false
                    end

                    frame.Parent = GameEmotes["Row"..row]
                    if frame:FindFirstChild("PlayButton") then
                        frame.PlayButton.Activated:Connect(function()
                            PlayEmote:Fire(emote)
                        end)
                    end

                    slot = slot + 1
                    if slot > maxSlotsPerRow then
                        slot = 1
                        row = row + 1
                    end
                end
            end
        end

        local function runUnlocker()
            local success = pcall(UnlockEmotesAdvanced)
            if not success then
                UnlockEmotesBasic()
            end
        end

        if PlayerGui:FindFirstChild("CrossPlatform") then
            runUnlocker()
        else
            PlayerGui:WaitForChild("CrossPlatform", 10)
            task.wait(1)
            runUnlocker()
        end

        local inventoryRemote = ReplicatedStorage.Remotes:FindFirstChild("Inventory") 
            and ReplicatedStorage.Remotes.Inventory:FindFirstChild("InventoryDataChanged")

        if inventoryRemote then
            inventoryRemote.Event:Connect(function(cat)
                if cat == "Emotes" or cat == "Toys" then
                    task.wait(0.3)
                    runUnlocker()
                end
            end)
        end
        
        SendNotification(T("unlockEmotes"), _G.SelectedLanguage == "Saudi" and "تم فتح جميع الإيموشنات!" or "All emotes unlocked!")
    end
    
    -- =============================================
    -- Kill Sheriff
    -- =============================================
    local function killSheriff()
        local sheriff = findSheriff()
        local player = game.Players.LocalPlayer
        if sheriff and sheriff.Character and sheriff.Character:FindFirstChild("HumanoidRootPart") then
            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                player.Character.HumanoidRootPart.CFrame = sheriff.Character.HumanoidRootPart.CFrame
                SendNotification(T("notification"), _G.SelectedLanguage == "Saudi" and "تم قتل الشرطي" or "Sheriff killed")
            end
        end
    end
    
    -- =============================================
    -- الدالة الرئيسية
    -- =============================================
    local function createMainScript()
        cleanupGUI()
        
        local success, newWindUI = pcall(function()
            local source = game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua", true)
            return loadstring(source)()
        end)
        
        if not success then return end
        
        WindUI = newWindUI
        WindUI.TransparencyValue = 0.7
        WindUI:SetTheme("Dark")
        
        local Players = game:GetService("Players")
        local RunService = game:GetService("RunService")
        local StatsService = game:GetService("Stats")
        local player = Players.LocalPlayer

        WindUI:Popup({
            Title = "PhantomXPro",
            Icon = "crown",
            Content = T("warning"),
            Buttons = {
                {
                    Title = _G.SelectedLanguage == "Saudi" and "موافق" or "OK",
                    Icon = "arrow-right",
                    Variant = "Primary",
                    Callback = function() end
                }
            }
        })

        task.wait(2)
        WindUI:Popup({
            Title = T("discord"),
            Icon = "message-circle",
            Content = T("discordContent"),
            Buttons = {
                {
                    Title = T("copyLink"),
                    Icon = "copy",
                    Variant = "Primary",
                    Callback = function()
                        setclipboard("https://discord.gg/CgUa36sPNs")
                    end
                }
            }
        })
        
        local Window = WindUI:CreateWindow({
            Title = "PhantomXPro",
            Icon = "crown", 
            Author = "By_Cypher",
            Folder = "PhantomXPro",
            Size = UDim2.fromOffset(950, 700),
            Theme = "Dark",
            SideBarWidth = 200,
            ScrollBarEnabled = true
        })

        -- =============================================
        -- 👤 INFO TAB
        -- =============================================
        local infoTab = Window:Tab({
            Title = T("infoTab"),
            Icon = "user"
        })
        
        infoTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "معلوماتك" or "Your Info",
            Desc = "",
            Image = "user",
            ImageSize = 24
        })
        
        local function getPlayerStats()
            local level = "0"
            local xp = "0"
            
            local playerLevel = player:GetAttribute("Level")
            local playerXP = player:GetAttribute("XP")
            
            if playerLevel ~= nil then
                level = tostring(playerLevel)
            end
            
            if playerXP ~= nil then
                xp = tostring(playerXP)
            end
            
            return level, xp
        end

        local nameBtn = infoTab:Button({
            Title = T("yourName") .. string.sub(player.DisplayName, 1, 12),
            Size = "medium",
            Callback = function() end
        })
        
        local userBtn = infoTab:Button({
            Title = T("yourUsername") .. string.sub(player.Name, 1, 10),
            Size = "medium",
            Callback = function() end
        })
        
        local ageBtn = infoTab:Button({
            Title = T("accountAge") .. player.AccountAge .. (_G.SelectedLanguage == "Saudi" and " يوم" or " days"),
            Size = "medium",
            Callback = function() end
        })
        
        local hackBtn = infoTab:Button({
            Title = T("hack") .. (identifyexecutor and identifyexecutor() or (_G.SelectedLanguage == "Saudi" and "ساينابس" or "Synapse")),
            Size = "medium",
            Callback = function() end
        })

        infoTab:Divider({ Title = "" })

        local levelBtn = infoTab:Button({
            Title = T("level") .. "0",
            Size = "medium",
            Callback = function() end
        })
        
        local xpBtn = infoTab:Button({
            Title = T("xp") .. "0",
            Size = "medium",
            Callback = function() end
        })

        infoTab:Divider({ Title = "" })

        local playersBtn = infoTab:Button({
            Title = T("players") .. "0",
            Size = "medium",
            Callback = function() end
        })
        
        local fpsBtn = infoTab:Button({
            Title = T("fps") .. "0",
            Size = "medium",
            Callback = function() end
        })
        
        local pingBtn = infoTab:Button({
            Title = T("ping") .. "0 ms",
            Size = "medium",
            Callback = function() end
        })

        spawn(function()
            while true do
                task.wait(1.5)
                
                local level, xp = getPlayerStats()
                levelBtn:SetTitle(T("level") .. level)
                xpBtn:SetTitle(T("xp") .. xp)
                
                playersBtn:SetTitle(T("players") .. #Players:GetPlayers())
                
                pcall(function()
                    local ping = StatsService.Network.ServerStatsItem["Data Ping"]:GetValue()
                    pingBtn:SetTitle(T("ping") .. math.floor(ping) .. " ms")
                end)
            end
        end)

        local frameCount = 0
        local lastTime = tick()
        RunService.RenderStepped:Connect(function()
            frameCount = frameCount + 1
            local currentTime = tick()
            if currentTime - lastTime >= 1 then
                local fps = math.floor(frameCount / (currentTime - lastTime))
                frameCount = 0
                lastTime = currentTime
                fpsBtn:SetTitle(T("fps") .. fps)
            end
        end)

        -- =============================================
        -- 🏠 MAIN TAB
        -- =============================================
        local mainTab = Window:Tab({
            Title = T("mainTab"),
            Icon = "zap"
        })
        
        mainTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "المميزات الرئيسية" or "Main Features",
            Desc = "",
            Image = "zap",
            ImageSize = 24
        })
        
        local autofarmToggle = mainTab:Toggle({
            Title = T("autofarm"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                _G.AutofarmActive = state
                AutoFarm.Enabled = state
                if state then
                    if AutoFarm.Connection then task.cancel(AutoFarm.Connection) end
                    AutoFarm.Connection = task.spawn(farmLoop)
                else
                    if AutoFarm.Connection then
                        task.cancel(AutoFarm.Connection)
                        AutoFarm.Connection = nil
                    end
                end
            end
        })
        
        mainTab:Button({
            Title = T("teleportToLobby"),
            Size = "medium",
            Callback = function()
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = LobbyPosition
                end
            end
        })
        
        mainTab:Button({
            Title = T("teleportTool"),
            Size = "medium",
            Callback = function()
                giveTeleportTool()
            end
        })

        -- =============================================
        -- 👁️ ESP TAB (من Rise بالكامل)
        -- =============================================
        local espTab = Window:Tab({
            Title = T("espTab"),
            Icon = "eye"
        })
        
        espTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "ميزات الرؤية المتطورة" or "Advanced ESP Features",
            Desc = _G.SelectedLanguage == "Saudi" and "صناديق ثلاثية الأبعاد وثنائية الأبعاد مع خطوط وأسماء" or "3D/2D Boxes with Tracers, Names and Distances",
            Image = "eye",
            ImageSize = 24
        })
        
        -- Highlights Section
        espTab:Label(T("highlight"))
        espTab:Toggle({
            Title = T("innocentESP") .. " " .. T("highlight"),
            Value = featureStates.InnocentHighlights,
            Callback = function(v) 
                featureStates.InnocentHighlights = v
                manageHighlightsConnection()
                refreshHighlights()
            end
        })
        espTab:Toggle({
            Title = T("murdererESP") .. " " .. T("highlight"),
            Value = featureStates.MurderHighlights,
            Callback = function(v) 
                featureStates.MurderHighlights = v
                manageHighlightsConnection()
                refreshHighlights()
            end
        })
        espTab:Toggle({
            Title = T("sheriffESP") .. " " .. T("highlight"),
            Value = featureStates.SheriffHeroHighlights,
            Callback = function(v) 
                featureStates.SheriffHeroHighlights = v
                manageHighlightsConnection()
                refreshHighlights()
            end
        })
        espTab:Toggle({
            Title = T("gunESP") .. " " .. T("highlight"),
            Value = featureStates.GunHighlights,
            Callback = function(v) 
                featureStates.GunHighlights = v
                manageHighlightsConnection()
                refreshHighlights()
            end
        })
        espTab:Toggle({
            Title = T("coinESP") .. " " .. T("highlight"),
            Value = featureStates.CoinHighlights,
            Callback = function(v) 
                featureStates.CoinHighlights = v
                manageHighlightsConnection()
                refreshHighlights()
            end
        })
        
        espTab:Divider()
        
        -- 3D/2D Box Options
        espTab:Label(T("boxType"))
        espTab:Toggle({
            Title = T("innocentESP") .. " 3D " .. T("boxType"),
            Value = false,
            Callback = function(v) 
                featureStates.InnocentESP.boxType = v and "3D" or "2D"
                featureStates.InnocentESP.boxes = true
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("murdererESP") .. " 3D " .. T("boxType"),
            Value = false,
            Callback = function(v) 
                featureStates.MurderESP.boxType = v and "3D" or "2D"
                featureStates.MurderESP.boxes = true
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("sheriffESP") .. " 3D " .. T("boxType"),
            Value = false,
            Callback = function(v) 
                featureStates.HeroSheriffESP.boxType = v and "3D" or "2D"
                featureStates.HeroSheriffESP.boxes = true
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("gunESP") .. " 3D " .. T("boxType"),
            Value = true,
            Callback = function(v) 
                featureStates.GunESP.boxType = v and "3D" or "2D"
                featureStates.GunESP.boxes = true
                manageGunESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("coinESP") .. " 3D " .. T("boxType"),
            Value = true,
            Callback = function(v) 
                featureStates.CoinESP.boxType = v and "3D" or "2D"
                featureStates.CoinESP.boxes = true
                manageCoinESPConnection()
            end
        })
        
        espTab:Divider()
        
        -- Box ESP
        espTab:Label(T("boxESP"))
        espTab:Toggle({
            Title = T("innocentESP") .. " " .. T("boxESP"),
            Value = false,
            Callback = function(v) 
                featureStates.InnocentESP.boxes = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("murdererESP") .. " " .. T("boxESP"),
            Value = false,
            Callback = function(v) 
                featureStates.MurderESP.boxes = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("sheriffESP") .. " " .. T("boxESP"),
            Value = false,
            Callback = function(v) 
                featureStates.HeroSheriffESP.boxes = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("gunESP") .. " " .. T("boxESP"),
            Value = false,
            Callback = function(v) 
                featureStates.GunESP.boxes = v
                manageGunESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("coinESP") .. " " .. T("boxESP"),
            Value = false,
            Callback = function(v) 
                featureStates.CoinESP.boxes = v
                manageCoinESPConnection()
            end
        })
        
        espTab:Divider()
        
        -- Name ESP
        espTab:Label(T("nameESP"))
        espTab:Toggle({
            Title = T("innocentESP") .. " " .. T("nameESP"),
            Value = false,
            Callback = function(v) 
                featureStates.InnocentESP.names = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("murdererESP") .. " " .. T("nameESP"),
            Value = false,
            Callback = function(v) 
                featureStates.MurderESP.names = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("sheriffESP") .. " " .. T("nameESP"),
            Value = false,
            Callback = function(v) 
                featureStates.HeroSheriffESP.names = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("gunESP") .. " " .. T("nameESP"),
            Value = false,
            Callback = function(v) 
                featureStates.GunESP.names = v
                manageGunESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("coinESP") .. " " .. T("nameESP"),
            Value = false,
            Callback = function(v) 
                featureStates.CoinESP.names = v
                manageCoinESPConnection()
            end
        })
        
        espTab:Divider()
        
        -- Tracer ESP
        espTab:Label(T("tracerESP"))
        espTab:Toggle({
            Title = T("innocentESP") .. " " .. T("tracerESP"),
            Value = false,
            Callback = function(v) 
                featureStates.InnocentESP.tracers = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("murdererESP") .. " " .. T("tracerESP"),
            Value = false,
            Callback = function(v) 
                featureStates.MurderESP.tracers = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("sheriffESP") .. " " .. T("tracerESP"),
            Value = false,
            Callback = function(v) 
                featureStates.HeroSheriffESP.tracers = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("gunESP") .. " " .. T("tracerESP"),
            Value = false,
            Callback = function(v) 
                featureStates.GunESP.tracers = v
                manageGunESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("coinESP") .. " " .. T("tracerESP"),
            Value = false,
            Callback = function(v) 
                featureStates.CoinESP.tracers = v
                manageCoinESPConnection()
            end
        })
        
        espTab:Divider()
        
        -- Distance ESP
        espTab:Label(T("distanceESP"))
        espTab:Toggle({
            Title = T("innocentESP") .. " " .. T("distanceESP"),
            Value = false,
            Callback = function(v) 
                featureStates.InnocentESP.distance = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("murdererESP") .. " " .. T("distanceESP"),
            Value = false,
            Callback = function(v) 
                featureStates.MurderESP.distance = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("sheriffESP") .. " " .. T("distanceESP"),
            Value = false,
            Callback = function(v) 
                featureStates.HeroSheriffESP.distance = v
                managePlayerESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("gunESP") .. " " .. T("distanceESP"),
            Value = false,
            Callback = function(v) 
                featureStates.GunESP.distance = v
                manageGunESPConnection()
            end
        })
        espTab:Toggle({
            Title = T("coinESP") .. " " .. T("distanceESP"),
            Value = false,
            Callback = function(v) 
                featureStates.CoinESP.distance = v
                manageCoinESPConnection()
            end
        })
        
        espTab:Divider()
        
        -- X-Ray
        espTab:Toggle({
            Title = T("xrayVision"),
            Value = false,
            Callback = function(state)
                _G.XrayEnabled = state
                setupXray()
            end
        })

        -- =============================================
        -- 🏃 PLAYER TAB
        -- =============================================
        local playerTab = Window:Tab({
            Title = T("playerTab"),
            Icon = "user"
        })
        
        playerTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "حركة اللاعب" or "Player Movement",
            Desc = "",
            Image = "user",
            ImageSize = 24
        })
        
        -- Movement Settings
        local walkspeedInput = playerTab:Input({
            Title = T("walkspeedAdjustment"),
            Value = tostring(_G.WalkspeedValue),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.WalkspeedValue = num
                    if player.Character and player.Character:FindFirstChild("Humanoid") then
                        player.Character.Humanoid.WalkSpeed = num
                    end
                end
            end
        })
        
        local jumpPowerInput = playerTab:Input({
            Title = T("jumpPowerAdjustment"),
            Value = tostring(_G.JumpPowerValue),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.JumpPowerValue = num
                    if player.Character and player.Character:FindFirstChild("Humanoid") then
                        player.Character.Humanoid.JumpPower = num
                    end
                end
            end
        })
        
        local gravityInput = playerTab:Input({
            Title = T("gravityAdjustment"),
            Value = tostring(_G.GravityValue),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.GravityValue = num
                    workspace.Gravity = num
                end
            end
        })
        
        local fovInput = playerTab:Input({
            Title = T("fovAdjustment"),
            Value = tostring(_G.FOVValue),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.FOVValue = num
                    workspace.CurrentCamera.FieldOfView = num
                end
            end
        })
        
        local flySpeedInput = playerTab:Input({
            Title = T("flySpeedAdjustment"),
            Value = tostring(_G.FlySpeedValue),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.FlySpeedValue = num
                end
            end
        })
        
        playerTab:Toggle({
            Title = T("infiniteJump"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                _G.InfiniteJumpEnabled = state
                setupInfiniteJump()
            end
        })
        
        playerTab:Toggle({
            Title = T("noclipAdjustment"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                _G.NoclipEnabled = state
                setupNoclip()
            end
        })
        
        playerTab:Toggle({
            Title = T("flyMode"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                _G.FlyEnabled = state
                setupFly()
            end
        })
        
        playerTab:Divider()
        
        -- Auto Farm Settings (من Rise)
        playerTab:Paragraph({
            Title = T("autofarm"),
            Desc = _G.SelectedLanguage == "Saudi" and "جمع العملات التلقائي" or "Auto collect coins",
            Image = "dollar-sign",
            ImageSize = 20
        })
        
        playerTab:Toggle({
            Title = T("autofarm"),
            Value = false,
            Callback = function(state)
                _G.AutofarmActive = state
                AutoFarm.Enabled = state
                if state then
                    if AutoFarm.Connection then task.cancel(AutoFarm.Connection) end
                    AutoFarm.Connection = task.spawn(farmLoop)
                    SendNotification(T("autofarm"), _G.SelectedLanguage == "Saudi" and "تم تشغيل الجمع التلقائي" or "Auto farm enabled")
                else
                    if AutoFarm.Connection then
                        task.cancel(AutoFarm.Connection)
                        AutoFarm.Connection = nil
                    end
                    SendNotification(T("autofarm"), _G.SelectedLanguage == "Saudi" and "تم إيقاف الجمع التلقائي" or "Auto farm disabled")
                end
            end
        })
        
        playerTab:Dropdown({
            Title = _G.SelectedLanguage == "Saudi" and "وضع الحركة" or "Movement Mode",
            Description = _G.SelectedLanguage == "Saudi" and "اختر طريقة الانتقال للعملات" or "Select how to travel to coins",
            Values = {"Teleport", "Smooth", "Walk"},
            Value = "Teleport",
            Callback = function(selected)
                if type(selected) == "table" then
                    for k, _ in pairs(selected) do
                        AutoFarm.Mode = k
                        break
                    end
                else
                    AutoFarm.Mode = selected
                end
            end
        })
        
        playerTab:Slider({
            Title = _G.SelectedLanguage == "Saudi" and "تأخير التلبورت" or "Teleport Delay",
            Description = _G.SelectedLanguage == "Saudi" and "بالثواني" or "In seconds",
            Value = 0,
            Min = 0,
            Max = 2,
            Callback = function(value)
                AutoFarm.TeleportDelay = tonumber(value)
            end
        })
        
        playerTab:Slider({
            Title = _G.SelectedLanguage == "Saudi" and "سرعة الحركة" or "Movement Speed",
            Description = _G.SelectedLanguage == "Saudi" and "لوضع Smooth" or "For Smooth mode",
            Value = 50,
            Min = 20,
            Max = 200,
            Callback = function(value)
                AutoFarm.MoveSpeed = tonumber(value)
            end
        })
        
        playerTab:Slider({
            Title = _G.SelectedLanguage == "Saudi" and "تردد الفحص" or "Scan Interval",
            Description = _G.SelectedLanguage == "Saudi" and "بالثواني" or "In seconds",
            Value = 0.5,
            Min = 0.1,
            Max = 2,
            Callback = function(value)
                AutoFarm.CoinCheckInterval = tonumber(value)
            end
        })
        
        playerTab:Divider()
        
        -- Emotes (من Rise)
        playerTab:Paragraph({
            Title = T("unlockEmotes"),
            Desc = T("unlockEmotesDesc"),
            Image = "smile",
            ImageSize = 20
        })
        
        playerTab:Button({
            Title = T("unlockEmotes"),
            Callback = function()
                unlockAllEmotes()
            end
        })
        
        local emoteInputValue = ""
        playerTab:Input({
            Title = T("emoteNameInput"),
            PlaceholderText = "zen, dance1, etc...",
            Callback = function(text)
                emoteInputValue = text
            end
        })
        
        playerTab:Button({
            Title = T("playEmote"),
            Callback = function()
                if emoteInputValue and emoteInputValue ~= "" then
                    local ReplicatedStorage = game:GetService("ReplicatedStorage")
                    local PlayEmote = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("Misc") and ReplicatedStorage.Remotes.Misc:FindFirstChild("PlayEmote")
                    if PlayEmote then
                        PlayEmote:Fire(emoteInputValue)
                        SendNotification(T("playEmote"), _G.SelectedLanguage == "Saudi" and ("تم تشغيل " .. emoteInputValue) or ("Playing " .. emoteInputValue))
                    else
                        SendNotification(T("notification"), _G.SelectedLanguage == "Saudi" and "لم يتم العثور على الإيموشن!" or "Emote not found!")
                    end
                else
                    SendNotification(T("notification"), _G.SelectedLanguage == "Saudi" and "الرجاء إدخال اسم الإيموشن" or "Please enter an emote name")
                end
            end
        })

        -- =============================================
        -- 🛡️ INNOCENT TAB
        -- =============================================
        local innocentTab = Window:Tab({
            Title = T("innocentTab"),
            Icon = "shield"
        })
        
        innocentTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "ميزات الأبرياء" or "Innocent Features",
            Desc = "",
            Image = "shield",
            ImageSize = 24
        })
        
        -- Gun Grabber (من Rise)
        innocentTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "أخذ السلاح" or "Gun Grabber",
            Image = "gun",
            ImageSize = 20
        })
        
        innocentTab:Button({
            Title = T("getDroppedGun"),
            Size = "medium",
            Callback = function()
                ManualGrab()
            end
        })
        
        innocentTab:Toggle({
            Title = T("autoGetDroppedGun"),
            Value = false,
            Callback = function(state)
                GunSystem.AutoGrabEnabled = state
                if state then
                    task.spawn(AutoGrabLoop)
                    SendNotification(T("autoGetDroppedGun"), _G.SelectedLanguage == "Saudi" and "سيتم أخذ السلاح تلقائياً" or "Auto grab enabled")
                else
                    SendNotification(T("autoGetDroppedGun"), _G.SelectedLanguage == "Saudi" and "تم إيقاف الأخذ التلقائي" or "Auto grab disabled")
                end
            end
        })
        
        innocentTab:Dropdown({
            Title = _G.SelectedLanguage == "Saudi" and "وضع الأخذ" or "Grab Mode",
            Description = _G.SelectedLanguage == "Saudi" and "الإجراء بعد أخذ السلاح" or "Action after grabbing",
            Values = {"Grab only", "Grab & shoot murderer"},
            Value = "Grab only",
            Callback = function(value)
                if type(value) == "table" then
                    for k,_ in pairs(value) do GunSystem.Mode = k break end
                else
                    GunSystem.Mode = value
                end
            end
        })
        
        innocentTab:Toggle({
            Title = _G.SelectedLanguage == "Saudi" and "إشعارات السلاح" or "Gun Notifications",
            Value = false,
            Callback = function(state)
                GunSystem.NotifyGun = state
            end
        })
        
        innocentTab:Divider()
        
        -- Fling System (من Rise)
        innocentTab:Paragraph({
            Title = T("flingTitle"),
            Image = "wind",
            ImageSize = 20
        })
        
        local flingInput = innocentTab:Input({
            Title = T("flingTargetInput"),
            Description = _G.SelectedLanguage == "Saudi" and "اكتب: اسم لاعب, all, nonfriends, murder, sheriff" or "Type: name, all, nonfriends, murder, sheriff",
            PlaceholderText = "all, murder, sheriff, or player name...",
            Callback = function(value)
                flingInputValue = value
                currentInput = string.lower(value)
            end
        })
        
        innocentTab:Dropdown({
            Title = T("flingMode"),
            Description = _G.SelectedLanguage == "Saudi" and "اختر طريقة التطيير" or "Select a method",
            Values = {T("flingModeFlinger"), T("flingModeShhhlol"), T("flingModeYeet")},
            Value = T("flingModeFlinger"),
            Callback = function(value)
                local selected = value
                if type(value) == "table" then 
                    for k,_ in pairs(value) do selected = k break end 
                end
                if selected == T("flingModeFlinger") or selected == "Flinger" then flingMode = 1
                elseif selected == T("flingModeShhhlol") or selected == "Shhhlol" then flingMode = 2
                elseif selected == T("flingModeYeet") or selected == "Yeet" then flingMode = 3
                end
            end
        })
        
        local flingToggle = innocentTab:Toggle({
            Title = T("flingActive"),
            Value = false,
            Callback = function(state)
                flingActive = state
                if flingActive then
                    currentInput = string.lower(flingInputValue or "")
                    local playersList = getPlayers(currentInput)
                    if #playersList == 0 then
                        SendNotification(T("flingTitle"), _G.SelectedLanguage == "Saudi" and "لم يتم العثور على لاعبين!" or "No players found!")
                        flingActive = false
                        flingToggle:Set(false)
                        return
                    end
                    processedPlayers = {}
                    for _, plr in ipairs(playersList) do
                        addPlayerToProcessed(plr)
                    end
                    SendNotification(T("flingTitle"), _G.SelectedLanguage == "Saudi" and ("يجري تطيير " .. #playersList .. " لاعب...") or ("Flinging " .. #playersList .. " players..."))
                    task.spawn(flingPlayers)
                else
                    processedPlayers = {}
                end
            end
        })
        
        innocentTab:Button({
            Title = T("flingMurdererBtn"),
            Callback = function()
                if flingActive then
                    SendNotification(T("flingTitle"), _G.SelectedLanguage == "Saudi" and "أوقف التطيير الحالي أولاً!" or "Stop current fling first!")
                    return
                end
                currentInput = "murder"
                local playersList = getPlayers(currentInput)
                if #playersList == 0 then
                    SendNotification(T("flingTitle"), _G.SelectedLanguage == "Saudi" and "لا يوجد قاتل حي" or "No alive murderer found")
                    return
                end
                flingActive = true
                processedPlayers = {}
                for _, plr in ipairs(playersList) do
                    addPlayerToProcessed(plr)
                end
                SendNotification(T("flingTitle"), _G.SelectedLanguage == "Saudi" and "تطيير القاتل لمدة 10 ثواني" or "Flinging Murderer for 10 seconds")
                local stopTimer = 10
                local startTime = tick()
                task.spawn(function()
                    while flingActive and tick() - startTime < stopTimer do task.wait(1) end
                    if flingActive then
                        flingActive = false
                        processedPlayers = {}
                        flingToggle:Set(false)
                        SendNotification(T("flingTitle"), _G.SelectedLanguage == "Saudi" and "انتهى تطيير القاتل" or "Murderer fling finished")
                    end
                end)
                task.spawn(flingPlayers)
            end
        })
        
        innocentTab:Button({
            Title = T("flingSheriffBtn"),
            Callback = function()
                if flingActive then
                    SendNotification(T("flingTitle"), _G.SelectedLanguage == "Saudi" and "أوقف التطيير الحالي أولاً!" or "Stop current fling first!")
                    return
                end
                currentInput = "sheriff"
                local playersList = getPlayers(currentInput)
                if #playersList == 0 then
                    SendNotification(T("flingTitle"), _G.SelectedLanguage == "Saudi" and "لا يوجد شرطي حي" or "No alive sheriff found")
                    return
                end
                flingActive = true
                processedPlayers = {}
                for _, plr in ipairs(playersList) do
                    addPlayerToProcessed(plr)
                end
                SendNotification(T("flingTitle"), _G.SelectedLanguage == "Saudi" and "تطيير الشرطي لمدة 10 ثواني" or "Flinging Sheriff for 10 seconds")
                local stopTimer = 10
                local startTime = tick()
                task.spawn(function()
                    while flingActive and tick() - startTime < stopTimer do task.wait(1) end
                    if flingActive then
                        flingActive = false
                        processedPlayers = {}
                        flingToggle:Set(false)
                        SendNotification(T("flingTitle"), _G.SelectedLanguage == "Saudi" and "انتهى تطيير الشرطي" or "Sheriff fling finished")
                    end
                end)
                task.spawn(flingPlayers)
            end
        })
        
        innocentTab:Divider()
        
        -- God Mode (من Rise)
        innocentTab:Paragraph({
            Title = T("godMode"),
            Image = "heart",
            ImageSize = 20
        })
        
        innocentTab:Toggle({
            Title = T("godMode"),
            Value = false,
            Callback = function(state)
                _G.GodModeEnabled = state
                if state then
                    applyGodMode()
                    startGodMode()
                    SendNotification(T("godMode"), _G.SelectedLanguage == "Saudi" and "تم تفعيل وضع الخلود" or "God mode enabled")
                else
                    stopGodMode()
                    SendNotification(T("godMode"), _G.SelectedLanguage == "Saudi" and "تم إيقاف وضع الخلود" or "God mode disabled")
                end
            end
        })
        
        innocentTab:Dropdown({
            Title = T("godModeMethod"),
            Description = _G.SelectedLanguage == "Saudi" and "اختر طريقة الخلود" or "Select method",
            Values = {"Health Math.huge", "Humanoid Replacement (Very buggy)"},
            Value = "Health Math.huge",
            Callback = function(value)
                if type(value) == "table" then
                    for k,_ in pairs(value) do _G.GodModeMethod = k break end
                else
                    _G.GodModeMethod = value
                end
                if _G.GodModeEnabled then
                    applyGodMode()
                end
            end
        })
        
        innocentTab:Divider()
        
        -- Anti Void (من Rise)
        innocentTab:Paragraph({
            Title = T("antiVoid"),
            Image = "shield",
            ImageSize = 20
        })
        
        innocentTab:Toggle({
            Title = T("antiVoid"),
            Value = false,
            Callback = function(state)
                _G.AntiVoidEnabled = state
                enableAntiVoid()
            end
        })

        -- =============================================
        -- 🔪 MURDERER TAB
        -- =============================================
        local murdererTab = Window:Tab({
            Title = T("murdererTab"),
            Icon = "droplet"
        })
        
        murdererTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "ميزات القتلة" or "Murderer Features",
            Desc = "",
            Image = "droplet",
            ImageSize = 24
        })
        
        murdererTab:Toggle({
            Title = T("aimbot"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                _G.AimbotEnabled = state
                if state then
                    setupAimbot("sheriff")
                    SendNotification(T("aimbot"), _G.SelectedLanguage == "Saudi" and "تم تفعيل التصويب التلقائي" or "Aimbot enabled")
                else
                    if espConnections.aimbot then
                        espConnections.aimbot:Disconnect()
                        espConnections.aimbot = nil
                    end
                    SendNotification(T("aimbot"), _G.SelectedLanguage == "Saudi" and "تم إيقاف التصويب التلقائي" or "Aimbot disabled")
                end
            end
        })
        
        local playerList = {}
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player then
                table.insert(playerList, p.Name)
            end
        end
        
        local selectedPlayer = nil
        murdererTab:Dropdown({
            Title = T("selectPlayer"),
            Values = playerList,
            Value = playerList[1] or "",
            Size = "medium",
            Callback = function(selected)
                selectedPlayer = selected
            end
        })
        
        murdererTab:Button({
            Title = T("refreshPlayers"),
            Size = "medium",
            Callback = function()
                local newList = {}
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= player then
                        table.insert(newList, p.Name)
                    end
                end
            end
        })
        
        murdererTab:Button({
            Title = T("killSelected"),
            Size = "medium",
            Callback = function()
                if selectedPlayer then
                    local target = Players:FindFirstChild(selectedPlayer)
                    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                            player.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame
                            SendNotification(T("killSelected"), _G.SelectedLanguage == "Saudi" and ("تم قتل " .. selectedPlayer) or ("Killed " .. selectedPlayer))
                        end
                    end
                end
            end
        })
        
        local killAllToggle = murdererTab:Toggle({
            Title = T("killAllPlayers"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                _G.KillAllEnabled = state
                if state then
                    setupKillAll()
                    SendNotification(T("killAllPlayers"), _G.SelectedLanguage == "Saudi" and "جاري قتل جميع اللاعبين..." or "Killing all players...")
                else
                    if espConnections.killAll then
                        espConnections.killAll:Disconnect()
                        espConnections.killAll = nil
                    end
                    for p, _ in pairs(frozenPlayers) do
                        if p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                            p.Character.HumanoidRootPart.Anchored = false
                        end
                    end
                    frozenPlayers = {}
                    SendNotification(T("killAllPlayers"), _G.SelectedLanguage == "Saudi" and "تم إيقاف القتل الجماعي" or "Kill all disabled")
                end
            end
        })
        
        murdererTab:Button({
            Title = T("killSheriff"),
            Size = "medium",
            Callback = function()
                killSheriff()
            end
        })

        -- =============================================
        -- 🤠 SHERIFF TAB (مع Auto Shoot من Rise)
        -- =============================================
        local sheriffTab = Window:Tab({
            Title = T("sheriffTab"),
            Icon = "target"
        })
        
        sheriffTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "ميزات الشرطة" or "Sheriff Features",
            Desc = "",
            Image = "target",
            ImageSize = 24
        })
        
        -- Auto Shoot System (من Rise)
        sheriffTab:Paragraph({
            Title = T("autoShoot"),
            Image = "target",
            ImageSize = 20
        })
        
        sheriffTab:Toggle({
            Title = T("autoShoot"),
            Value = false,
            Callback = function(state)
                _G.AutoShootEnabled = state
                if state then
                    task.spawn(startAutoShoot)
                    SendNotification(T("autoShoot"), _G.SelectedLanguage == "Saudi" and "تم تفعيل الإطلاق التلقائي" or "Auto shoot enabled")
                else
                    SendNotification(T("autoShoot"), _G.SelectedLanguage == "Saudi" and "تم إيقاف الإطلاق التلقائي" or "Auto shoot disabled")
                end
            end
        })
        
        sheriffTab:Toggle({
            Title = T("showManualButton"),
            Value = false,
            Callback = function(state)
                _G.ShootButtonActive = state
                if state then
                    createSheriffGui()
                else
                    removeSheriffGui()
                end
            end
        })
        
        sheriffTab:Button({
            Title = T("shootMurderer"),
            Callback = function()
                ShootMurderer()
            end
        })
        
        sheriffTab:Slider({
            Title = T("shootPrediction"),
            Description = _G.SelectedLanguage == "Saudi" and "توقع حركة الهدف" or "Predict target movement",
            Value = 0,
            Min = 0,
            Max = 20,
            Callback = function(value)
                _G.ShootPrediction = tonumber(value)
            end
        })
        
        sheriffTab:Slider({
            Title = T("pingMultiplier"),
            Description = _G.SelectedLanguage == "Saudi" and "تعديل حسب البينق" or "Adjust based on latency",
            Value = 0,
            Min = 0,
            Max = 10,
            Callback = function(value)
                _G.PingMultiplier = tonumber(value)
            end
        })
        
        sheriffTab:Divider()
        
        -- Other Sheriff Features
        sheriffTab:Toggle({
            Title = T("lockCamera"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                _G.LockCameraEnabled = state
                setupLockCamera()
            end
        })
        
        sheriffTab:Toggle({
            Title = T("aimbotMurderer"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                _G.AimbotMurdererEnabled = state
                if state then
                    setupAimbot("murderer")
                    SendNotification(T("aimbotMurderer"), _G.SelectedLanguage == "Saudi" and "تم تفعيل التصويب على القاتل" or "Murderer aimbot enabled")
                else
                    if espConnections.aimbotMurderer then
                        espConnections.aimbotMurderer:Disconnect()
                        espConnections.aimbotMurderer = nil
                    end
                    SendNotification(T("aimbotMurderer"), _G.SelectedLanguage == "Saudi" and "تم إيقاف التصويب على القاتل" or "Murderer aimbot disabled")
                end
            end
        })
        
        local shootDistanceInput = sheriffTab:Input({
            Title = T("shootDistance"),
            Value = tostring(_G.ShootDistance),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.ShootDistance = num
                end
            end
        })

        -- =============================================
        -- 📍 TELEPORT TAB
        -- =============================================
        local teleportTab = Window:Tab({
            Title = T("teleportTab"),
            Icon = "map-pin"
        })
        
        teleportTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "الانتقالات السريعة" or "Quick Teleports",
            Desc = "",
            Image = "map-pin",
            ImageSize = 24
        })
        
        local teleportPlayerList = {}
        for _, p in pairs(Players:GetPlayers()) do
            table.insert(teleportPlayerList, p.Name)
        end
        
        local teleportSelected = nil
        teleportTab:Dropdown({
            Title = T("playerSelectionTeleport"),
            Values = teleportPlayerList,
            Value = teleportPlayerList[1] or "",
            Size = "medium",
            Callback = function(selected)
                teleportSelected = selected
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToSelected"),
            Size = "medium",
            Callback = function()
                if teleportSelected then
                    local target = Players:FindFirstChild(teleportSelected)
                    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                            player.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame
                        end
                    end
                end
            end
        })
        
        teleportTab:Button({
            Title = T("updatePlayersList"),
            Size = "medium",
            Callback = function()
                local newList = {}
                for _, p in pairs(Players:GetPlayers()) do
                    table.insert(newList, p.Name)
                end
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToLobbyTP"),
            Size = "medium",
            Callback = function()
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = LobbyPosition
                end
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToSheriff"),
            Size = "medium",
            Callback = function()
                local sheriff = findSheriff()
                if sheriff and sheriff.Character and sheriff.Character:FindFirstChild("HumanoidRootPart") then
                    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        player.Character.HumanoidRootPart.CFrame = sheriff.Character.HumanoidRootPart.CFrame
                    end
                end
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToMurderer"),
            Size = "medium",
            Callback = function()
                local murderer = findMurderer()
                if murderer and murderer.Character and murderer.Character:FindFirstChild("HumanoidRootPart") then
                    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        player.Character.HumanoidRootPart.CFrame = murderer.Character.HumanoidRootPart.CFrame
                    end
                end
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToRandom"),
            Size = "medium",
            Callback = function()
                local allPlayers = Players:GetPlayers()
                if #allPlayers > 1 then
                    local randomPlayer
                    repeat
                        randomPlayer = allPlayers[math.random(1, #allPlayers)]
                    until randomPlayer ~= player
                    
                    if randomPlayer and randomPlayer.Character and randomPlayer.Character:FindFirstChild("HumanoidRootPart") then
                        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                            player.Character.HumanoidRootPart.CFrame = randomPlayer.Character.HumanoidRootPart.CFrame
                        end
                    end
                end
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToGun"),
            Size = "medium",
            Callback = function()
                local gun = findGun()
                if gun and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = gun.CFrame + Vector3.new(0, 3, 0)
                end
            end
        })

        -- =============================================
        -- ⚙️ SETTINGS TAB
        -- =============================================
        local settingsTab = Window:Tab({
            Title = T("settingsTab"),
            Icon = "settings"
        })
        
        settingsTab:Paragraph({
            Title = T("settings"),
            Desc = "",
            Image = "settings",
            ImageSize = 24
        })
        
        settingsTab:Toggle({
            Title = T("boostFPS"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                _G.BoostFPSEnabled = state
                setupBoostFPS()
            end
        })
        
        settingsTab:Toggle({
            Title = T("fullBrightness"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                _G.FullBrightEnabled = state
                setupFullBright()
            end
        })
        
        settingsTab:Toggle({
            Title = T("antiAFK"),
            Value = true,
            Size = "medium",
            Callback = function(state)
                _G.AntiAFKEnabled = state
                setupAntiAFK()
            end
        })
        
        settingsTab:Dropdown({
            Title = T("selectTheme"),
            Values = {"Dark", "Light", "Blue", "Red"},
            Value = "Dark",
            Size = "medium",
            Callback = function(selected)
                WindUI:SetTheme(selected)
            end
        })
        
        settingsTab:Divider({ Title = "" })
        
        settingsTab:Paragraph({
            Title = T("advancedSettings"),
            Desc = "",
            Image = "sliders",
            ImageSize = 24
        })
        
        settingsTab:Button({
            Title = T("rejoinServer"),
            Size = "medium",
            Callback = function()
                local ts = game:GetService("TeleportService")
                ts:Teleport(game.PlaceId)
            end
        })
        
        settingsTab:Button({
            Title = T("closeScriptBtn"),
            Size = "medium",
            Callback = function()
                Window:Destroy()
            end
        })
        
        settingsTab:Divider({ Title = "" })
        
        settingsTab:Paragraph({
            Title = T("developer"),
            Desc = "",
            Image = "user",
            ImageSize = 24
        })
        
        settingsTab:Button({
            Title = "👤 " .. T("developerName"),
            Size = "medium",
            Callback = function() end
        })
        
        settingsTab:Button({
            Title = "📱 " .. T("developerDesc"),
            Size = "medium",
            Callback = function() end
        })
        
        settingsTab:Button({
            Title = "🎮 " .. T("joinDiscord"),
            Size = "medium",
            Callback = function()
                setclipboard("https://discord.gg/CgUa36sPNs")
                SendNotification(T("notification"), T("linkCopiedContent"))
            end
        })
        
        settingsTab:Button({
            Title = "⚡ " .. T("version"),
            Size = "medium",
            Callback = function() end
        })

        Window:OnDestroy(function()
            cleanupGUI()
            clearAllHighlights()
            
            for name, connection in pairs(espConnections) do
                if connection then
                    connection:Disconnect()
                end
            end
            
            if playerEspConnection then playerEspConnection:Disconnect() end
            if roleUpdateConnection then roleUpdateConnection:Disconnect() end
            if coinEspConnection then coinEspConnection:Disconnect() end
            if gunEspConnection then gunEspConnection:Disconnect() end
            if HighlightsConnection then HighlightsConnection:Disconnect() end
            
            if BodyVelocity then BodyVelocity:Destroy() end
            if BodyGyro then BodyGyro:Destroy() end
            if sheriffGui then sheriffGui:Destroy() end
            
            stopGodMode()
            
            for _, esp in pairs(innocentEspElements) do cleanupDrawingTable(esp) end
            for _, esp in pairs(murderEspElements) do cleanupDrawingTable(esp) end
            for _, esp in pairs(sheriffEspElements) do cleanupDrawingTable(esp) end
            for _, esp in pairs(coinEspElements) do cleanupDrawingTable(esp) end
            for _, esp in pairs(gunEspElements) do cleanupDrawingTable(esp) end
        end)

        task.wait(3)
        WindUI:Notify({
            Title = T("welcome"),
            Content = _G.SelectedLanguage == "Saudi" 
                and "أهلاً وسهلاً في PhantomXPro 🌟"
                or "Welcome to PhantomXPro 🌟",
            Duration = 6
        })
        
        -- Enable Anti AFK by default
        setupAntiAFK()
    end
    
    -- نافذة اختيار اللغة
    WindUI:Popup({
        Title = "🌍 اختر اللغة / Select Language",
        Icon = "globe",
        Content = "اختر لغتك المفضلة:\nChoose your preferred language:",
        Buttons = {
            {
                Title = "🇸🇦 عربي",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    _G.SelectedLanguage = "Saudi"
                    WindUI:Notify({
                        Title = "✅ تم الاختيار",
                        Content = "تم اختيار اللغة العربية",
                        Duration = 3
                    })
                    task.wait(1)
                    createMainScript()
                end
            },
            {
                Title = "🇺🇸 English",
                Icon = "flag",
                Variant = "Secondary",
                Callback = function()
                    _G.SelectedLanguage = "English"
                    WindUI:Notify({
                        Title = "✅ Selected",
                        Content = "English language selected",
                        Duration = 3
                    })
                    task.wait(1)
                    createMainScript()
                end
            }
        }
    })
else
    print("❌ فشل تحميل WindUI / Failed to load WindUI")
end
print("Done - PhantomXPro Ultimate Edition Loaded")