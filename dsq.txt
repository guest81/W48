print("PhantomXPro - MM2 Script (Enhanced with Rise Features)")

-- تحميل WindUI
local success, result = pcall(function()
    local source = game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua", true)
    if source then
        local loaded = loadstring(source)
        if loaded then
            return loaded()
        end
    end
    return nil
end)

if success and result then
    local WindUI = result
    
    -- =============================================
    -- نظام اللغات - عربي (سعودي) + إنجليزي
    -- =============================================
    _G.SelectedLanguage = _G.SelectedLanguage or "Saudi"
    
    local Translations = {
        Saudi = {
            welcome = "أهلاً وسهلاً في PhantomXPro 🌟",
            warning = "⚠️ تنبيه هام:\nتشغيل السكربت يكون على مسؤوليتك الشخصية والكاملة، ونحن غير مسؤولين عن أي ضرر. مع أننا نعمل على تقديم سكربت بأعلى معايير الأمان الممكنة، هذا تحذير احتراطي في حال حدوث أي مشكلة غير متوقعة.نتمني لكم تجربة ممتعة ❤",
            discord = "🎮 انظم لسيرفر الدسكورد",
            discordContent = "انظم للسيرفر الرسمي للسكربت!\n• آخر التحديثات\n• مساعدة ودعم\n• اقتراحات وتطويرات\n• تحديثات أمان\n\nالرابط: https://discord.gg/CgUa36sPNs",
            copyLink = "انسخ الرابط",
            linkCopied = "✅ انسخت الرابط",
            linkCopiedContent = "رابط الدسكورد في الحافظة",
            scriptClosed = "أقفل السكربت",
            notification = "إشعار",
            
            -- تبويبات
            infoTab = "👤 معلوماتك",
            mainTab = "🏠 الرئيسي",
            espTab = "👁️ رؤية",
            playerTab = "🏃 حركات",
            innocentTab = "🛡️ أبرياء",
            murdererTab = "🔪 قتلة",
            sheriffTab = "🤠 شرطة",
            teleportTab = "📍 انتقالات",
            settingsTab = "⚙️ إعدادات",
            
            -- MAIN
            autofarm = "💰 جمع العملات",
            teleportToLobby = "🏠 انتقال للوبي",
            teleportTool = "🛠️ أداة الانتقال",
            
            -- ESP
            xrayVision = "🟦 رؤية خلف الجدران",
            murdererESP = "🔴 شوف قاتل",
            innocentESP = "🟢 شوف بريء",
            sheriffESP = "🔵 شوف شرطي",
            gunDropESP = "🔫 شوف سلاح",
            coinESP = "🪙 شوف عملات",
            
            -- PLAYER
            walkspeedAdjustment = "👟 سرعة المشي (16)",
            jumpPowerAdjustment = "🚀 قوة القفز (50)",
            gravityAdjustment = "🌌 الجاذبية (196.2)",
            fovAdjustment = "🎥 زاوية الرؤية (70)",
            flySpeedAdjustment = "✈️ سرعة الطيران (60)",
            infiniteJump = "☁️ قفز لا نهائي",
            noclipAdjustment = "👻 عبور الجدران",
            flyMode = "🦅 طيران",
            unlockEmotes = "🎭 فتح كل الإيموشنات",
            
            -- INNOCENT
            getDroppedGun = "🔫 سحب السلاح",
            autoGetDroppedGun = "🤖 سحب تلقائي",
            getGunAndShoot = "💥 خذ السلاح و صوب",
            flingMurderer = "🌀 تطير القاتل",
            flingSheriff = "🌀 تطير الشرطي",
            godMode = "🛡️ وضع الخلود",
            godModeMethod = "طريقة الخلود",
            
            -- MURDERER
            aimbot = "🎯 تصويب تلقائي",
            selectPlayer = "👤 اختر لاعب",
            refreshPlayers = "🔄 تحديث القائمة",
            killSelected = "💀 قتل المختار",
            killAllPlayers = "❌ قتل الجميع",
            killSheriff = "💀 قتل الشرطي",
            
            -- SHERIFF
            shotTypeSelection = "🛰️ نوع الطلقة",
            shootMurderer = "🔫 صوب على القاتل",
            lockCamera = "🔒 تثبيت الكاميرا",
            aimbotMurderer = "🎯 تصويب تلقائي",
            shootButton = "🔫 زر الإطلاق",
            shootDistance = "📏 مسافة الإطلاق (500)",
            autoShoot = "🎯 تصويب تلقائي مستمر",
            manualShoot = "🖱️ زر إطلاق يدوي",
            shootPrediction = "📐 توقع التصويب",
            pingMultiplier = "📡 مضاعف البينق",
            shootNow = "💥 اضرب الآن",
            
            -- GUN GRABBER
            grabMode = "وضع السحب",
            grabOnly = "سحب فقط",
            grabAndShoot = "سحب وتصويب",
            manualGrab = "🖐️ سحب يدوي",
            notifyGunEvents = "🔔 إشعارات السلاح",
            
            -- FLING
            flingTargetInput = "🎯 اسم الهدف",
            flingMode = "طريقة التطويع",
            flingMode1 = "مقلاع",
            flingMode2 = "صاعق",
            flingMode3 = "دفع",
            flingActive = "🌀 تفعيل التطويع",
            flingMurdererBtn = "💀 تطويع القاتل",
            flingSheriffBtn = "👮 تطويع الشرطي",
            
            -- ANTI VOID
            antiVoid = "🕳️ حماية من الفراغ",
            
            -- AUTO FARM
            movementMode = "وضع الحركة",
            teleportDelay = "⏱️ تأخير الانتقال",
            smoothSpeed = "🚀 سرعة الحركة",
            checkInterval = "📡 فترة الفحص",
            
            -- TELEPORT
            playerSelectionTeleport = "👤 اختر لاعب",
            teleportToSelected = "🌀 انتقل للاعب",
            updatePlayersList = "🔄 تحديث القائمة",
            teleportToLobbyTP = "🏠 انتقال للوبي",
            teleportToSheriff = "🔵 انتقل لشرطي",
            teleportToMurderer = "🔴 انتقل لقاتل",
            teleportToRandom = "🎲 انتقل عشوائي",
            teleportToGun = "🔫 انتقل للسلاح",
            teleportToInnocent = "👼 انتقل لبريء",
            teleportToCoin = "🪙 انتقل للعملة",
            teleportToMap = "🗺️ انتقل للخريطة",
            
            -- SETTINGS
            settings = "⚙️ إعدادات",
            advancedSettings = "🔧 إعدادات متقدمة",
            developer = "💼 المطور",
            
            boostFPS = "🚀 تحسين الفريمات",
            fullBrightness = "💡 إضاءة كاملة",
            antiAFK = "💤 منع الطرد",
            rejoinServer = "🔄 رجع للسيرفر",
            serverHop = "🛰️ غير السيرفر",
            joinLowerServer = "📉 سيرفر بأقل ناس",
            selectTheme = "🎨 غير المظهر",
            updateScript = "🔄 حدث السكربت",
            closeScriptBtn = "❌ قفل السكربت",
            
            -- INFO
            yourName = "👤 اسمك: ",
            yourUsername = "🎫 يوزرك: ",
            accountAge = "📅 عمر الحساب: ",
            hack = "⚡ الهاك: ",
            level = "📊 مستواك: ",
            xp = "⭐ خبرتك: ",
            players = "👥 عدد اللاعبين: ",
            fps = "🔄 فريماتك: ",
            ping = "📡 البينق: ",
            
            -- Developer
            developerName = "Cypher",
            developerDesc = "مبرمج سكربتات روبلوكس",
            joinDiscord = "انضم للدسكورد",
            version = "نسخة 12.0 (Enhanced)",
            contactDev = "تواصل مع المبرمج",
            
            -- Esp Translations
            innocentHighlight = "🌟 هايلايت بريء",
            murderHighlight = "🔥 هايلايت قاتل",
            sheriffHighlight = "💙 هايلايت شرطي",
            gunHighlight = "🔫 هايلايت سلاح",
            coinHighlight = "🪙 هايلايت عملة",
            boxESP = "📦 صندوق ESP",
            nameESP = "🏷️ اسم ESP",
            tracerESP = "📌 تريسير ESP",
            distanceESP = "📏 مسافة ESP",
            mode3D = "وضع 3D",
            mode2D = "وضع 2D"
        },
        English = {
            welcome = "Welcome to PhantomXPro 🌟",
            warning = "⚠️ Important Warning:\nUsing this script is entirely at your own risk and responsibility. We are not responsible for any damage. Although we work to provide a script with the highest possible security standards, this is a precautionary warning in case any unexpected problem occurs. We hope you have an enjoyable experience ❤",
            discord = "🎮 Join Discord Server",
            discordContent = "Join the official script server!\n• Latest updates\n• Support\n• Suggestions\n• Security updates\n\nLink: https://discord.gg/CgUa36sPNs",
            copyLink = "Copy Link",
            linkCopied = "✅ Link Copied",
            linkCopiedContent = "Discord link copied",
            scriptClosed = "Script Closed",
            notification = "Notification",
            
            -- Tabs
            infoTab = "👤 Your Info",
            mainTab = "🏠 Main",
            espTab = "👁️ Esp",
            playerTab = "🏃 Movement",
            innocentTab = "🛡️ Innocent",
            murdererTab = "🔪 Murderer",
            sheriffTab = "🤠 Sheriff",
            teleportTab = "📍 Teleport",
            settingsTab = "⚙️ Settings",
            
            -- MAIN
            autofarm = "💰 Collect Coins",
            teleportToLobby = "🏠 Go to Lobby",
            teleportTool = "🛠️ Teleport Tool",
            
            -- ESP
            xrayVision = "🟦 X-Ray Vision",
            murdererESP = "🔴 Murderer ESP",
            innocentESP = "🟢 Innocent ESP",
            sheriffESP = "🔵 Sheriff ESP",
            gunDropESP = "🔫 Gun ESP",
            coinESP = "🪙 Coin ESP",
            
            -- PLAYER
            walkspeedAdjustment = "👟 Walk Speed (16)",
            jumpPowerAdjustment = "🚀 Jump Power (50)",
            gravityAdjustment = "🌌 Gravity (196.2)",
            fovAdjustment = "🎥 FOV (70)",
            flySpeedAdjustment = "✈️ Fly Speed (60)",
            infiniteJump = "☁️ Infinite Jump",
            noclipAdjustment = "👻 Noclip",
            flyMode = "🦅 Fly Mode",
            unlockEmotes = "🎭 Unlock All Emotes",
            
            -- INNOCENT
            getDroppedGun = "🔫 Get Gun",
            autoGetDroppedGun = "🤖 Auto Gun",
            getGunAndShoot = "💥 Get Gun & Shoot",
            flingMurderer = "🌀 Fling Murderer",
            flingSheriff = "🌀 Fling Sheriff",
            godMode = "🛡️ God Mode",
            godModeMethod = "God Mode Method",
            
            -- MURDERER
            aimbot = "🎯 Aimbot",
            selectPlayer = "👤 Select Player",
            refreshPlayers = "🔄 Refresh List",
            killSelected = "💀 Kill Selected",
            killAllPlayers = "❌ Kill All",
            killSheriff = "💀 Kill Sheriff",
            
            -- SHERIFF
            shotTypeSelection = "🛰️ Shot Type",
            shootMurderer = "🔫 Shoot Murderer",
            lockCamera = "🔒 Lock Camera",
            aimbotMurderer = "🎯 Aimbot",
            shootButton = "🔫 Shoot Button",
            shootDistance = "📏 Shoot Distance (500)",
            autoShoot = "🎯 Auto Shoot",
            manualShoot = "🖱️ Manual Shoot Button",
            shootPrediction = "📐 Shoot Prediction",
            pingMultiplier = "📡 Ping Multiplier",
            shootNow = "💥 Shoot Now",
            
            -- GUN GRABBER
            grabMode = "Grab Mode",
            grabOnly = "Grab only",
            grabAndShoot = "Grab & shoot",
            manualGrab = "🖐️ Manual Grab",
            notifyGunEvents = "🔔 Notify Gun Events",
            
            -- FLING
            flingTargetInput = "🎯 Target Name",
            flingMode = "Fling Mode",
            flingMode1 = "Flinger",
            flingMode2 = "Shhhlol",
            flingMode3 = "Yeet",
            flingActive = "🌀 Fling Active",
            flingMurdererBtn = "💀 Fling Murderer",
            flingSheriffBtn = "👮 Fling Sheriff",
            
            -- ANTI VOID
            antiVoid = "🕳️ Anti Void",
            
            -- AUTO FARM
            movementMode = "Movement Mode",
            teleportDelay = "⏱️ Teleport Delay",
            smoothSpeed = "🚀 Smooth Speed",
            checkInterval = "📡 Check Interval",
            
            -- TELEPORT
            playerSelectionTeleport = "👤 Select Player",
            teleportToSelected = "🌀 Teleport to Player",
            updatePlayersList = "🔄 Update List",
            teleportToLobbyTP = "🏠 Teleport to Cabin",
            teleportToSheriff = "🔵 Teleport to Sheriff",
            teleportToMurderer = "🔴 Teleport to Murderer",
            teleportToRandom = "🎲 Teleport Random",
            teleportToGun = "🔫 Teleport to Gun",
            teleportToInnocent = "👼 Teleport to Innocent",
            teleportToCoin = "🪙 Teleport to Coin",
            teleportToMap = "🗺️ Teleport to Map",
            
            -- SETTINGS
            settings = "⚙️ Settings",
            advancedSettings = "🔧 Advanced",
            developer = "💼 Developer",
            
            boostFPS = "🚀 Boost FPS",
            fullBrightness = "💡 Full Bright",
            antiAFK = "💤 Anti AFK",
            rejoinServer = "🔄 Rejoin Server",
            serverHop = "🛰️ Server Hop",
            joinLowerServer = "📉 Join Lower Server",
            selectTheme = "🎨 Select Theme",
            updateScript = "🔄 Update Script",
            closeScriptBtn = "❌ Close Script",
            
            -- INFO
            yourName = "👤 Name: ",
            yourUsername = "🎫 Username: ",
            accountAge = "📅 Account Age: ",
            hack = "⚡ Executor: ",
            level = "📊 Level: ",
            xp = "⭐ XP: ",
            players = "👥 Players: ",
            fps = "🔄 FPS: ",
            ping = "📡 Ping: ",
            
            -- Developer
            developerName = "Cypher",
            developerDesc = "Roblox Script Developer",
            joinDiscord = "Join Discord",
            version = "Version 12.0 (Enhanced)",
            contactDev = "Contact Developer",
            
            -- Esp Translations
            innocentHighlight = "🌟 Innocent Highlight",
            murderHighlight = "🔥 Murderer Highlight",
            sheriffHighlight = "💙 Sheriff Highlight",
            gunHighlight = "🔫 Gun Highlight",
            coinHighlight = "🪙 Coin Highlight",
            boxESP = "📦 Box ESP",
            nameESP = "🏷️ Name ESP",
            tracerESP = "📌 Tracer ESP",
            distanceESP = "📏 Distance ESP",
            mode3D = "3D Mode",
            mode2D = "2D Mode"
        }
    }
    
    local function T(key)
        return Translations[_G.SelectedLanguage][key] or key
    end
    
    local function cleanupGUI()
        for _, v in pairs(game.CoreGui:GetChildren()) do
            if v:IsA("ScreenGui") and (v.Name:find("Wind") or v.Name:find("Phantom") or v.Name:find("Shoot") or v.Name:find("SheriffGui")) then
                v:Destroy()
            end
        end
    end
    
    -- =============================================
    -- المتغيرات العالمية
    -- =============================================
    _G.WalkspeedValue = 16
    _G.JumpPowerValue = 50
    _G.GravityValue = 196.2
    _G.FOVValue = 70
    _G.FlySpeedValue = 60
    _G.ShootDistance = 500
    _G.LockCameraEnabled = false
    _G.XrayEnabled = false
    _G.InfiniteJumpEnabled = false
    _G.NoclipEnabled = false
    _G.FlyEnabled = false
    _G.KillAllEnabled = false
    _G.FullBrightEnabled = false
    _G.AntiAFKEnabled = true
    _G.BoostFPSEnabled = false
    
    -- New variables from Rise
    _G.AutoShootEnabled = false
    _G.ShootPrediction = 0
    _G.PingMultiplier = 0
    _G.GrabMode = "Grab only"
    _G.AutoGrabEnabled = false
    _G.NotifyGunEvents = false
    _G.GodModeEnabled = false
    _G.GodModeMethod = "Health Math.huge"
    _G.AntiVoidEnabled = false
    _G.AutoFarmEnabled = false
    _G.FarmMode = "Teleport"
    _G.TeleportDelay = 0
    _G.FarmSpeed = 50
    _G.CheckInterval = 0.5
    
    -- ESP variables from Rise
    _G.InnocentESP = {names = false, boxes = false, tracers = false, distance = false, boxType = "2D"}
    _G.MurderESP = {names = false, boxes = false, tracers = false, distance = false, boxType = "2D"}
    _G.SheriffESP = {names = false, boxes = false, tracers = false, distance = false, boxType = "2D"}
    _G.GunESP = {names = false, boxes = false, tracers = false, distance = false, boxType = "3D"}
    _G.CoinESPConfig = {names = false, boxes = false, tracers = false, distance = false, boxType = "3D"}
    _G.InnocentHighlights = false
    _G.MurderHighlights = false
    _G.SheriffHighlights = false
    _G.GunHighlights = false
    _G.CoinHighlights = false
    
    -- متغيرات الاتصالات
    local playerHighlights = {}
    local espConnections = {}
    local espLoops = {}
    local playerRoles = {}
    local gunTexts = {}
    local coinHighlights = {}
    local gunHighlights = {}
    
    -- متغيرات الطيران
    local BodyVelocity = nil
    local BodyGyro = nil
    
    -- متغيرات التجميد
    local frozenPlayers = {}
    
    -- إحداثيات الكوتش
    local LobbyPosition = CFrame.new(361.785, 521.475, -11.444)
    
    -- =============================================
    -- الدوال المساعدة من Rise
    -- =============================================
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")
    local Workspace = game:GetService("Workspace")
    local UserInputService = game:GetService("UserInputService")
    local player = Players.LocalPlayer
    
    local function getPlayerRoleFromServer()
        local getPlayerDataRemote = ReplicatedStorage:FindFirstChild("GetPlayerData", true)
        if not getPlayerDataRemote then return nil end
        local success, roles = pcall(function()
            return getPlayerDataRemote:InvokeServer()
        end)
        if success and roles then
            for name, data in pairs(roles) do
                if name == player.Name then
                    return data.Role
                end
            end
        end
        return nil
    end
    
    local function findMurderer()
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player and p.Character then
                local hasKnife = p.Character:FindFirstChild("Knife") or 
                                (p.Backpack and p.Backpack:FindFirstChild("Knife"))
                if hasKnife then
                    return p
                end
            end
        end
        return nil
    end
    
    local function findSheriff()
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player and p.Character then
                local hasGun = p.Character:FindFirstChild("Gun") or 
                              (p.Backpack and p.Backpack:FindFirstChild("Gun"))
                if hasGun then
                    return p
                end
            end
        end
        return nil
    end
    
    local function findGun()
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart") and (obj.Name == "GunDrop" or obj.Name == "DropGun") then
                return obj
            end
        end
        return nil
    end
    
    local function findCoins()
        local coins = {}
        for _, container in pairs(workspace:GetDescendants()) do
            if container:IsA("Model") and container.Name == "CoinContainer" then
                for _, coinServer in pairs(container:GetChildren()) do
                    if coinServer.Name == "Coin_Server" then
                        local coinVisual = coinServer:FindFirstChild("CoinVisual")
                        if coinVisual then
                            local mainCoin = coinVisual:FindFirstChild("MainCoin")
                            if mainCoin and mainCoin:IsA("MeshPart") and mainCoin.Parent then
                                table.insert(coins, mainCoin)
                            end
                        end
                    end
                end
            end
        end
        return coins
    end
    
    -- =============================================
    -- GOD MODE from Rise
    -- =============================================
    local godModeConnection = nil
    
    local function applyGodMode()
        local char = player.Character
        if not char then return end
        
        if _G.GodModeMethod == "Humanoid Replacement (Very buggy)" then
            local Human = char:FindFirstChildWhichIsA("Humanoid")
            if not Human then return end
            
            local nHuman = Human:Clone()
            nHuman.Parent = char
            player.Character = nil
            nHuman:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
            nHuman:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
            nHuman:SetStateEnabled(Enum.HumanoidStateType.Physics, false)
            nHuman.BreakJointsOnDeath = true
            nHuman.MaxHealth = math.huge
            nHuman.Health = math.huge
            Human:Destroy()
            player.Character = char
            nHuman.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
            
            local Script = char:FindFirstChild("Animate")
            if Script then
                Script.Disabled = true
                task.wait()
                Script.Disabled = false
            end
        elseif _G.GodModeMethod == "Health Math.huge" then
            local Human = char:FindFirstChildWhichIsA("Humanoid")
            if not Human then return end
            
            Human.MaxHealth = math.huge
            Human.Health = math.huge
            
            local connection
            connection = Human:GetPropertyChangedSignal("Health"):Connect(function()
                if _G.GodModeEnabled and Human.Health < Human.MaxHealth then
                    Human.Health = Human.MaxHealth
                else
                    if not _G.GodModeEnabled and connection then connection:Disconnect() end
                end
            end)
            table.insert(espConnections, connection)
        end
    end
    
    local function startGodMode()
        if godModeConnection then return end
        
        godModeConnection = RunService.Heartbeat:Connect(function()
            if _G.GodModeEnabled and player.Character then
                local Human = player.Character:FindFirstChildWhichIsA("Humanoid")
                if Human and Human.Health < math.huge then
                    applyGodMode()
                end
            end
        end)
        espConnections.godMode = godModeConnection
    end
    
    local function stopGodMode()
        if godModeConnection then
            godModeConnection:Disconnect()
            godModeConnection = nil
            espConnections.godMode = nil
        end
    end
    
    -- =============================================
    -- AUTO SHOOT from Rise
    -- =============================================
    local sheriffGui = nil
    
    local function makeDraggable(frame)
        local dragging, dragInput, dragStart, startPos
        
        local function update(input)
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
        
        frame.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                dragging = true
                dragStart = input.Position
                startPos = frame.Position
                input.Changed:Connect(function()
                    if input.UserInputState == Enum.UserInputState.End then dragging = false end
                end)
            end
        end)
        
        frame.InputChanged:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
                dragInput = input
            end
        end)
        
        UserInputService.InputChanged:Connect(function(input)
            if input == dragInput and dragging then update(input) end
        end)
    end
    
    local function ShootMurderer()
        if not player.Character then return false end
        
        local murderer = findMurderer()
        if not murderer or not murderer.Character then return false end
        
        local gun = player.Character:FindFirstChild("Gun") or player.Backpack:FindFirstChild("Gun")
        if not gun then return false end
        
        if gun.Parent ~= player.Character then
            gun.Parent = player.Character
            task.wait(0.1)
        end
        
        gun = player.Character:FindFirstChild("Gun")
        if gun and gun:FindFirstChild("KnifeLocal") then
            local targetPart = murderer.Character:FindFirstChild("HumanoidRootPart")
            if targetPart then
                local targetPos = targetPart.Position
                local mHum = murderer.Character:FindFirstChild("Humanoid")
                
                if _G.ShootPrediction ~= 0 and mHum then
                    local moveDir = mHum.MoveDirection
                    if moveDir.Magnitude > 0 then
                        local finalOffset = _G.ShootPrediction + (_G.PingMultiplier * 0.5)
                        targetPos = targetPos + (moveDir.Unit * finalOffset)
                    end
                end
                
                pcall(function()
                    gun.KnifeLocal.CreateBeam.RemoteFunction:InvokeServer(1, targetPos, "AH2")
                end)
                return true
            end
        end
        return false
    end
    
    local function startAutoShoot()
        while _G.AutoShootEnabled and RunService.Heartbeat:Wait() do
            ShootMurderer()
            task.wait(0.25)
        end
    end
    
    local function createSheriffGui()
        if sheriffGui then sheriffGui:Destroy() end
        
        sheriffGui = Instance.new("ScreenGui")
        sheriffGui.Name = "SheriffGui"
        sheriffGui.Parent = game:GetService("CoreGui")
        sheriffGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
        sheriffGui.ResetOnSpawn = false
        
        local Frame = Instance.new("Frame")
        Frame.Parent = sheriffGui
        Frame.BackgroundColor3 = Color3.fromRGB(26, 26, 26)
        Frame.BackgroundTransparency = 0.450
        Frame.Position = UDim2.new(0.5, -80, 0.2, 0)
        Frame.Size = UDim2.new(0, 161, 0, 78)
        Frame.Active = true
        
        local UICorner = Instance.new("UICorner")
        UICorner.Parent = Frame
        
        local TextLabel = Instance.new("TextLabel")
        TextLabel.Parent = Frame
        TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        TextLabel.BackgroundTransparency = 1.000
        TextLabel.Position = UDim2.new(0, 0, 0.1025, 0)
        TextLabel.Size = UDim2.new(0, 161, 0, 29)
        TextLabel.Font = Enum.Font.Michroma
        TextLabel.Text = _G.SelectedLanguage == "Saudi" and "اطلق" or "SHOOT"
        TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        TextLabel.TextScaled = true
        TextLabel.TextSize = 14.000
        TextLabel.TextWrapped = true
        
        local TriggerButton = Instance.new("TextButton")
        TriggerButton.Name = "TriggerButton"
        TriggerButton.Parent = Frame
        TriggerButton.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
        TriggerButton.Position = UDim2.new(0.0496, 0, 0.5512, 0)
        TriggerButton.Size = UDim2.new(0, 144, 0, 27)
        TriggerButton.Font = Enum.Font.Michroma
        TriggerButton.Text = T("shootNow")
        TriggerButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        TriggerButton.TextScaled = true
        TriggerButton.TextSize = 14.000
        TriggerButton.TextWrapped = true
        
        local UICorner_2 = Instance.new("UICorner")
        UICorner_2.Parent = TriggerButton
        
        makeDraggable(Frame)
        
        TriggerButton.MouseButton1Click:Connect(function()
            ShootMurderer()
        end)
    end
    
    local function removeSheriffGui()
        if sheriffGui then
            sheriffGui:Destroy()
            sheriffGui = nil
        end
    end
    
    -- =============================================
    -- GUN GRABBER from Rise
    -- =============================================
    local lastNotifyTime = 0
    local notifyCooldown = 25
    
    local function SendNotification(title, description)
        if tick() - lastNotifyTime < notifyCooldown then return end
        lastNotifyTime = tick()
        WindUI:Notify({
            Title = title,
            Content = description,
            Duration = 4
        })
    end
    
    local function ManualGrab()
        local hasKnife = player.Backpack:FindFirstChild("Knife") or (player.Character and player.Character:FindFirstChild("Knife"))
        if hasKnife then
            SendNotification(T("notification"), "You cannot pick up the gun (You are Murderer)!")
            return
        end
        
        local gun = findGun()
        if not gun then
            SendNotification(T("notification"), "No gun drops found!")
            return
        end
        
        local char = player.Character
        if not char then return end
        local root = char:FindFirstChild("HumanoidRootPart")
        if not root then return end
        
        local prevPos = root.CFrame
        root.CFrame = gun.CFrame
        task.wait(0.2)
        local prompt = gun:FindFirstChildOfClass("ProximityPrompt")
        if prompt then 
            fireproximityprompt(prompt)
        end
        task.wait(0.2)
        root.CFrame = prevPos
        
        if _G.GrabMode == "Grab & shoot murderer" then
            task.wait(0.5)
            local gunTool = player.Character:FindFirstChild("Gun") or player.Backpack:FindFirstChild("Gun")
            if gunTool then
                if gunTool.Parent ~= player.Character then
                    gunTool.Parent = player.Character
                end
                ShootMurderer()
            end
        end
    end
    
    local function AutoGrabLoop()
        while _G.AutoGrabEnabled do
            local hasKnife = player.Backpack:FindFirstChild("Knife") or (player.Character and player.Character:FindFirstChild("Knife"))
            if not hasKnife then
                ManualGrab()
            end
            task.wait(2)
        end
    end
    
    local function MonitorGunEvents()
        workspace.DescendantAdded:Connect(function(child)
            if _G.NotifyGunEvents and (child.Name == "GunDrop" or child.Name == "DropGun") and child:IsA("BasePart") then
                SendNotification(T("notification"), "A gun has dropped on the map!")
            end
        end)
        
        for _, plr in ipairs(Players:GetPlayers()) do
            plr.CharacterAdded:Connect(function(char)
                char.ChildAdded:Connect(function(child)
                    if _G.NotifyGunEvents and child.Name == "Gun" and plr ~= player then
                        SendNotification(T("notification"), plr.Name .. " has picked up the gun!")
                    end
                end)
            end)
        end
    end
    task.spawn(MonitorGunEvents)
    
    -- =============================================
    -- FLING SYSTEM from Rise (Simplified)
    -- =============================================
    local flingActive = false
    local flingMode = 1
    local flingTargetInput = ""
    local processedPlayers = {}
    local originalPositions = {}
    
    local function Flinger(targetPlayer, duration)
        local startTime = tick()
        local Character = player.Character
        local Humanoid = Character and Character:FindFirstChildOfClass("Humanoid")
        local RootPart = Humanoid and Humanoid.RootPart
        local TCharacter = targetPlayer.Character
        local THumanoid = TCharacter and TCharacter:FindFirstChildOfClass("Humanoid")
        local TRootPart = THumanoid and THumanoid.RootPart
        
        if not (Character and Humanoid and RootPart and TCharacter and THumanoid and TRootPart) then return end
        
        if RootPart.Velocity.Magnitude < 50 then originalPositions[targetPlayer] = RootPart.CFrame end
        
        local previousDestroyHeight = workspace.FallenPartsDestroyHeight
        workspace.FallenPartsDestroyHeight = 0/0
        
        local BV = Instance.new("BodyVelocity", RootPart)
        BV.Velocity = Vector3.new(9e8, 9e8, 9e8)
        BV.MaxForce = Vector3.new(1/0, 1/0, 1/0)
        
        local connection
        connection = RunService.Heartbeat:Connect(function()
            if not flingActive or not targetPlayer.Character or not RootPart or not TRootPart then
                connection:Disconnect()
                BV:Destroy()
                workspace.FallenPartsDestroyHeight = previousDestroyHeight
                return
            end
            
            RootPart.CFrame = TRootPart.CFrame * CFrame.new(0, 1.5, 2)
            RootPart.Velocity = Vector3.new(9e7, 9e7 * 10, 9e7)
            RootPart.RotVelocity = Vector3.new(9e8, 9e8, 9e8)
            
            if duration and tick() > startTime + duration then
                connection:Disconnect()
                BV:Destroy()
                workspace.FallenPartsDestroyHeight = previousDestroyHeight
                if originalPositions[targetPlayer] then
                    RootPart.CFrame = originalPositions[targetPlayer]
                end
            end
        end)
        
        table.insert(espConnections, connection)
    end
    
    local function startFling()
        if not flingActive or flingTargetInput == "" then return end
        
        local targetPlayers = {}
        local input = string.lower(flingTargetInput)
        
        if input == "all" then
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= player then table.insert(targetPlayers, p) end
            end
        elseif input == "murder" then
            local murderer = findMurderer()
            if murderer then table.insert(targetPlayers, murderer) end
        elseif input == "sheriff" then
            local sheriff = findSheriff()
            if sheriff then table.insert(targetPlayers, sheriff) end
        else
            for _, p in pairs(Players:GetPlayers()) do
                if string.lower(p.Name):find(input) or string.lower(p.DisplayName or ""):find(input) then
                    table.insert(targetPlayers, p)
                end
            end
        end
        
        for _, target in pairs(targetPlayers) do
            if target and target.Character then
                local duration = (input == "all") and 1.5 or nil
                Flinger(target, duration)
            end
        end
    end
    
    -- =============================================
    -- ANTI VOID from Rise
    -- =============================================
    local originalDestroyHeight = nil
    
    local function enableAntiVoid()
        if _G.AntiVoidEnabled then
            originalDestroyHeight = workspace.FallenPartsDestroyHeight
            workspace.FallenPartsDestroyHeight = -math.huge
        end
    end
    
    local function disableAntiVoid()
        if originalDestroyHeight then
            workspace.FallenPartsDestroyHeight = originalDestroyHeight
        end
    end
    
    -- =============================================
    -- AUTO FARM from Rise
    -- =============================================
    local farmConnection = nil
    
    local function getHumanoidRootPart()
        if not player.Character then return nil end
        return player.Character:FindFirstChild("HumanoidRootPart")
    end
    
    local function findNearestCoin()
        local hrp = getHumanoidRootPart()
        if not hrp then return nil end
        
        local closestCoin
        local shortestDistance = math.huge
        
        local coinContainers = {"Factory", "Hospital3", "MilBase", "House2", "Workplace", "Mansion2", "BioLab", "Hotel", "Bank2", "PoliceStation", "ResearchFacility"}
        
        for _, containerName in ipairs(coinContainers) do
            local container = workspace:FindFirstChild(containerName)
            if container then
                local coinContainer = container:FindFirstChild("CoinContainer")
                if coinContainer then
                    for _, coin in ipairs(coinContainer:GetChildren()) do
                        if coin:IsA("BasePart") and coin.Parent then
                            local distance = (hrp.Position - coin.Position).Magnitude
                            if distance < shortestDistance then
                                shortestDistance = distance
                                closestCoin = coin
                            end
                        end
                    end
                end
            end
        end
        
        if not closestCoin then
            for _, coin in pairs(findCoins()) do
                if coin then
                    local distance = (hrp.Position - coin.Position).Magnitude
                    if distance < shortestDistance then
                        shortestDistance = distance
                        closestCoin = coin
                    end
                end
            end
        end
        
        return closestCoin
    end
    
    local function teleportToCoin(coin)
        local hrp = getHumanoidRootPart()
        if hrp and coin then
            hrp.CFrame = CFrame.new(coin.Position + Vector3.new(0, 3, 0))
            task.wait(_G.TeleportDelay)
        end
    end
    
    local function smoothMoveToCoin(coin)
        local hrp = getHumanoidRootPart()
        if not hrp or not coin then return end
        
        local startPos = hrp.Position
        local endPos = coin.Position + Vector3.new(0, 3, 0)
        local distance = (endPos - startPos).Magnitude
        local duration = distance / _G.FarmSpeed
        local startTime = tick()
        
        while tick() - startTime < duration and _G.AutoFarmEnabled do
            if not coin.Parent then break end
            local progress = math.min((tick() - startTime) / duration, 1)
            hrp.CFrame = CFrame.new(startPos:Lerp(endPos, progress))
            task.wait()
        end
    end
    
    local function walkToCoin(coin)
        if not player.Character or not coin then return end
        local humanoid = player.Character:FindFirstChildOfClass("Humanoid")
        if not humanoid then return end
        
        humanoid.WalkSpeed = 16
        humanoid:MoveTo(coin.Position + Vector3.new(0, 0, 3))
        local startTime = tick()
        
        while _G.AutoFarmEnabled and humanoid.MoveDirection.Magnitude > 0 and tick() - startTime < 10 do
            task.wait(0.5)
        end
    end
    
    local function collectCoin(coin)
        local hrp = getHumanoidRootPart()
        if hrp and coin and coin.Parent then
            firetouchinterest(hrp, coin, 0)
            firetouchinterest(hrp, coin, 1)
            task.wait(1)
        end
    end
    
    local function farmLoop()
        while _G.AutoFarmEnabled do
            local coin = findNearestCoin()
            if coin then
                if _G.FarmMode == "Teleport" then
                    teleportToCoin(coin)
                elseif _G.FarmMode == "Smooth" then
                    smoothMoveToCoin(coin)
                else
                    walkToCoin(coin)
                end
                collectCoin(coin)
            else
                task.wait(1)
            end
            task.wait(_G.CheckInterval)
        end
    end
    
    -- =============================================
    -- ESP SYSTEM from Rise
    -- =============================================
    local roleData = {}
    local innocentEspElements = {}
    local murderEspElements = {}
    local sheriffEspElements = {}
    local coinEspElements = {}
    local gunEspElements = {}
    local playerEspConnection = nil
    local coinEspConnection = nil
    local gunEspConnection = nil
    local HighlightsConnection = nil
    
    local function IsAlive(plr)
        return plr and plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health > 0
    end
    
    local function getOutlineColor(color)
        if color.R > 0.8 and color.G > 0.8 and color.B > 0.8 then
            return Color3.new(0,0,0)
        else
            return Color3.new(1,1,1)
        end
    end
    
    local function getDistanceFromPlayer(targetPosition)
        if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then return 0 end
        return (targetPosition - player.Character.HumanoidRootPart.Position).Magnitude
    end
    
    local function cleanupDrawingTable(drawingTable)
        if not drawingTable then return end
        for _, drawing in pairs(drawingTable) do
            if type(drawing) == "table" then
                for _, line in ipairs(drawing) do
                    if line and line.Remove then pcall(line.Remove, line) end
                end
            else
                if drawing and drawing.Remove then pcall(drawing.Remove, drawing) end
            end
        end
    end
    
    local function createESPObject()
        return {
            box = Drawing.new("Square"),
            tracer = Drawing.new("Line"),
            name = Drawing.new("Text"),
            distance = Drawing.new("Text"),
            boxLines = {}
        }
    end
    
    local function setupESPObject(esp)
        esp.box.Thickness = 2
        esp.box.Filled = false
        esp.box.Visible = false
        
        esp.tracer.Thickness = 1
        esp.tracer.Visible = false
        
        esp.name.Size = 14
        esp.name.Center = true
        esp.name.Outline = true
        esp.name.Visible = false
        
        esp.distance.Size = 14
        esp.distance.Center = true
        esp.distance.Outline = true
        esp.distance.Visible = false
    end
    
    local function draw3DBox(esp, cf, pos, cam, boxColor, boxSize)
        if not cf or not cam then return end
        
        boxSize = boxSize or Vector3.new(4, 5, 3)
        local size = boxSize
        local offsets = {
            Vector3.new( size.X/2,  size.Y/2,  size.Z/2), Vector3.new( size.X/2,  size.Y/2, -size.Z/2),
            Vector3.new( size.X/2, -size.Y/2,  size.Z/2), Vector3.new( size.X/2, -size.Y/2, -size.Z/2),
            Vector3.new(-size.X/2,  size.Y/2,  size.Z/2), Vector3.new(-size.X/2,  size.Y/2, -size.Z/2),
            Vector3.new(-size.X/2, -size.Y/2,  size.Z/2), Vector3.new(-size.X/2, -size.Y/2, -size.Z/2),
        }
        
        local screenPoints = {}
        local anyPointOnScreen = false
        
        for i, offset in ipairs(offsets) do
            local success, vec, onScreen = pcall(function()
                local worldPos = cf * CFrame.Angles(0, math.rad(90), 0) * offset
                return cam:WorldToViewportPoint(worldPos)
            end)
            if success then
                screenPoints[i] = {pos = Vector2.new(vec.X, vec.Y), depth = vec.Z, onScreen = onScreen}
                if onScreen and vec.Z > 0 then anyPointOnScreen = true end
            end
        end
        
        if not esp.boxLines or #esp.boxLines == 0 then
            esp.boxLines = {}
            for i = 1, 12 do
                local line = Drawing.new("Line")
                line.Thickness = 1
                line.ZIndex = 2
                table.insert(esp.boxLines, line)
            end
        end
        
        local edges = {
            {1, 2}, {1, 3}, {1, 5}, {2, 4}, {2, 6},
            {3, 4}, {3, 7}, {5, 6}, {5, 7}, {4, 8}, {6, 8}, {7, 8}
        }
        
        local dist = getDistanceFromPlayer(pos) or 10
        local thickness = math.clamp(3 / (dist / 50), 1, 3)
        
        for i, edge in ipairs(edges) do
            local line = esp.boxLines[i]
            if line then
                local p1, p2 = screenPoints[edge[1]], screenPoints[edge[2]]
                line.Color = boxColor or Color3.fromRGB(255, 255, 255)
                line.Thickness = thickness
                line.Transparency = 1
                if anyPointOnScreen and p1 and p2 and p1.depth > 0 and p2.depth > 0 then
                    line.From = p1.pos
                    line.To = p2.pos
                    line.Visible = true
                else
                    line.Visible = false
                end
            end
        end
    end
    
    local function updatePlayerRole(playerObj)
        local hasKnife = playerObj.Character and (playerObj.Character:FindFirstChild("Knife") or (playerObj.Backpack and playerObj.Backpack:FindFirstChild("Knife")))
        local hasGun = playerObj.Character and (playerObj.Character:FindFirstChild("Gun") or (playerObj.Backpack and playerObj.Backpack:FindFirstChild("Gun")))
        
        if hasKnife then
            playerRoles[playerObj] = "Murderer"
        elseif hasGun then
            playerRoles[playerObj] = "Sheriff"
        else
            playerRoles[playerObj] = "Innocent"
        end
    end
    
    local function updateRoleHighlights()
        for _, otherPlayer in ipairs(Players:GetPlayers()) do
            if otherPlayer ~= player and otherPlayer.Character then
                local model = otherPlayer.Character
                local highlight = model:FindFirstChild("PlayerHighlight")
                local role = playerRoles[otherPlayer]
                local isAlivePlayer = IsAlive(otherPlayer)
                
                local highlightEnabled = false
                if not role then highlightEnabled = _G.InnocentHighlights
                elseif role == "Murderer" then highlightEnabled = _G.MurderHighlights
                elseif role == "Sheriff" then highlightEnabled = _G.SheriffHighlights
                else highlightEnabled = _G.InnocentHighlights end
                
                if highlightEnabled then
                    local color = Color3.new(1,1,1)
                    if isAlivePlayer then
                        if role == "Murderer" then color = Color3.fromRGB(225, 0, 0)
                        elseif role == "Sheriff" then color = Color3.fromRGB(0, 0, 225)
                        else color = Color3.fromRGB(0, 225, 0) end
                    end
                    
                    if not highlight then
                        highlight = Instance.new("Highlight")
                        highlight.Name = "PlayerHighlight"
                        highlight.Adornee = model
                        highlight.FillTransparency = 0.5
                        highlight.OutlineTransparency = 0
                        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
                        highlight.Parent = model
                        playerHighlights[otherPlayer] = highlight
                    end
                    highlight.FillColor = color
                    highlight.OutlineColor = getOutlineColor(color)
                else
                    if highlight then highlight:Destroy() end
                    playerHighlights[otherPlayer] = nil
                end
            end
        end
        
        if _G.CoinHighlights then
            for _, coin in pairs(findCoins()) do
                if coin and coin.Parent then
                    local h = coin:FindFirstChild("ItemHighlight")
                    if not h then
                        h = Instance.new("Highlight")
                        h.Name = "ItemHighlight"
                        h.Adornee = coin
                        h.FillTransparency = 0.5
                        h.Parent = coin
                    end
                    h.FillColor = Color3.fromRGB(255, 215, 0)
                    h.OutlineColor = Color3.new(0,0,0)
                    table.insert(coinHighlights, h)
                end
            end
        end
        
        if _G.GunHighlights then
            local gun = findGun()
            if gun and gun.Parent then
                local h = gun:FindFirstChild("ItemHighlight")
                if not h then
                    h = Instance.new("Highlight")
                    h.Name = "ItemHighlight"
                    h.Adornee = gun
                    h.FillTransparency = 0.5
                    h.Parent = gun
                end
                h.FillColor = Color3.fromRGB(255, 0, 255)
                h.OutlineColor = Color3.new(1,1,1)
                table.insert(gunHighlights, h)
            end
        end
    end
    
    local function clearAllHighlights()
        for _, highlight in pairs(playerHighlights) do
            if highlight then highlight:Destroy() end
        end
        playerHighlights = {}
        for _, h in pairs(coinHighlights) do if h then h:Destroy() end end
        coinHighlights = {}
        for _, h in pairs(gunHighlights) do if h then h:Destroy() end end
        gunHighlights = {}
    end
    
    local function updateRoleESP()
        local cam = Workspace.CurrentCamera
        if not cam then return end
        local screenBottomCenter = Vector2.new(cam.ViewportSize.X / 2, cam.ViewportSize.Y)
        
        local currentTargets = {Innocent={}, Murder={}, Sheriff={}}
        
        for _, otherPlayer in ipairs(Players:GetPlayers()) do
            if otherPlayer ~= player and otherPlayer.Character then
                local hrp = otherPlayer.Character:FindFirstChild("HumanoidRootPart")
                local hum = otherPlayer.Character:FindFirstChild("Humanoid")
                
                if hrp and hum then
                    updatePlayerRole(otherPlayer)
                    local role = playerRoles[otherPlayer]
                    local isDead = hum.Health <= 0
                    
                    local config, elements, targetTable, color
                    
                    if isDead then
                        config = _G.InnocentESP
                        elements = innocentEspElements
                        targetTable = currentTargets.Innocent
                        color = Color3.fromRGB(200, 200, 200)
                    elseif role == "Murderer" then
                        config = _G.MurderESP
                        elements = murderEspElements
                        targetTable = currentTargets.Murder
                        color = Color3.fromRGB(255, 0, 0)
                    elseif role == "Sheriff" then
                        config = _G.SheriffESP
                        elements = sheriffEspElements
                        targetTable = currentTargets.Sheriff
                        color = Color3.fromRGB(0, 0, 255)
                    else
                        config = _G.InnocentESP
                        elements = innocentEspElements
                        targetTable = currentTargets.Innocent
                        color = Color3.fromRGB(0, 255, 0)
                    end
                    
                    if config.names or config.boxes or config.tracers or config.distance then
                        targetTable[otherPlayer.Character] = true
                        
                        if not elements[otherPlayer.Character] then
                            elements[otherPlayer.Character] = createESPObject()
                            setupESPObject(elements[otherPlayer.Character])
                        end
                        
                        local esp = elements[otherPlayer.Character]
                        local vec, onScreen = cam:WorldToViewportPoint(hrp.Position)
                        
                        if onScreen then
                            local topY = cam:WorldToViewportPoint(hrp.Position + Vector3.new(0, 3, 0)).Y
                            local bottomY = cam:WorldToViewportPoint(hrp.Position - Vector3.new(0, 3, 0)).Y
                            local sizeY = math.abs(bottomY - topY)
                            local sizeX = sizeY / 2
                            
                            if config.boxes then
                                if config.boxType == "2D" then
                                    esp.box.Visible = true
                                    esp.box.Size = Vector2.new(sizeX, sizeY)
                                    esp.box.Position = Vector2.new(vec.X - sizeX/2, vec.Y - sizeY/2)
                                    esp.box.Color = color
                                    for _, l in ipairs(esp.boxLines) do l.Visible = false end
                                else
                                    esp.box.Visible = false
                                    local boxSize = Vector3.new(2, 5, 2)
                                    draw3DBox(esp, hrp.CFrame, hrp.Position, cam, color, boxSize)
                                end
                            else
                                esp.box.Visible = false
                                for _, l in ipairs(esp.boxLines) do l.Visible = false end
                            end
                            
                            if config.tracers then
                                esp.tracer.Visible = true
                                esp.tracer.From = screenBottomCenter
                                esp.tracer.To = Vector2.new(vec.X, vec.Y)
                                esp.tracer.Color = color
                            else
                                esp.tracer.Visible = false
                            end
                            
                            if config.names then
                                esp.name.Visible = true
                                esp.name.Text = otherPlayer.Name
                                esp.name.Position = Vector2.new(vec.X, vec.Y - sizeY/2 - 15)
                                esp.name.Color = color
                            else
                                esp.name.Visible = false
                            end
                            
                            if config.distance then
                                esp.distance.Visible = true
                                esp.distance.Text = math.floor((hrp.Position - cam.CFrame.Position).Magnitude) .. "m"
                                esp.distance.Position = Vector2.new(vec.X, vec.Y + sizeY/2 + 5)
                                esp.distance.Color = color
                            else
                                esp.distance.Visible = false
                            end
                        else
                            esp.box.Visible = false
                            esp.tracer.Visible = false
                            esp.name.Visible = false
                            esp.distance.Visible = false
                            for _, l in ipairs(esp.boxLines) do l.Visible = false end
                        end
                    end
                end
            end
        end
        
        for target, esp in pairs(innocentEspElements) do
            if not currentTargets.Innocent[target] then
                cleanupDrawingTable(esp)
                innocentEspElements[target] = nil
            end
        end
        for target, esp in pairs(murderEspElements) do
            if not currentTargets.Murder[target] then
                cleanupDrawingTable(esp)
                murderEspElements[target] = nil
            end
        end
        for target, esp in pairs(sheriffEspElements) do
            if not currentTargets.Sheriff[target] then
                cleanupDrawingTable(esp)
                sheriffEspElements[target] = nil
            end
        end
    end
    
    local function updateItemESP(config, elementTable, itemList, itemName, color)
        local cam = Workspace.CurrentCamera
        if not cam then return end
        local screenBot = Vector2.new(cam.ViewportSize.X/2, cam.ViewportSize.Y)
        local currentItems = {}
        
        for _, item in ipairs(itemList) do
            if item and item.Parent then
                currentItems[item] = true
                if not elementTable[item] then
                    elementTable[item] = createESPObject()
                    setupESPObject(elementTable[item])
                end
                local esp = elementTable[item]
                local vec, onScreen = cam:WorldToViewportPoint(item.Position)
                
                if onScreen and (config.names or config.boxes or config.tracers or config.distance) then
                    local size = 20
                    if config.boxes then
                        if config.boxType == "2D" then
                            esp.box.Visible = true
                            esp.box.Size = Vector2.new(size, size)
                            esp.box.Position = Vector2.new(vec.X - size/2, vec.Y - size/2)
                            esp.box.Color = color
                            for _, l in ipairs(esp.boxLines) do l.Visible = false end
                        else
                            esp.box.Visible = false
                            draw3DBox(esp, item.CFrame, item.Position, cam, color, Vector3.new(2,2,2))
                        end
                    else
                        esp.box.Visible = false
                        for _, l in ipairs(esp.boxLines) do l.Visible = false end
                    end
                    
                    if config.tracers then
                        esp.tracer.Visible = true
                        esp.tracer.From = screenBot
                        esp.tracer.To = Vector2.new(vec.X, vec.Y)
                        esp.tracer.Color = color
                    else
                        esp.tracer.Visible = false
                    end
                    
                    if config.names then
                        esp.name.Visible = true
                        esp.name.Text = itemName
                        esp.name.Position = Vector2.new(vec.X, vec.Y - size/2 - 15)
                        esp.name.Color = color
                    else
                        esp.name.Visible = false
                    end
                    
                    if config.distance then
                        esp.distance.Visible = true
                        esp.distance.Text = math.floor((item.Position - cam.CFrame.Position).Magnitude) .. "m"
                        esp.distance.Position = Vector2.new(vec.X, vec.Y + size/2 + 5)
                        esp.distance.Color = color
                    else
                        esp.distance.Visible = false
                    end
                else
                    esp.box.Visible = false
                    esp.tracer.Visible = false
                    esp.name.Visible = false
                    esp.distance.Visible = false
                    for _, l in ipairs(esp.boxLines) do l.Visible = false end
                end
            end
        end
        
        for item, esp in pairs(elementTable) do
            if not currentItems[item] then
                cleanupDrawingTable(esp)
                elementTable[item] = nil
            end
        end
    end
    
    local function manageESPConnections()
        local anyESPActive = _G.InnocentESP.names or _G.InnocentESP.boxes or _G.InnocentESP.tracers or _G.InnocentESP.distance or
                            _G.MurderESP.names or _G.MurderESP.boxes or _G.MurderESP.tracers or _G.MurderESP.distance or
                            _G.SheriffESP.names or _G.SheriffESP.boxes or _G.SheriffESP.tracers or _G.SheriffESP.distance
        
        if anyESPActive then
            if not playerEspConnection then
                playerEspConnection = RunService.RenderStepped:Connect(updateRoleESP)
            end
        else
            if playerEspConnection then
                playerEspConnection:Disconnect()
                playerEspConnection = nil
                for _, tbl in pairs({innocentEspElements, murderEspElements, sheriffEspElements}) do
                    for _, esp in pairs(tbl) do cleanupDrawingTable(esp) end
                end
                innocentEspElements = {}
                murderEspElements = {}
                sheriffEspElements = {}
            end
        end
        
        local anyCoinESP = _G.CoinESPConfig.names or _G.CoinESPConfig.boxes or _G.CoinESPConfig.tracers or _G.CoinESPConfig.distance
        if anyCoinESP then
            if not coinEspConnection then
                coinEspConnection = RunService.RenderStepped:Connect(function()
                    updateItemESP(_G.CoinESPConfig, coinEspElements, findCoins(), "Coin", Color3.fromRGB(255, 215, 0))
                end)
            end
        else
            if coinEspConnection then
                coinEspConnection:Disconnect()
                coinEspConnection = nil
                for _, esp in pairs(coinEspElements) do cleanupDrawingTable(esp) end
                coinEspElements = {}
            end
        end
        
        local anyGunESP = _G.GunESP.names or _G.GunESP.boxes or _G.GunESP.tracers or _G.GunESP.distance
        if anyGunESP then
            if not gunEspConnection then
                gunEspConnection = RunService.RenderStepped:Connect(function()
                    local gun = findGun()
                    if gun then
                        updateItemESP(_G.GunESP, gunEspElements, {gun}, "Gun", Color3.fromRGB(255, 0, 255))
                    end
                end)
            end
        else
            if gunEspConnection then
                gunEspConnection:Disconnect()
                gunEspConnection = nil
                for _, esp in pairs(gunEspElements) do cleanupDrawingTable(esp) end
                gunEspElements = {}
            end
        end
        
        local anyHighlights = _G.InnocentHighlights or _G.MurderHighlights or _G.SheriffHighlights or _G.CoinHighlights or _G.GunHighlights
        if anyHighlights then
            if not HighlightsConnection then
                HighlightsConnection = RunService.Heartbeat:Connect(updateRoleHighlights)
            end
        else
            if HighlightsConnection then
                HighlightsConnection:Disconnect()
                HighlightsConnection = nil
                clearAllHighlights()
            end
        end
    end
    
    -- =============================================
    -- UNLOCK EMOTES from Rise
    -- =============================================
    local function unlockAllEmotes()
        local PlayerGui = player:WaitForChild("PlayerGui")
        local PlayEmote = ReplicatedStorage:FindFirstChild("Remotes") 
            and ReplicatedStorage.Remotes:FindFirstChild("Misc") 
            and ReplicatedStorage.Remotes.Misc:FindFirstChild("PlayEmote")
        
        if not PlayEmote then return end
        
        local ItemServiceModule = ReplicatedStorage:FindFirstChild("ClientServices") 
            and ReplicatedStorage.ClientServices:FindFirstChild("ItemService")
        
        if not ItemServiceModule then return end
        local ItemService = require(ItemServiceModule)
        
        local function getAllEmotes()
            local emotes = {}
            local added = {}
            
            local success, syncModule = pcall(require, ReplicatedStorage.Database.Sync)
            if success and syncModule then
                if syncModule.Emotes then
                    for emoteName, _ in pairs(syncModule.Emotes) do
                        if not added[emoteName] then
                            table.insert(emotes, emoteName)
                            added[emoteName] = true
                        end
                    end
                end
                if syncModule.Toys then
                    for toyName, _ in pairs(syncModule.Toys) do
                        if not added[toyName] then
                            table.insert(emotes, toyName)
                            added[toyName] = true
                        end
                    end
                end
            end
            
            local defaultEmotes = {"wave", "cheer", "laugh", "dance1", "dance2", "dance3"}
            for _, emote in ipairs(defaultEmotes) do
                if not added[emote] then
                    table.insert(emotes, emote)
                    added[emote] = true
                end
            end
            return emotes
        end
        
        local AllEmotes = getAllEmotes()
        
        local function runUnlocker()
            local Cross = PlayerGui:FindFirstChild("CrossPlatform")
            if not Cross then return end
            local Emotes = Cross:FindFirstChild("Emotes")
            if not Emotes then return end
            local Controller = Emotes:FindFirstChild("EmoteController")
            local Window = Emotes:FindFirstChild("EmoteWindow")
            if not Controller or not Window then return end
            
            local GameEmotes = Window.EmoteContainer.EmotePages:FindFirstChild("Game Emotes")
            local FrameTemplate = Controller:FindFirstChild("EmoteFrame")
            
            if not GameEmotes or not FrameTemplate then return end
            
            for _, v in pairs(GameEmotes:GetChildren()) do
                if v.Name:match("^Row") or v.Name:match("^Emote") then
                    v:Destroy()
                end
            end
            
            if not GameEmotes:IsA("ScrollingFrame") then
                local scrollingFrame = Instance.new("ScrollingFrame")
                scrollingFrame.Name = "EmoteScroller"
                scrollingFrame.Size = UDim2.new(1, 0, 1, 0)
                scrollingFrame.Position = UDim2.new(0, 0, 0, 0)
                scrollingFrame.BackgroundTransparency = 1
                scrollingFrame.BorderSizePixel = 0
                scrollingFrame.ScrollBarThickness = 8
                scrollingFrame.Parent = GameEmotes
                
                local uiGridLayout = Instance.new("UIGridLayout")
                uiGridLayout.CellSize = UDim2.new(0, 80, 0, 80)
                uiGridLayout.CellPadding = UDim2.new(0, 5, 0, 5)
                uiGridLayout.FillDirection = Enum.FillDirection.Horizontal
                uiGridLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
                uiGridLayout.SortOrder = Enum.SortOrder.LayoutOrder
                uiGridLayout.Parent = scrollingFrame
                
                GameEmotes = scrollingFrame
            end
            
            for index, emote in ipairs(AllEmotes) do
                local info = ItemService:GetItemInfo(emote, "Emotes")
                if not info then
                    info = ItemService:GetItemInfo(emote, "Toys")
                end
                
                if info then
                    local frame = FrameTemplate:Clone()
                    frame.Name = "Emote"..index
                    frame:SetAttribute("EmoteName", emote)
                    if frame:FindFirstChild("EmoteName") then frame.EmoteName.Text = info.Name or emote end
                    if frame:FindFirstChild("EmoteIcon") then frame.EmoteIcon.Image = ItemService:GetItemImage(info) end
                    
                    if frame:FindFirstChild("Hotkey") and frame.Hotkey:FindFirstChild("KeyLabel") then
                        frame.Hotkey.KeyLabel.Text = index
                        frame.Hotkey.Visible = UserInputService.KeyboardEnabled
                    end
                    
                    frame.LayoutOrder = index
                    frame.Parent = GameEmotes
                    
                    if frame:FindFirstChild("PlayButton") then
                        frame.PlayButton.Activated:Connect(function()
                            PlayEmote:Fire(emote)
                        end)
                    end
                end
            end
            
            local emotesCount = #AllEmotes
            local rows = math.ceil(emotesCount / 4)
            if GameEmotes:IsA("ScrollingFrame") then
                GameEmotes.CanvasSize = UDim2.new(0, 0, 0, rows * 90)
            end
        end
        
        if PlayerGui:FindFirstChild("CrossPlatform") then
            runUnlocker()
        else
            PlayerGui:WaitForChild("CrossPlatform", 10)
            task.wait(1)
            runUnlocker()
        end
        
        local inventoryRemote = ReplicatedStorage.Remotes:FindFirstChild("Inventory") 
            and ReplicatedStorage.Remotes.Inventory:FindFirstChild("InventoryDataChanged")
        
        if inventoryRemote then
            inventoryRemote.Event:Connect(function(cat)
                if cat == "Emotes" or cat == "Toys" then
                    task.wait(0.3)
                    runUnlocker()
                end
            end)
        end
        
        WindUI:Notify({
            Title = T("notification"),
            Content = T("unlockEmotes"),
            Duration = 3
        })
    end
    
    -- =============================================
    -- الدوال الأساسية (حركات، تصويب، الخ)
    -- =============================================
    local function setupXray()
        if _G.XrayEnabled then
            for _, part in pairs(workspace:GetDescendants()) do
                if part:IsA("BasePart") and not part:IsA("MeshPart") then
                    part.LocalTransparencyModifier = 0.5
                end
            end
        else
            for _, part in pairs(workspace:GetDescendants()) do
                if part:IsA("BasePart") and not part:IsA("MeshPart") then
                    part.LocalTransparencyModifier = 0
                end
            end
        end
    end
    
    local function setupInfiniteJump()
        if _G.InfiniteJumpEnabled then
            if espConnections.infiniteJump then
                espConnections.infiniteJump:Disconnect()
            end
            espConnections.infiniteJump = UserInputService.JumpRequest:Connect(function()
                if player.Character and player.Character:FindFirstChild("Humanoid") then
                    player.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
                end
            end)
        elseif espConnections.infiniteJump then
            espConnections.infiniteJump:Disconnect()
            espConnections.infiniteJump = nil
        end
    end
    
    local function setupNoclip()
        if _G.NoclipEnabled then
            if espConnections.noclip then
                espConnections.noclip:Disconnect()
            end
            espConnections.noclip = RunService.Stepped:Connect(function()
                if player.Character then
                    for _, part in pairs(player.Character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
            end)
        elseif espConnections.noclip then
            espConnections.noclip:Disconnect()
            espConnections.noclip = nil
        end
    end
    
    local function setupFly()
        local Camera = workspace.CurrentCamera
        
        if _G.FlyEnabled then
            if BodyVelocity then BodyVelocity:Destroy() end
            if BodyGyro then BodyGyro:Destroy() end
            
            local Character = player.Character
            if not Character then return end
            
            local RootPart = Character:FindFirstChild("HumanoidRootPart")
            local Humanoid = Character:FindFirstChild("Humanoid")
            
            if not RootPart or not Humanoid then return end
            
            BodyVelocity = Instance.new("BodyVelocity")
            BodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
            BodyVelocity.Velocity = Vector3.new(0, 0.1, 0)
            BodyVelocity.Parent = RootPart
            
            BodyGyro = Instance.new("BodyGyro")
            BodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
            BodyGyro.P = 10000
            BodyGyro.D = 500
            BodyGyro.CFrame = RootPart.CFrame
            BodyGyro.Parent = RootPart
            
            Humanoid.PlatformStand = true
            
            if espConnections.fly then
                espConnections.fly:Disconnect()
            end
            
            espConnections.fly = RunService.RenderStepped:Connect(function()
                if not _G.FlyEnabled or not RootPart or not RootPart.Parent then return end
                
                local moveDirection = Humanoid.MoveDirection
                local targetVelocity = Vector3.new(0, 0.1, 0)
                
                if moveDirection.Magnitude > 0 then
                    local cameraCFrame = Camera.CFrame
                    targetVelocity = moveDirection * _G.FlySpeedValue
                    
                    if math.abs(cameraCFrame.LookVector.Y) > 0.1 then
                        targetVelocity = Vector3.new(targetVelocity.X, cameraCFrame.LookVector.Y * _G.FlySpeedValue, targetVelocity.Z)
                    end
                end
                
                BodyVelocity.Velocity = BodyVelocity.Velocity:Lerp(targetVelocity, 0.15)
                BodyGyro.CFrame = BodyGyro.CFrame:Lerp(Camera.CFrame, 0.15)
            end)
        else
            if espConnections.fly then
                espConnections.fly:Disconnect()
                espConnections.fly = nil
            end
            if BodyVelocity then BodyVelocity:Destroy() end
            if BodyGyro then BodyGyro:Destroy() end
            BodyVelocity, BodyGyro = nil, nil
            local Character = player.Character
            if Character then
                local Humanoid = Character:FindFirstChild("Humanoid")
                if Humanoid then Humanoid.PlatformStand = false end
            end
        end
    end
    
    local function setupLockCamera()
        if _G.LockCameraEnabled then
            if espConnections.lockCamera then
                espConnections.lockCamera:Disconnect()
            end
            
            espConnections.lockCamera = RunService.RenderStepped:Connect(function()
                if not _G.LockCameraEnabled then
                    if espConnections.lockCamera then
                        espConnections.lockCamera:Disconnect()
                        espConnections.lockCamera = nil
                    end
                    return
                end
                
                local target = findMurderer()
                if not target then
                    target = findSheriff()
                end
                
                if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                    if player.Character and player.Character:FindFirstChild("Head") then
                        workspace.CurrentCamera.CFrame = CFrame.new(
                            workspace.CurrentCamera.CFrame.Position,
                            target.Character.HumanoidRootPart.Position
                        )
                    end
                end
            end)
        end
    end
    
    local function setupAimbot()
        if not _G.AimbotEnabled then
            if espConnections.aimbot then
                espConnections.aimbot:Disconnect()
                espConnections.aimbot = nil
            end
            return
        end
        
        if espConnections.aimbot then
            espConnections.aimbot:Disconnect()
        end
        
        espConnections.aimbot = RunService.Heartbeat:Connect(function()
            local target = findSheriff()
            
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = CFrame.new(
                        player.Character.HumanoidRootPart.Position,
                        target.Character.HumanoidRootPart.Position
                    )
                end
            end
        end)
    end
    
    local function setupAimbotMurderer()
        if not _G.AimbotMurdererEnabled then
            if espConnections.aimbotMurderer then
                espConnections.aimbotMurderer:Disconnect()
                espConnections.aimbotMurderer = nil
            end
            return
        end
        
        if espConnections.aimbotMurderer then
            espConnections.aimbotMurderer:Disconnect()
        end
        
        espConnections.aimbotMurderer = RunService.Heartbeat:Connect(function()
            local target = findMurderer()
            
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = CFrame.new(
                        player.Character.HumanoidRootPart.Position,
                        target.Character.HumanoidRootPart.Position
                    )
                end
            end
        end)
    end
    
    local function setupKillAll()
        _G.KillAllEnabled = true
        frozenPlayers = {}
        
        if espConnections.killAll then
            espConnections.killAll:Disconnect()
        end
        
        espConnections.killAll = RunService.RenderStepped:Connect(function()
            if not _G.KillAllEnabled then return end
            
            local myChar = player.Character
            if not myChar then return end
            
            local myRoot = myChar:FindFirstChild("HumanoidRootPart") or myChar:FindFirstChild("Torso")
            if not myRoot then return end
            
            for _, targetPlayer in pairs(Players:GetPlayers()) do
                if targetPlayer ~= player and not frozenPlayers[targetPlayer] and targetPlayer.Character then
                    local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart") or targetPlayer.Character:FindFirstChild("Torso")
                    if targetRoot then
                        targetRoot.CFrame = myRoot.CFrame * CFrame.new(0, 0, -7)
                        targetRoot.Anchored = true
                        frozenPlayers[targetPlayer] = true
                    end
                end
            end
        end)
    end
    
    local function giveTeleportTool()
        local toolName = "Teleport Tool"
        
        local existingTool = player.Backpack:FindFirstChild(toolName) or 
                            (player.Character and player.Character:FindFirstChild(toolName))
        
        if existingTool then
            existingTool:Destroy()
        end
        
        local TpTool = Instance.new("Tool")
        TpTool.Name = toolName
        TpTool.RequiresHandle = false
        TpTool.CanBeDropped = false
        TpTool.Parent = player.Backpack
        TpTool.ToolTip = _G.SelectedLanguage == "Saudi" and "اضغط للتليبرت" or "Click to teleport"
        
        TpTool.Activated:Connect(function()
            local character = player.Character
            local hrp = character and (character:FindFirstChild("HumanoidRootPart") or 
                                       character:FindFirstChild("Torso") or 
                                       character:FindFirstChild("UpperTorso"))
            
            if not character or not hrp then return end
            
            local mouse = player:GetMouse()
            local targetPos = mouse.Hit
            
            if targetPos then
                hrp.CFrame = CFrame.new(
                    targetPos.X,
                    targetPos.Y + 3,
                    targetPos.Z,
                    select(4, hrp.CFrame:components())
                )
            end
        end)
    end
    
    local function setupBoostFPS()
        if _G.BoostFPSEnabled then
            settings().Rendering.QualityLevel = 1
            for _, v in pairs(game:GetDescendants()) do
                if v:IsA("Part") or v:IsA("UnionOperation") or v:IsA("MeshPart") then
                    v.Material = Enum.Material.Plastic
                    v.Reflectance = 0
                elseif v:IsA("Decal") then
                    v.Transparency = 1
                elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                    v.Enabled = false
                end
            end
        else
            settings().Rendering.QualityLevel = 21
            for _, v in pairs(game:GetDescendants()) do
                if v:IsA("Decal") then
                    v.Transparency = 0
                elseif v:IsA("ParticleEmitter") or v:IsA("Trail") then
                    v.Enabled = true
                end
            end
        end
    end
    
    local function setupFullBright()
        if _G.FullBrightEnabled then
            game.Lighting.GlobalShadows = false
            game.Lighting.FogEnd = 9e9
            game.Lighting.Brightness = 2
        else
            game.Lighting.GlobalShadows = true
            game.Lighting.FogEnd = 1000
            game.Lighting.Brightness = 1
        end
    end
    
    local function setupAntiAFK()
        if _G.AntiAFKEnabled then
            if espConnections.antiAFK then
                espConnections.antiAFK:Disconnect()
            end
            
            espConnections.antiAFK = RunService.Heartbeat:Connect(function()
                game:GetService("VirtualInputManager"):SendMouseMoveEvent(100, 100, workspace)
            end)
        elseif espConnections.antiAFK then
            espConnections.antiAFK:Disconnect()
            espConnections.antiAFK = nil
        end
    end
    
    local function teleportToRandom()
        local allPlayers = Players:GetPlayers()
        if #allPlayers > 1 then
            local randomPlayer
            repeat
                randomPlayer = allPlayers[math.random(1, #allPlayers)]
            until randomPlayer ~= player
            
            if randomPlayer and randomPlayer.Character and randomPlayer.Character:FindFirstChild("HumanoidRootPart") then
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = randomPlayer.Character.HumanoidRootPart.CFrame
                end
            end
        end
    end
    
    local function teleportToInnocent()
        local murderer = findMurderer()
        local sheriff = findSheriff()
        local innocents = {}
        
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= player and plr ~= murderer and plr ~= sheriff and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                local hasKnife = plr.Backpack:FindFirstChild("Knife") or (plr.Character and plr.Character:FindFirstChild("Knife"))
                local hasGun = plr.Backpack:FindFirstChild("Gun") or (plr.Character and plr.Character:FindFirstChild("Gun"))
                if not hasKnife and not hasGun then
                    table.insert(innocents, plr)
                end
            end
        end
        
        if #innocents > 0 then
            local randomInnocent = innocents[math.random(1, #innocents)]
            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                player.Character.HumanoidRootPart.CFrame = randomInnocent.Character.HumanoidRootPart.CFrame
            end
        end
    end
    
    local function teleportToCoinTP()
        local coin = findNearestCoin()
        if coin and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            player.Character.HumanoidRootPart.CFrame = CFrame.new(coin.Position + Vector3.new(0, 3, 0))
        end
    end
    
    local function teleportToMap()
        local char = player.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if not root then return end
        
        local spawnParts = {}
        for _, obj in pairs(workspace:GetDescendants()) do
            if obj:IsA("BasePart") and obj.Name == "Spawn" then
                local isInLobby = false
                local parent = obj.Parent
                while parent ~= nil do
                    if parent.Name == "Lobby" and parent.Parent == workspace then isInLobby = true break end
                    parent = parent.Parent
                end
                if not isInLobby then table.insert(spawnParts, obj) end
            end
        end
        
        if #spawnParts > 0 then
            local randomSpawn = spawnParts[math.random(1, #spawnParts)]
            root.CFrame = randomSpawn.CFrame + Vector3.new(0, 5, 0)
        end
    end
    
    -- =============================================
    -- الدالة الرئيسية لإنشاء الواجهة
    -- =============================================
    local function createMainScript()
        cleanupGUI()
        
        local success, newWindUI = pcall(function()
            local source = game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua", true)
            return loadstring(source)()
        end)
        
        if not success then return end
        
        WindUI = newWindUI
        WindUI.TransparencyValue = 0.7
        WindUI:SetTheme("Dark")
        
        -- Popup التحذير
        WindUI:Popup({
            Title = "PhantomXPro",
            Icon = "crown",
            Content = T("warning"),
            Buttons = {
                {
                    Title = _G.SelectedLanguage == "Saudi" and "موافق" or "OK",
                    Icon = "arrow-right",
                    Variant = "Primary",
                    Callback = function() end
                }
            }
        })
        
        task.wait(2)
        WindUI:Popup({
            Title = T("discord"),
            Icon = "message-circle",
            Content = T("discordContent"),
            Buttons = {
                {
                    Title = T("copyLink"),
                    Icon = "copy",
                    Variant = "Primary",
                    Callback = function()
                        setclipboard("https://discord.gg/CgUa36sPNs")
                    end
                }
            }
        })
        
        local Window = WindUI:CreateWindow({
            Title = "PhantomXPro",
            Icon = "crown", 
            Author = "By_Cypher",
            Folder = "PhantomXPro",
            Size = UDim2.fromOffset(900, 700),
            Theme = "Dark",
            SideBarWidth = 200,
            ScrollBarEnabled = true
        })
        
        -- =============================================
        -- 👤 INFO TAB
        -- =============================================
        local infoTab = Window:Tab({
            Title = T("infoTab"),
            Icon = "user"
        })
        
        infoTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "معلوماتك" or "Your Info",
            Desc = "",
            Image = "user",
            ImageSize = 24
        })
        
        local function getPlayerStats()
            local level = player:GetAttribute("Level") or "0"
            local xp = player:GetAttribute("XP") or "0"
            return tostring(level), tostring(xp)
        end
        
        local nameBtn = infoTab:Button({
            Title = T("yourName") .. string.sub(player.DisplayName, 1, 12),
            Size = "medium",
            Callback = function() end
        })
        
        local userBtn = infoTab:Button({
            Title = T("yourUsername") .. string.sub(player.Name, 1, 10),
            Size = "medium",
            Callback = function() end
        })
        
        local ageBtn = infoTab:Button({
            Title = T("accountAge") .. player.AccountAge .. (_G.SelectedLanguage == "Saudi" and " يوم" or " days"),
            Size = "medium",
            Callback = function() end
        })
        
        local hackBtn = infoTab:Button({
            Title = T("hack") .. (identifyexecutor and identifyexecutor() or "Synapse X"),
            Size = "medium",
            Callback = function() end
        })
        
        infoTab:Divider({ Title = "" })
        
        local levelBtn = infoTab:Button({
            Title = T("level") .. "0",
            Size = "medium",
            Callback = function() end
        })
        
        local xpBtn = infoTab:Button({
            Title = T("xp") .. "0",
            Size = "medium",
            Callback = function() end
        })
        
        infoTab:Divider({ Title = "" })
        
        local playersBtn = infoTab:Button({
            Title = T("players") .. "0",
            Size = "medium",
            Callback = function() end
        })
        
        local fpsBtn = infoTab:Button({
            Title = T("fps") .. "0",
            Size = "medium",
            Callback = function() end
        })
        
        local pingBtn = infoTab:Button({
            Title = T("ping") .. "0 ms",
            Size = "medium",
            Callback = function() end
        })
        
        spawn(function()
            local StatsService = game:GetService("Stats")
            local frameCount = 0
            local lastTime = tick()
            
            RunService.RenderStepped:Connect(function()
                frameCount = frameCount + 1
                local currentTime = tick()
                if currentTime - lastTime >= 1 then
                    local fps = math.floor(frameCount / (currentTime - lastTime))
                    frameCount = 0
                    lastTime = currentTime
                    fpsBtn:SetTitle(T("fps") .. fps)
                end
            end)
            
            while true do
                task.wait(1.5)
                local level, xp = getPlayerStats()
                levelBtn:SetTitle(T("level") .. level)
                xpBtn:SetTitle(T("xp") .. xp)
                playersBtn:SetTitle(T("players") .. #Players:GetPlayers())
                pcall(function()
                    local ping = StatsService.Network.ServerStatsItem["Data Ping"]:GetValue()
                    pingBtn:SetTitle(T("ping") .. math.floor(ping) .. " ms")
                end)
            end
        end)
        
        -- =============================================
        -- 🏠 MAIN TAB
        -- =============================================
        local mainTab = Window:Tab({
            Title = T("mainTab"),
            Icon = "zap"
        })
        
        mainTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "المميزات الرئيسية" or "Main Features",
            Desc = "",
            Image = "zap",
            ImageSize = 24
        })
        
        mainTab:Button({
            Title = T("teleportToLobby"),
            Size = "medium",
            Callback = function()
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = LobbyPosition
                end
            end
        })
        
        mainTab:Button({
            Title = T("teleportTool"),
            Size = "medium",
            Callback = function()
                giveTeleportTool()
            end
        })
        
        -- =============================================
        -- 👁️ ESP TAB (Enhanced from Rise)
        -- =============================================
        local espTab = Window:Tab({
            Title = T("espTab"),
            Icon = "eye"
        })
        
        espTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "ميزات الرؤية المتقدمة" or "Advanced ESP Features",
            Desc = "",
            Image = "eye",
            ImageSize = 24
        })
        
        -- Highlights Section
        espTab:Label(T("highlights") or "Highlights (Chams)")
        espTab:Toggle(T("innocentHighlight"), "", _G.InnocentHighlights, function(v) _G.InnocentHighlights = v; manageESPConnections() end)
        espTab:Toggle(T("murderHighlight"), "", _G.MurderHighlights, function(v) _G.MurderHighlights = v; manageESPConnections() end)
        espTab:Toggle(T("sheriffHighlight"), "", _G.SheriffHighlights, function(v) _G.SheriffHighlights = v; manageESPConnections() end)
        espTab:Toggle(T("gunHighlight"), "", _G.GunHighlights, function(v) _G.GunHighlights = v; manageESPConnections() end)
        espTab:Toggle(T("coinHighlight"), "", _G.CoinHighlights, function(v) _G.CoinHighlights = v; manageESPConnections() end)
        
        espTab:Line()
        
        -- 3D/2D Mode Toggles
        espTab:Label(T("mode3D") .. " / " .. T("mode2D"))
        espTab:Toggle(T("innocentESP") .. " 3D", "", false, function(v) _G.InnocentESP.boxType = v and "3D" or "2D"; manageESPConnections() end)
        espTab:Toggle(T("murdererESP") .. " 3D", "", false, function(v) _G.MurderESP.boxType = v and "3D" or "2D"; manageESPConnections() end)
        espTab:Toggle(T("sheriffESP") .. " 3D", "", false, function(v) _G.SheriffESP.boxType = v and "3D" or "2D"; manageESPConnections() end)
        espTab:Toggle(T("gunDropESP") .. " 3D", "", true, function(v) _G.GunESP.boxType = v and "3D" or "2D"; manageESPConnections() end)
        espTab:Toggle(T("coinESP") .. " 3D", "", true, function(v) _G.CoinESPConfig.boxType = v and "3D" or "2D"; manageESPConnections() end)
        
        espTab:Line()
        
        -- Box ESP
        espTab:Label(T("boxESP"))
        espTab:Toggle(T("innocentESP") .. " " .. T("boxESP"), "", false, function(v) _G.InnocentESP.boxes = v; manageESPConnections() end)
        espTab:Toggle(T("murdererESP") .. " " .. T("boxESP"), "", false, function(v) _G.MurderESP.boxes = v; manageESPConnections() end)
        espTab:Toggle(T("sheriffESP") .. " " .. T("boxESP"), "", false, function(v) _G.SheriffESP.boxes = v; manageESPConnections() end)
        espTab:Toggle(T("gunDropESP") .. " " .. T("boxESP"), "", false, function(v) _G.GunESP.boxes = v; manageESPConnections() end)
        espTab:Toggle(T("coinESP") .. " " .. T("boxESP"), "", false, function(v) _G.CoinESPConfig.boxes = v; manageESPConnections() end)
        
        espTab:Line()
        
        -- Name ESP
        espTab:Label(T("nameESP"))
        espTab:Toggle(T("innocentESP") .. " " .. T("nameESP"), "", false, function(v) _G.InnocentESP.names = v; manageESPConnections() end)
        espTab:Toggle(T("murdererESP") .. " " .. T("nameESP"), "", false, function(v) _G.MurderESP.names = v; manageESPConnections() end)
        espTab:Toggle(T("sheriffESP") .. " " .. T("nameESP"), "", false, function(v) _G.SheriffESP.names = v; manageESPConnections() end)
        espTab:Toggle(T("gunDropESP") .. " " .. T("nameESP"), "", false, function(v) _G.GunESP.names = v; manageESPConnections() end)
        espTab:Toggle(T("coinESP") .. " " .. T("nameESP"), "", false, function(v) _G.CoinESPConfig.names = v; manageESPConnections() end)
        
        espTab:Line()
        
        -- Tracer ESP
        espTab:Label(T("tracerESP"))
        espTab:Toggle(T("innocentESP") .. " " .. T("tracerESP"), "", false, function(v) _G.InnocentESP.tracers = v; manageESPConnections() end)
        espTab:Toggle(T("murdererESP") .. " " .. T("tracerESP"), "", false, function(v) _G.MurderESP.tracers = v; manageESPConnections() end)
        espTab:Toggle(T("sheriffESP") .. " " .. T("tracerESP"), "", false, function(v) _G.SheriffESP.tracers = v; manageESPConnections() end)
        espTab:Toggle(T("gunDropESP") .. " " .. T("tracerESP"), "", false, function(v) _G.GunESP.tracers = v; manageESPConnections() end)
        espTab:Toggle(T("coinESP") .. " " .. T("tracerESP"), "", false, function(v) _G.CoinESPConfig.tracers = v; manageESPConnections() end)
        
        espTab:Line()
        
        -- Distance ESP
        espTab:Label(T("distanceESP"))
        espTab:Toggle(T("innocentESP") .. " " .. T("distanceESP"), "", false, function(v) _G.InnocentESP.distance = v; manageESPConnections() end)
        espTab:Toggle(T("murdererESP") .. " " .. T("distanceESP"), "", false, function(v) _G.MurderESP.distance = v; manageESPConnections() end)
        espTab:Toggle(T("sheriffESP") .. " " .. T("distanceESP"), "", false, function(v) _G.SheriffESP.distance = v; manageESPConnections() end)
        espTab:Toggle(T("gunDropESP") .. " " .. T("distanceESP"), "", false, function(v) _G.GunESP.distance = v; manageESPConnections() end)
        espTab:Toggle(T("coinESP") .. " " .. T("distanceESP"), "", false, function(v) _G.CoinESPConfig.distance = v; manageESPConnections() end)
        
        -- X-Ray (keep original)
        espTab:Line()
        espTab:Toggle(T("xrayVision"), "", _G.XrayEnabled, function(v) _G.XrayEnabled = v; setupXray() end)
        
        -- =============================================
        -- 🏃 PLAYER TAB
        -- =============================================
        local playerTab = Window:Tab({
            Title = T("playerTab"),
            Icon = "user"
        })
        
        playerTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "حركة اللاعب" or "Player Movement",
            Desc = "",
            Image = "user",
            ImageSize = 24
        })
        
        local walkspeedInput = playerTab:Input({
            Title = T("walkspeedAdjustment"),
            Value = tostring(_G.WalkspeedValue),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.WalkspeedValue = num
                    if player.Character and player.Character:FindFirstChild("Humanoid") then
                        player.Character.Humanoid.WalkSpeed = num
                    end
                end
            end
        })
        
        local jumpPowerInput = playerTab:Input({
            Title = T("jumpPowerAdjustment"),
            Value = tostring(_G.JumpPowerValue),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.JumpPowerValue = num
                    if player.Character and player.Character:FindFirstChild("Humanoid") then
                        player.Character.Humanoid.JumpPower = num
                    end
                end
            end
        })
        
        local gravityInput = playerTab:Input({
            Title = T("gravityAdjustment"),
            Value = tostring(_G.GravityValue),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.GravityValue = num
                    workspace.Gravity = num
                end
            end
        })
        
        local fovInput = playerTab:Input({
            Title = T("fovAdjustment"),
            Value = tostring(_G.FOVValue),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.FOVValue = num
                    workspace.CurrentCamera.FieldOfView = num
                end
            end
        })
        
        local flySpeedInput = playerTab:Input({
            Title = T("flySpeedAdjustment"),
            Value = tostring(_G.FlySpeedValue),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.FlySpeedValue = num
                end
            end
        })
        
        playerTab:Toggle({
            Title = T("infiniteJump"),
            Value = _G.InfiniteJumpEnabled,
            Size = "medium",
            Callback = function(state)
                _G.InfiniteJumpEnabled = state
                setupInfiniteJump()
            end
        })
        
        playerTab:Toggle({
            Title = T("noclipAdjustment"),
            Value = _G.NoclipEnabled,
            Size = "medium",
            Callback = function(state)
                _G.NoclipEnabled = state
                setupNoclip()
            end
        })
        
        playerTab:Toggle({
            Title = T("flyMode"),
            Value = _G.FlyEnabled,
            Size = "medium",
            Callback = function(state)
                _G.FlyEnabled = state
                setupFly()
            end
        })
        
        playerTab:Line()
        playerTab:Button({
            Title = T("unlockEmotes"),
            Size = "medium",
            Callback = function()
                unlockAllEmotes()
            end
        })
        
        -- Auto Farm Section
        playerTab:Line()
        playerTab:Label(_G.SelectedLanguage == "Saudi" and "جمع العملات التلقائي" or "Auto Farm")
        
        playerTab:Toggle({
            Title = T("autofarm"),
            Value = _G.AutoFarmEnabled,
            Size = "medium",
            Callback = function(state)
                _G.AutoFarmEnabled = state
                if state then
                    if farmConnection then task.cancel(farmConnection) end
                    farmConnection = task.spawn(farmLoop)
                else
                    if farmConnection then
                        task.cancel(farmConnection)
                        farmConnection = nil
                    end
                end
            end
        })
        
        playerTab:Dropdown({
            Title = T("movementMode"),
            Values = {"Teleport", "Smooth", "Walk"},
            Value = _G.FarmMode,
            Size = "medium",
            Callback = function(value)
                _G.FarmMode = value
            end
        })
        
        playerTab:Slider({
            Title = T("teleportDelay"),
            Value = _G.TeleportDelay,
            Min = 0,
            Max = 2,
            Size = "medium",
            Callback = function(value)
                _G.TeleportDelay = tonumber(value)
            end
        })
        
        playerTab:Slider({
            Title = T("smoothSpeed"),
            Value = _G.FarmSpeed,
            Min = 20,
            Max = 200,
            Size = "medium",
            Callback = function(value)
                _G.FarmSpeed = tonumber(value)
            end
        })
        
        playerTab:Slider({
            Title = T("checkInterval"),
            Value = _G.CheckInterval,
            Min = 0.1,
            Max = 2,
            Size = "medium",
            Callback = function(value)
                _G.CheckInterval = tonumber(value)
            end
        })
        
        -- =============================================
        -- 🛡️ INNOCENT TAB
        -- =============================================
        local innocentTab = Window:Tab({
            Title = T("innocentTab"),
            Icon = "shield"
        })
        
        innocentTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "ميزات الأبرياء" or "Innocent Features",
            Desc = "",
            Image = "shield",
            ImageSize = 24
        })
        
        innocentTab:Button({
            Title = T("getDroppedGun"),
            Size = "medium",
            Callback = function()
                local gun = findGun()
                if gun and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = gun.CFrame + Vector3.new(0, 3, 0)
                end
            end
        })
        
        innocentTab:Toggle({
            Title = T("autoGetDroppedGun"),
            Value = _G.AutoGrabEnabled,
            Size = "medium",
            Callback = function(state)
                _G.AutoGrabEnabled = state
                if state then
                    task.spawn(AutoGrabLoop)
                end
            end
        })
        
        innocentTab:Dropdown({
            Title = T("grabMode"),
            Values = {T("grabOnly"), T("grabAndShoot")},
            Value = _G.GrabMode,
            Size = "medium",
            Callback = function(value)
                _G.GrabMode = value
            end
        })
        
        innocentTab:Button({
            Title = T("manualGrab"),
            Size = "medium",
            Callback = function()
                ManualGrab()
            end
        })
        
        innocentTab:Toggle({
            Title = T("notifyGunEvents"),
            Value = _G.NotifyGunEvents,
            Size = "medium",
            Callback = function(state)
                _G.NotifyGunEvents = state
            end
        })
        
        innocentTab:Line()
        
        innocentTab:Button({
            Title = T("getGunAndShoot"),
            Size = "medium",
            Callback = function()
                local murderer = findMurderer()
                if murderer and murderer.Character and murderer.Character:FindFirstChild("HumanoidRootPart") then
                    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        player.Character.HumanoidRootPart.CFrame = murderer.Character.HumanoidRootPart.CFrame + Vector3.new(0, 0, 5)
                    end
                end
            end
        })
        
        innocentTab:Line()
        
        -- God Mode Section
        innocentTab:Label(T("godMode"))
        
        innocentTab:Toggle({
            Title = T("godMode"),
            Value = _G.GodModeEnabled,
            Size = "medium",
            Callback = function(state)
                _G.GodModeEnabled = state
                if state then                    applyGodMode()
                    startGodMode()
                else
                    stopGodMode()
                end
            end
        })
        
        innocentTab:Dropdown({
            Title = T("godModeMethod"),
            Values = {"Health Math.huge", "Humanoid Replacement (Very buggy)"},
            Value = _G.GodModeMethod,
            Size = "medium",
            Callback = function(value)
                _G.GodModeMethod = value
                if _G.GodModeEnabled then
                    applyGodMode()
                end
            end
        })
        
        innocentTab:Line()
        
        -- Fling Section
        innocentTab:Label(_G.SelectedLanguage == "Saudi" and "التطويع" or "Fling")
        
        local flingInput = innocentTab:Input({
            Title = T("flingTargetInput"),
            Value = "",
            Size = "medium",
            Callback = function(value)
                flingTargetInput = value
            end
        })
        
        innocentTab:Dropdown({
            Title = T("flingMode"),
            Values = {T("flingMode1"), T("flingMode2"), T("flingMode3")},
            Value = T("flingMode1"),
            Size = "medium",
            Callback = function(value)
                if value == T("flingMode1") then flingMode = 1
                elseif value == T("flingMode2") then flingMode = 2
                else flingMode = 3 end
            end
        })
        
        innocentTab:Toggle({
            Title = T("flingActive"),
            Value = flingActive,
            Size = "medium",
            Callback = function(state)
                flingActive = state
                if state then
                    startFling()
                end
            end
        })
        
        innocentTab:Button({
            Title = T("flingMurdererBtn"),
            Size = "medium",
            Callback = function()
                flingTargetInput = "murder"
                flingActive = true
                startFling()
                task.wait(10)
                flingActive = false
            end
        })
        
        innocentTab:Button({
            Title = T("flingSheriffBtn"),
            Size = "medium",
            Callback = function()
                flingTargetInput = "sheriff"
                flingActive = true
                startFling()
                task.wait(10)
                flingActive = false
            end
        })
        
        -- =============================================
        -- 🔪 MURDERER TAB
        -- =============================================
        local murdererTab = Window:Tab({
            Title = T("murdererTab"),
            Icon = "droplet"
        })
        
        murdererTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "ميزات القتلة" or "Murderer Features",
            Desc = "",
            Image = "droplet",
            ImageSize = 24
        })
        
        murdererTab:Toggle({
            Title = T("aimbot"),
            Value = _G.AimbotEnabled,
            Size = "medium",
            Callback = function(state)
                _G.AimbotEnabled = state
                setupAimbot()
            end
        })
        
        local playerList = {}
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= player then
                table.insert(playerList, p.Name)
            end
        end
        
        local selectedPlayer = nil
        murdererTab:Dropdown({
            Title = T("selectPlayer"),
            Values = playerList,
            Value = playerList[1] or "",
            Size = "medium",
            Callback = function(selected)
                selectedPlayer = selected
            end
        })
        
        murdererTab:Button({
            Title = T("refreshPlayers"),
            Size = "medium",
            Callback = function()
                local newList = {}
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= player then
                        table.insert(newList, p.Name)
                    end
                end
            end
        })
        
        murdererTab:Button({
            Title = T("killSelected"),
            Size = "medium",
            Callback = function()
                if selectedPlayer then
                    local target = Players:FindFirstChild(selectedPlayer)
                    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                            player.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame
                        end
                    end
                end
            end
        })
        
        local killAllToggle = murdererTab:Toggle({
            Title = T("killAllPlayers"),
            Value = _G.KillAllEnabled,
            Size = "medium",
            Callback = function(state)
                _G.KillAllEnabled = state
                if state then
                    setupKillAll()
                else
                    if espConnections.killAll then
                        espConnections.killAll:Disconnect()
                        espConnections.killAll = nil
                    end
                    for p, _ in pairs(frozenPlayers) do
                        if p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                            p.Character.HumanoidRootPart.Anchored = false
                        end
                    end
                    frozenPlayers = {}
                end
            end
        })
        
        murdererTab:Button({
            Title = T("killSheriff"),
            Size = "medium",
            Callback = function()
                local sheriff = findSheriff()
                if sheriff and sheriff.Character and sheriff.Character:FindFirstChild("HumanoidRootPart") then
                    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        player.Character.HumanoidRootPart.CFrame = sheriff.Character.HumanoidRootPart.CFrame
                    end
                end
            end
        })
        
        -- =============================================
        -- 🤠 SHERIFF TAB (Enhanced with Auto Shoot)
        -- =============================================
        local sheriffTab = Window:Tab({
            Title = T("sheriffTab"),
            Icon = "target"
        })
        
        sheriffTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "ميزات الشرطة" or "Sheriff Features",
            Desc = "",
            Image = "target",
            ImageSize = 24
        })
        
        sheriffTab:Toggle({
            Title = T("autoShoot"),
            Value = _G.AutoShootEnabled,
            Size = "medium",
            Callback = function(state)
                _G.AutoShootEnabled = state
                if state then
                    task.spawn(startAutoShoot)
                end
            end
        })
        
        sheriffTab:Toggle({
            Title = T("manualShoot"),
            Value = false,
            Size = "medium",
            Callback = function(state)
                if state then
                    createSheriffGui()
                else
                    removeSheriffGui()
                end
            end
        })
        
        sheriffTab:Slider({
            Title = T("shootPrediction"),
            Value = _G.ShootPrediction,
            Min = 0,
            Max = 20,
            Size = "medium",
            Callback = function(value)
                _G.ShootPrediction = tonumber(value)
            end
        })
        
        sheriffTab:Slider({
            Title = T("pingMultiplier"),
            Value = _G.PingMultiplier,
            Min = 0,
            Max = 10,
            Size = "medium",
            Callback = function(value)
                _G.PingMultiplier = tonumber(value)
            end
        })
        
        sheriffTab:Button({
            Title = T("shootNow"),
            Size = "medium",
            Callback = function()
                ShootMurderer()
            end
        })
        
        sheriffTab:Line()
        
        sheriffTab:Toggle({
            Title = T("lockCamera"),
            Value = _G.LockCameraEnabled,
            Size = "medium",
            Callback = function(state)
                _G.LockCameraEnabled = state
                setupLockCamera()
            end
        })
        
        sheriffTab:Toggle({
            Title = T("aimbotMurderer"),
            Value = _G.AimbotMurdererEnabled,
            Size = "medium",
            Callback = function(state)
                _G.AimbotMurdererEnabled = state
                setupAimbotMurderer()
            end
        })
        
        local shootDistanceInput = sheriffTab:Input({
            Title = T("shootDistance"),
            Value = tostring(_G.ShootDistance),
            Size = "medium",
            Callback = function(value)
                local num = tonumber(value)
                if num then
                    _G.ShootDistance = num
                end
            end
        })
        
        -- =============================================
        -- 📍 TELEPORT TAB
        -- =============================================
        local teleportTab = Window:Tab({
            Title = T("teleportTab"),
            Icon = "map-pin"
        })
        
        teleportTab:Paragraph({
            Title = _G.SelectedLanguage == "Saudi" and "الانتقالات السريعة" or "Quick Teleports",
            Desc = "",
            Image = "map-pin",
            ImageSize = 24
        })
        
        local teleportPlayerList = {}
        for _, p in pairs(Players:GetPlayers()) do
            table.insert(teleportPlayerList, p.Name)
        end
        
        local teleportSelected = nil
        teleportTab:Dropdown({
            Title = T("playerSelectionTeleport"),
            Values = teleportPlayerList,
            Value = teleportPlayerList[1] or "",
            Size = "medium",
            Callback = function(selected)
                teleportSelected = selected
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToSelected"),
            Size = "medium",
            Callback = function()
                if teleportSelected then
                    local target = Players:FindFirstChild(teleportSelected)
                    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                            player.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame
                        end
                    end
                end
            end
        })
        
        teleportTab:Button({
            Title = T("updatePlayersList"),
            Size = "medium",
            Callback = function()
                local newList = {}
                for _, p in pairs(Players:GetPlayers()) do
                    table.insert(newList, p.Name)
                end
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToLobbyTP"),
            Size = "medium",
            Callback = function()
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = LobbyPosition
                end
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToSheriff"),
            Size = "medium",
            Callback = function()
                local sheriff = findSheriff()
                if sheriff and sheriff.Character and sheriff.Character:FindFirstChild("HumanoidRootPart") then
                    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        player.Character.HumanoidRootPart.CFrame = sheriff.Character.HumanoidRootPart.CFrame
                    end
                end
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToMurderer"),
            Size = "medium",
            Callback = function()
                local murderer = findMurderer()
                if murderer and murderer.Character and murderer.Character:FindFirstChild("HumanoidRootPart") then
                    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        player.Character.HumanoidRootPart.CFrame = murderer.Character.HumanoidRootPart.CFrame
                    end
                end
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToRandom"),
            Size = "medium",
            Callback = function()
                teleportToRandom()
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToGun"),
            Size = "medium",
            Callback = function()
                local gun = findGun()
                if gun and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = gun.CFrame + Vector3.new(0, 3, 0)
                end
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToInnocent"),
            Size = "medium",
            Callback = function()
                teleportToInnocent()
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToCoin"),
            Size = "medium",
            Callback = function()
                teleportToCoinTP()
            end
        })
        
        teleportTab:Button({
            Title = T("teleportToMap"),
            Size = "medium",
            Callback = function()
                teleportToMap()
            end
        })
        
        -- =============================================
        -- ⚙️ SETTINGS TAB
        -- =============================================
        local settingsTab = Window:Tab({
            Title = T("settingsTab"),
            Icon = "settings"
        })
        
        settingsTab:Paragraph({
            Title = T("settings"),
            Desc = "",
            Image = "settings",
            ImageSize = 24
        })
        
        settingsTab:Toggle({
            Title = T("boostFPS"),
            Value = _G.BoostFPSEnabled,
            Size = "medium",
            Callback = function(state)
                _G.BoostFPSEnabled = state
                setupBoostFPS()
            end
        })
        
        settingsTab:Toggle({
            Title = T("fullBrightness"),
            Value = _G.FullBrightEnabled,
            Size = "medium",
            Callback = function(state)
                _G.FullBrightEnabled = state
                setupFullBright()
            end
        })
        
        settingsTab:Toggle({
            Title = T("antiAFK"),
            Value = _G.AntiAFKEnabled,
            Size = "medium",
            Callback = function(state)
                _G.AntiAFKEnabled = state
                setupAntiAFK()
            end
        })
        
        settingsTab:Toggle({
            Title = T("antiVoid"),
            Value = _G.AntiVoidEnabled,
            Size = "medium",
            Callback = function(state)
                _G.AntiVoidEnabled = state
                if state then
                    enableAntiVoid()
                else
                    disableAntiVoid()
                end
            end
        })
        
        settingsTab:Dropdown({
            Title = T("selectTheme"),
            Values = {"Dark", "Light", "Blue", "Red"},
            Value = "Dark",
            Size = "medium",
            Callback = function(selected)
                WindUI:SetTheme(selected)
            end
        })
        
        settingsTab:Divider({ Title = "" })
        
        settingsTab:Paragraph({
            Title = T("advancedSettings"),
            Desc = "",
            Image = "sliders",
            ImageSize = 24
        })
        
        settingsTab:Button({
            Title = T("rejoinServer"),
            Size = "medium",
            Callback = function()
                local ts = game:GetService("TeleportService")
                ts:Teleport(game.PlaceId)
            end
        })
        
        settingsTab:Button({
            Title = T("closeScriptBtn"),
            Size = "medium",
            Callback = function()
                Window:Destroy()
            end
        })
        
        settingsTab:Divider({ Title = "" })
        
        settingsTab:Paragraph({
            Title = T("developer"),
            Desc = "",
            Image = "user",
            ImageSize = 24
        })
        
        settingsTab:Button({
            Title = "👤 " .. T("developerName"),
            Size = "medium",
            Callback = function() end
        })
        
        settingsTab:Button({
            Title = "📱 " .. T("developerDesc"),
            Size = "medium",
            Callback = function() end
        })
        
        settingsTab:Button({
            Title = "🎮 " .. T("joinDiscord"),
            Size = "medium",
            Callback = function()
                setclipboard("https://discord.gg/CgUa36sPNs")
            end
        })
        
        settingsTab:Button({
            Title = "⚡ " .. T("version"),
            Size = "medium",
            Callback = function() end
        })
        
        Window:OnDestroy(function()
            cleanupGUI()
            clearAllHighlights()
            
            for name, connection in pairs(espConnections) do
                if connection then
                    connection:Disconnect()
                end
            end
            
            for name, loop in pairs(espLoops) do
                if loop then
                    loop:Disconnect()
                end
            end
            
            if BodyVelocity then BodyVelocity:Destroy() end
            if BodyGyro then BodyGyro:Destroy() end
            if sheriffGui then sheriffGui:Destroy() end
            if farmConnection then task.cancel(farmConnection) end
            if godModeConnection then godModeConnection:Disconnect() end
            if playerEspConnection then playerEspConnection:Disconnect() end
            if coinEspConnection then coinEspConnection:Disconnect() end
            if gunEspConnection then gunEspConnection:Disconnect() end
            if HighlightsConnection then HighlightsConnection:Disconnect() end
        end)
        
        task.wait(3)
        WindUI:Notify({
            Title = T("welcome"),
            Content = _G.SelectedLanguage == "Saudi" 
                and "أهلاً وسهلاً في PhantomXPro 🌟 (نسخة مطورة)"
                or "Welcome to PhantomXPro 🌟 (Enhanced Version)",
            Duration = 6
        })
        
        -- Start ESP connections
        manageESPConnections()
    end
    
    -- نافذة اختيار اللغة
    WindUI:Popup({
        Title = "🌍 اختر اللغة / Select Language",
        Icon = "globe",
        Content = "اختر لغتك المفضلة:\nChoose your preferred language:",
        Buttons = {
            {
                Title = "🇸🇦 عربي",
                Icon = "flag",
                Variant = "Primary",
                Callback = function()
                    _G.SelectedLanguage = "Saudi"
                    WindUI:Notify({
                        Title = "✅ تم الاختيار",
                        Content = "تم اختيار اللغة العربية",
                        Duration = 3
                    })
                    task.wait(1)
                    createMainScript()
                end
            },
            {
                Title = "🇺🇸 English",
                Icon = "flag",
                Variant = "Secondary",
                Callback = function()
                    _G.SelectedLanguage = "English"
                    WindUI:Notify({
                        Title = "✅ Selected",
                        Content = "English language selected",
                        Duration = 3
                    })
                    task.wait(1)
                    createMainScript()
                end
            }
        }
    })
else
    print("❌ فشل تحميل WindUI / Failed to load WindUI")
end
print("Done")